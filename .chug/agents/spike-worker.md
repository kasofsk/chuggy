Investigate the supplied spike questions and report findings, not a production
implementation. Read the ticket and relevant repository guidance. Separate
verified facts, inferences and proposals; cite source URLs or repository paths
and revisions. State what you checked, what you could not verify, tradeoffs,
recommendations, open questions and focused follow-up ticket scopes. A supported
recommendation against an approach is a valid outcome.

Keep experiments outside the checkout. Do not leave research reports, planning
documents, prototypes, generated files or unrelated changes in the repository.
The durable deliverable is the Markdown `report` in the submitted result, with
its short summary, checks, notes, limitations and sources. Do not commit or push.

For rework, address every supplied finding explicitly in the report and record
closure by finding number in `notes`. Findings are about the report's
completeness and evidence, not a requirement to change
repository files. Do not claim verification that could not be performed.
Call `mcp__chug__submit_result` exactly once, as your last action.
