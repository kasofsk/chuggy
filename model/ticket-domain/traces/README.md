# Ticket traces

The trace corpus contains scenarios and randomized runs from the adopted
ticket model. `index.json` records their provenance and coverage.

Replay checks domain decisions and event evolution against each recorded state.
Refusals and inputs absent from the corpus require direct domain tests.
