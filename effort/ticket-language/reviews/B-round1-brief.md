# Review: Task B round 1 (interpreter, adapters, contract; the schema merged)

You are a fresh reviewer who did not author this change. Work in
`~/claude/chuggy-wt/no-accounts-review-b`, detached at eb7f1fdb (Task B's
tip; `node_modules` linked). Read `.chug/tasks/review-change.md` there
first; verdicts APPROVE / CHANGES / ESCALATE only. Then
`~/claude/chuggy-effort/ticket-language/GOAL.md`, `tasks/B.md` (the brief,
including its three appended sections), and `tasks/B-report.md` (the
author's claim, not evidence). `reviews/S-round1.md` and `reviews/A-round1.md`
/`A-round2.md` cover the schema and the model; do not redo them.

Scope: `git diff dc867998..eb7f1fdb` minus the merge of `schema/no-accounts`
(cf708286, already reviewed). Six commits by Task B. One of them,
`8c7cfcfd`, edits the landed baseline migration; that is already known and
is being moved into migration 004 on another branch — do not report it,
but DO check whether the new figure it renders is the right one (derive
`sessionTurnInputCharsMax` from `src/contract/http.ts` and compare).

Docker is running; `.chug/tasks/check-postgres.sh` and `check-queries.sh`
run against a real server. The console is red until Task C; every other
gate must be green here — run `check-source` and confirm the only red
files are under `ui/chuggy-ui/`.

Check explicitly:
- **The journal chain (`cc70361e`).** The author says the digest used to
  verify rows by re-encoding the decoded row, and that after the model
  change no pre-3 row re-encodes to its bytes, so the service would refuse
  to start against any database that has run. Verify the claim on main
  (does main's verification re-encode?), verify the fix chains over the
  stored column, and verify a stored history at semantics 1 and 2 with
  pricing fields and bare-int `EvalReduce` rows loads and replays. Then
  ask the harder question: does chaining over stored bytes weaken what
  the chain proved before (a row whose stored text differs from what its
  decoded event would encode is now accepted — is that a hole?), and is
  `postgresJournalDispatchContracts` parsing through the same seam sound.
- **The pre-3 lift.** Where the store decodes a stored row: a semantics
  < 3 bare-int `EvalReduce` is rewritten with the disposition from its
  record; a semantics-3 bare-int row is refused; tests for the escalated
  rec, the reworked rec, and the refusal all exist and can go red.
- **The rework cap.** `rework.cyclesMax` in the ticket-service config;
  `workCyclesStarted` in `src/domain/task.ts` counts maximal runs of
  Work-kind tasks in id order (probe: a fanout-2 work cycle counts once;
  a rework after an eval failure counts a second cycle); the disposition
  is decided where the writer has the core and journaled on the event;
  `cyclesMax: 2` allows exactly two reworks and escalates the third
  failure. Red-proof by flipping the comparison.
- **The deployment-policy pin** compares `::jsonb`, refuses a differing
  config, and a row rendered as 004 renders it matches the image's
  encoding (the coupling test S's reviewer asked for).
- **The wire.** `escalationReasons` lost two literals; the ticket view
  lost four fields; the authoring options lost three pages; the contract
  document was regenerated by its test, not hand-edited (diff it against
  what the test emits).
- **The `dispatchViewSchemaVersion` decision**: the author did not bump
  it; judge whether a cached view at the old version can be misread.
- **Attribution**: one commit carries a different Co-Authored-By; note it,
  not a finding.
- `check-postgres` (75 suites), `check-queries`, `check-conformance`,
  `check-boundaries`, `check-comments`, `check-paths`, `check-figures`:
  run them. Exit 2 is could-not-run.

Write to `~/claude/chuggy-effort/ticket-language/reviews/B-round1.md`,
verdict first; each finding names a failure that actually happens
(file:line, input, effect). No style nits without a failure. Do not edit
any branch.
