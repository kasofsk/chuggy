# Review: PR 2 Task A round 1 (model, goldens, domain, actor semantics 4)

Fresh reviewer; you did not author the change. Work in
`~/claude/chuggy-wt/three-deletions-review-a`, detached at c64bb04c
(`node_modules` linked). Read `.chug/tasks/review-change.md` first;
verdicts APPROVE / CHANGES / ESCALATE. Commit nothing; revert scratch with
`git checkout -- .`. Then `GOAL.md`, `tasks/A.md`, and `tasks/A-report.md`
(the author's claim) under `~/claude/chuggy-effort/ticket-language/pr2/`.
PR 1's `../reviews/A-round1.md` and `A-round2.md` show what that round
caught: stale prose citing deleted symbols was the whole of it. Do that
sweep again here, and then the harder checks below.

Scope: `git diff 617675bb..c64bb04c` (five commits; nothing under
`src/interpreter`, `src/adapters`, `src/contract`, `ui/` beyond the forced
parameter drops the report lists).

Check:
- **The model.** `model/ticket.qnt`, `domain.qnt`, `refinement.qnt`,
  `mc/`, `tests/`: no surviving mention of Combinator, AnyPass,
  UnanimousPass, Finalizer, NoFinalizer, ManagedFinalizer,
  DependencyRevoked, cascadeSafety, revokeDoomed, noStructuralDeadlock,
  modeledResumeExists, wrapup_none — as a symbol OR in prose (80-column
  wraps: grep line-joined). Every comment that argued the cascade now
  argues what the code beside it does. `decideRevoke` transitions one
  ticket with one CancelTicketWork. A passing final stage always enters
  Finalizing. `deskConsistent`'s new claim ("every wall has a resume") is
  true of every Reason left. `allInvariants` lists what exists. The witness
  roster in `check-model.sh` matches `model/tests/`.
- **The deadlock claim.** `noStructuralDeadlock` left. What, if anything,
  now states that a Pending ticket behind a Revoked dependency is a state
  the machine accepts? If `stuckSubsetCovered` or another invariant
  still asserts coverage of stuck tickets, does a stranded Pending ticket
  falsify it — run `check-model` and the randomized invariants yourself and
  read the invariant text, not the report.
- **Goldens.** Regenerated by the pinned quint from `manifest.json`;
  `nofinalizer-completion` gone; the committed ITF files equal a fresh
  emission (run `emit-goldens.sh` into scratch and diff); `corpus.ts` and
  `coverage.test.ts` name only what exists.
- **Domain.** `revokedDependencies(core, id)`: direct deps only, id order,
  empty when none; a Revoked dep of a Done ticket (can it happen?); a dep
  that is itself Pending behind a Revoked grand-dep returns empty here
  (transitive is blocked through its parent — is that what the console
  needs? judge, don't fix). `derived.ts`/`invariants.ts` lost revokeDoomed
  and cascadeSafety and nothing else.
- **Semantics 4** (`src/actor/decisionSemantics.ts`): the author says the
  actor cannot see NoFinalizer/AnyPass because zod strips the keys, so
  refusal reads the record: `ticket-done` leaving any phase but
  Finalizing, `ticket-revoked` with more than one transition. Verify the
  codec really strips (feed a pinned pre-4 row with `finalizer:
  NoFinalizer`); verify a pre-4 ManagedFinalizer row and a UnanimousPass
  stage replay; verify both refusals and that the refusal message names
  the row; verify a ≤3 EvalReduce bare-int row still lifts (PR 1's
  correction survives). Flip each of the eight mutations the report names
  and confirm red; find one it does not name.
- **Conformance**: `test/conformance/evaluate.ts`'s guard change — the
  control still proves a throwing invariant is caught; red-proof it.
- **Prose**: `check-comments`, `check-figures`, `check-paths`; every
  touched comment read against its code; no quantity.
- Gates: `check-conformance`, `check-random`, `check-model`,
  `check-boundaries` green; `check-source` red only on the seventeen files
  the report lists.

Write `~/claude/chuggy-effort/ticket-language/pr2/reviews/A-round1.md`,
verdict first; each finding names a real failure at file:line.
