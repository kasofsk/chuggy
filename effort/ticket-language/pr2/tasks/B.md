# Task B (PR 2): the interpreter, adapters and contract lose the three

Worktree `~/claude/chuggy-wt/three-deletions`, branch `model/three-deletions`
at Task A's tip (read `tasks/A-report.md` first: the domain, generated types
and semantics 4 are already there; `schema/three-deletions` is merged in,
read `tasks/S-report.md` for what 005 did). Read `.chug/tasks/review-change.md`,
`GOAL.md` here, and PR 1's `../reviews/B-round1.md` and `../reviews/B-round2.md`
for the shapes that round caught (a claim about "the old meaning" that was
false for one ticket kind; a narrowed CHECK outside its guard).

Scope:
1. **Contract** (`src/contract/`): `finalizers`/`FinalizerChoice` leave;
   `briefFinalizationModes` gains `"None"` with `briefFinalizationProposes`
   false for it; `escalationReasons` loses `DependencyRevoked`; authoring
   loses `finalizer` and each stage's `combinator`; the ticket read gains
   `revokedDependencies: readonly number[]`; the contract document is
   regenerated by its test.
2. **Authoring / dispatch view / selector** (`src/interpreter/authoring.ts`,
   `dispatchView.ts`, `src/adapters/postgres/dispatchViews.ts`,
   `selector.ts`): `checkedDraftLanding` leaves; the dispatch view loses
   `finalizer` and `combinator` (bump `dispatchViewSchemaVersion` only if a
   cached view at the old version can be misread — say which way you
   decided and why); the release command carries neither.
3. **Finalizer** (`src/interpreter/finalizer*.ts`, `src/adapters/postgres/finalizer.ts`):
   a brief whose finalization mode is `None` reports
   `FinalizationSucceeded` at once — no forge call, no artifact read, no
   proposal — and the journal shows one FinalizationResult. Every ticket
   now enters Finalizing, so a ticket with no `finalization` in its brief
   must be decided: either the default landing (`landing-default-2026-09-12`
   made a per-repository default) or `None`; read `finalizerSettings.ts`
   and the repository landing default and pick the one the existing default
   already implies, stating it in the report.
4. **Ticket read** (`src/adapters/postgres/nativeReads.ts` and the wire's
   ticket resource): `revokedDependencies` from the domain's derivation over
   the core the read already holds, or from the projection's dependency and
   phase rows if the read has no core — say which.
5. **Journal store**: the decoder at semantics ≤3 drops `finalizer =
   ManagedFinalizer` and `combinator = UnanimousPass` (Task A did the
   semantics side; you do the postgres row seam if it is separate), and the
   refusals surface at startup with the message naming the row.
6. Tests: every touched suite; `test/contract/responses.test.ts` pins the
   emitted key set; a finalizer run test for `None`; a ticket read test for
   a Pending ticket behind a Revoked dependency listing it and a Done one
   listing nothing; red-proof each and name the mutation.

Gates: `check-postgres`, `check-queries`, `check-conformance`,
`check-boundaries`, `check-comments`, `check-figures`, `check-paths`,
`node --test`. `check-source` red only on console files C owns — list them.
Commits end `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`;
`--no-verify` only for that red. Report to `tasks/B-report.md`.

## From Task S (read before starting)

- `test/adapters/nativeReads.test.ts` ~413 seeds a `DependencyRevoked` row;
  under 005 the narrowed CHECK refuses it and the suite HANGS idle-in-
  transaction rather than going red. Change the literal; do not chase it.
- `decision.ts:234` and `dispatchViews.ts:120` still write
  `dispatch_candidate.finalizer`; `authoring.test.ts` has six cases pinning
  the deleted NoFinalizer draft arm.
- `create_draft`/`revise_draft` now resolve a landing for every draft, so a
  `None` mode reaches `draft_brief`; 005 widens the two landing-mode CHECKs.
  S's report says whether NULL `finalization_mode` rows were rewritten to
  `None` or left meaning "repository default" — follow whichever it says.
- S's addendum: NULL `finalization_mode` meant only the NoFinalizer arm and
  005 rewrites it to `None`, so B never treats NULL as default. The roster
  gaining `"None"` and 005 ship in one release: `asBriefFinalization`
  (`ticketBrief.ts` ~241) throws on a mode outside the roster.
