# Review brief — fix/cascade-rows-replay (tip aef4c3b7, three commits off main ba0c5a68)

Worktree `~/claude/chuggy-wt/cascade-rows`. You did not write this change. Read `CLAUDE.md` and `.chug/tasks/review-change.md` first, then `GOAL.md` in this directory (what went wrong and what was asked), then `git log -3 -p` on the branch.

Judge, in this order: (1) does a stored semantics-1/2/3 cascade row in the rig's shape replay legal under `storedJournalLegalOn`, with each parked dependent's later `Revoke` from Escalated also legal — trace it by hand against `decideRevoke`/`recordEquals`, do not take the test's word; (2) is anything refused that the old machine could have written (a cascade whose revoked ticket was Working or Escalated; dependents parked from Pending only; effect order `CancelTicketWork` then one `OpenHumanTask` per dependent); (3) is anything admitted that the current machine would not write at semantics 4; (4) 005's in-place edit: is the guard's remaining text true, is the header true, does `migration.test.ts` prove the cascade row is admitted; (5) docs and comments at the tree's bar (no figure a reader must trust, no claim about a path that is not there). A finding must name a failure that actually happens with concrete input. Verdict APPROVE or CHANGES with findings ranked; write it to `~/claude/chuggy-effort/ticket-language/pr2-fix/review-round1.md` and hand back the verdict and findings in under 30 lines.
