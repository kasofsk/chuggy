# Review brief, round 1 (surface half) — model/rename, tip 64e5abb5, off main 55de9de6

Worktree `~/claude/chuggy-wt/rename-review-surface` (detached at the tip; run `npm ci` at the root only first (the console is a workspace of the root; an install inside it wipes the root)). You did not write this change. Read `CLAUDE.md` and `.chug/tasks/review-change.md` first, then `GOAL.md` in `~/claude/chuggy-effort/ticket-language/pr3/` (the decisions; do not reopen them, judge whether the code keeps them), `survey.md` §1, §5–§12, the memory rule that console copy is nouns and one-word statuses (`~/.claude/projects/-home-geoff-claude-chuggy/memory/chuggy-ui-copy-standard.md`), then `git diff main...HEAD --stat` and the diff of your half: `src/interpreter/`, `src/adapters/` (not `schema/`), `src/contract/`, `ui/chuggy-ui/`, `test/interpreter/`, `test/adapters/`, `test/contract/`, `test/ui/`, `ui/chuggy-ui/test/`. Another reviewer holds the model, actor, migration and goldens.

This PR is a rename: language only, no machine change. Judge, in this order:

1. **The fabric boundary does not move.** `executionTaskKinds`, `execution_request_task.kind`, `CHUG_WORKER_TASK.taskKind`, `images/worker/*`, effect strings, `Verdict` are byte-identical to main; the domain's `WorkTask`/`EvaluationTask` maps to the fabric's `Work`/`Evaluation` at the adapter and back. Find every place a domain task kind reaches a fabric-facing string or a fabric string reaches the domain, and check each maps. A worker running today's image must be able to complete a task dispatched by this branch.
2. **The evidence reaches the reader.** `executionBlockedBy` on the single ticket read: the tagged query in `src/adapters/postgres/nativeReads.ts` (most recent `outcome='Blocked'` execution's `blocked_reason`, only while `reason='WorkExecutionUnavailableEscalated'`); trace a ticket parked by the kubernetes adapter (`ExecutionProfileUnavailable`) from `submit_execution_outcome` through the ticket's `reason` to the response and the console's `TicketSituation`. Then the continuation path (`projectWriter.ts` lost `executionSourceBlockedReason`): is a ticket parked for an unreadable source read with `executionBlockedBy` present or absent, and does the response schema and the console tolerate absent? GOAL.md and B's report say the RemoteDenied distinction is deferred to PR 5; check that the wall name at least reaches the execution row.
3. **The stored-row readers.** Every reader of a journal or decision row in your half goes through the actor's normalising decoder; `checkedFinalizationSubmission` in `wire.ts` lifts a stored `FinalizationFailed` (Task M's addition, `tasks/M-report.md`); `storedSchedulerCompletion` lifts before decoding. Name any reader that compares against a spelling directly.
4. **The wire and contract.** `escalationReasons` is the three; `ticketResponseSchema` carries `executionBlockedBy` optional; the contract document and fixtures follow; `resumeAt` and phase rosters renamed; nothing on the wire still spells an old name (grep the branch for `Working|Evaluating|Finalizing|ResumeWorking|ReleaseTicket|WorkFailed|ReworkBudgetExhausted|FinalizationFailed` and account for every hit: copy that stays, an old-spelling admit, or a miss).
5. **Console**: phase words are the product's existing copy ("Working" for `Work` etc. — a product choice kept, not the constructor); the three reason labels plus the wall label off `executionBlockedBy`; `Stage` headers and `ConversationWorkCard`'s "Working" stay; fixtures pass `ticketResponseSchema`. Run `check-console.sh` and `check-source.sh` and report their exits.
6. **Docs and comments** at the tree's bar: no figure a reader must trust, no claim naming a path that is not there, no comment still saying an old name in your half (`src/adapters/postgres/schema/README.md` is the machine reviewer's).

A finding must name a failure that actually happens with concrete input; "could be cleaner" is not a finding. Verdict APPROVE or CHANGES with findings ranked; write it to `~/claude/chuggy-effort/ticket-language/pr3/reviews/round1-surface.md` and hand back the verdict and findings in under 30 lines.
