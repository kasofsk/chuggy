# Review brief, round 1 (machine half) — model/rename, tip 64e5abb5, off main 55de9de6

Worktree `~/claude/chuggy-wt/rename-review-machine` (detached at the tip; run `npm ci` at the root only first (the console is a workspace of the root; an install inside it wipes the root)). You did not write this change. Read `CLAUDE.md` and `.chug/tasks/review-change.md` first, then `GOAL.md` in `~/claude/chuggy-effort/ticket-language/pr3/` (the decisions; do not reopen them, judge whether the code keeps them), `survey.md` §1–§4, §8, §11, then `git diff main...HEAD --stat` and the diff of your half: `model/`, `test/golden/`, `src/domain/`, `src/actor/`, `src/adapters/postgres/schema/`, `test/actor/`, `test/postgres/`, `test/conformance/`, `test/random/`. Another reviewer holds the interpreter, adapters, contract and console.

This PR is a rename: language only, no machine change. Judge, in this order:

1. **Is it only a rename?** Diff `model/ticket.qnt`, `domain.qnt`, `refinement.qnt`, `api.qnt` against main modulo the rename table in GOAL.md; anything that is not a substitution from that table is a finding (the one admitted exception: `executionBlocked` lost its `why` draw because five reasons became one; check that this is all it lost).
2. **Do stored rows still replay?** `rowAtCurrentVocabulary` in `src/actor/decisionSemantics.ts` is the one total map old→new. Trace by hand, against the code not the tests: a semantics-1 row (`test/actor/journalAtSemanticsOne*.json`, bytes unchanged), a semantics-2 cascade row in the rig's shape (`~/claude/chuggy-effort/ticket-language/pr2-fix/GOAL.md` gives it), a semantics-4 row written by main today (`ReleaseTicket`, `Working`, `WorkFailed`, `FinalizationFailed`, a wall name as `reason`), each through the decoder and its correction to `storedJournalLegalOn`. Name any field the map misses (nested task kinds inside `event`/`rec`, `resumeAt`, `out`, labels), any correction that still compares against an old spelling, and any row the map admits that main would have refused (the two removed-wall labels must stay refused). Run `~/claude/chuggy-effort/ticket-language/pr2-fix/replay-rig.ts` against this branch's `src` over `pr2-fix/rig-journal/vteng.jsonl` and report the count.
3. **Migration 006** (`src/adapters/postgres/schema/migrations/006-rename.ts`): guard first reading fields not text; the rewrite total over every old spelling in `ticket_projection.phase/.reason/.resume_at`, `native_action.reason`, `project_continuation.expected_phase`; the CHECKs restated at the new spellings only; `decision_event_is_valid` v5 and every function reading a stored spelling admitting both; `journal_entry_release_ticket` over both tags; `submit_finalization_result` taking `in_outcome` at both; the `chuggy_api` grant on `execution.blocked_reason`. Is there a row the rig could hold today (rig facts in `tasks/S.md`) that the guard refuses or the rewrite leaves at an old spelling? Render-diff every earlier migration main vs branch with `~/claude/chuggy-effort/ticket-language/scratch/B-fix0/render.mjs` (header gives usage) and report it empty or not.
4. **Goldens and witnesses**: `test/golden/manifest.json` invariants in the new names; the witness roster `check-model.sh` counts still matches; no golden hand-edited (each is what `emit-goldens.sh` emits — re-emit one and diff).
5. **Docs and comments** at the tree's bar: no figure a reader must trust, no claim naming a path that is not there, no comment still saying an old name.

A finding must name a failure that actually happens with concrete input; "could be cleaner" is not a finding. Verdict APPROVE or CHANGES with findings ranked; write it to `~/claude/chuggy-effort/ticket-language/pr3/reviews/round1-machine.md` and hand back the verdict and findings in under 30 lines.
