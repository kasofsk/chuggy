# Task F2 — the sweep's findings, and a frozen semantics-4 journal

Worktree `~/claude/chuggy-wt/rename`, branch `model/rename`, tip 9ab90d02. Root `npm ci` only if stale (never inside `ui/chuggy-ui/`). Read `CLAUDE.md`, `.chug/tasks/review-change.md`, then `~/claude/chuggy-effort/ticket-language/pr3/reviews/sweep.md` — its three findings and its "unpinned" list. Then `test/actor/decisionSemantics.test.ts` whole (how `journalAtSemanticsOne*.json` are pinned, read as bytes, and replayed) and `src/actor/decisionSemantics.ts` (`wordAtCurrentVocabulary`'s map).

## Part 1 — the three findings, one commit

1. `ui/chuggy-ui/app/core/conversationMention.ts:58` and `ui/chuggy-ui/app/browser/ui/TicketReference.tsx:70` draw `ticket.phase` raw where every other surface draws `phaseLabel(phase)`. Wrap both; restore `conversationMention.test.ts:86` and `ticketReference.test.tsx:62` to assert the product's words ("Working" etc.), as they did on main.
2. Comments: `test/postgres/finalizerClosure.test.ts:3,:7`, `test/postgres/ticketProjection.test.ts:13`, `ui/chuggy-ui/test/ticketApproval.test.tsx:7`, `test/postgres/finalizerHarness.ts:3,:16,:531,:596` say the new names. A comment only reworded to survive is deleted instead.

## Part 2 — `test/actor/journalAtSemanticsFour.json`, one commit

The sweep found six keys of `wordAtCurrentVocabulary` that no test reddens (`Finalizing`, `WorkFailed`, `ReworkBudgetExhausted`, three step labels): the frozen semantics-1 fixtures never wrote them. Freeze a semantics-4 journal written by **main's own code** so every key is pinned. The machine reviewer's recipe is in `/tmp/claude-1000/-home-geoff-claude-chuggy/6988229d-0563-475a-9255-7ce0b2467e85/scratchpad/r1m/mk4.ts` (main's `src` extracted at `…/r1m/mainsrc/src`; if that is gone, `git -C ~/claude/chuggy archive 55de9de6 src | tar -x -C <scratch>` restores it). Extend it until the encoded rows contain **every key** of the map (every old phase, resume point, reason including one wall, `FinalizationFailed`, `ReleaseTicket`, every old step label); main's `decisionEventEnabled` must admit every step (no forged rows). Write the fixture in the same file shape the semantics-1 fixtures use (read `pinned()` and the files); stamp semantics 4.

Test, beside the semantics-1 cases: (a) the fixture's bytes contain every key of the map — derive the roster from the map itself (export what is needed, narrowly), so a key added later without a fixture row fails; (b) the journal replays legal under `storedJournalLegalOn` at semantics 4; (c) after replay the phases read the new words (spot-check the final state). Red-proof: remove `Finalizing` from the map and (b) must fail; remove a row from the fixture and (a) must fail.

## Gates

`check-source` (unit + static), `check-console`, `check-comments`, `check-figures`, `check-paths`. Report exits. Commits in the tree's voice ending `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Never push. Report to `~/claude/chuggy-effort/ticket-language/pr3/tasks/F2-report.md` under 30 lines: tip, what each commit did, which keys the fixture carries, red-proof results, gates.
