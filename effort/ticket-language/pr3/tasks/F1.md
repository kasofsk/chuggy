# Task F1 — round 1 surface findings

Worktree `~/claude/chuggy-wt/rename`, branch `model/rename`, tip 64e5abb5. Root `npm ci` only if stale (never inside `ui/chuggy-ui/`: it has no lockfile and wipes the root install). Read `CLAUDE.md`, `.chug/tasks/review-change.md`, then `~/claude/chuggy-effort/ticket-language/pr3/reviews/round1-surface.md` whole. Fix exactly its two findings, nothing else:

1. `ui/chuggy-ui/test/codeLabels.test.ts`: the drawn-labels case maps `blockedReasons` over `blockedReasonLabel` alongside the three escalation reasons (same three assertions: non-empty, at most the existing length bound, none of `.:;`); plus one case for `escalationDetail` with a wall present (the wall's label appears) and one with it absent (the reason's generic word alone). Read `ui/chuggy-ui/app/core/codeLabels.ts` to get the real signatures; the tests prove the code, not the other way round. Red-proof: break `blockedReasonLabel` for one wall and the test must fail.
2. Comments: `ui/chuggy-ui/app/core/inboxUnion.ts:8`, `ui/chuggy-ui/test/inboxApproval.test.tsx:6`, `test/interpreter/leadTurn.test.ts:422`, `test/interpreter/selector.test.ts:2723` say the new phase words. `ui/chuggy-ui/app/core/codeLabels.ts:142-146`: keep the sentence that the gerunds are the product's copy for the phases; drop the clause bound to this PR ("decided before the phases were renamed… not reopened here"). If a comment is only reworded to survive, delete it instead.

Gates: `check-console`, `check-source`, `check-comments`, `check-figures` — report exits. One or two commits in the tree's voice ending `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Never push. Report to `~/claude/chuggy-effort/ticket-language/pr3/tasks/F1-report.md` under 20 lines and hand back tip and gate exits.
