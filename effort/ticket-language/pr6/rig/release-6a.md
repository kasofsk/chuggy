# PR 6a release recipe (second with a wipe; 009)

1. Merge the PR; `git -C ~/claude/chuggy pull`; note the sha.
2. Dump: `ssh geoff@192.168.0.114 'kubectl -n chuggy exec postgres-0 -- pg_dump -U postgres -Fc chuggy > /home/geoff/backups/chuggy-pre-<sha>.dump'`; `ls -la` it.
3. Phase 1: `CHUG_RIG_SSH=geoff@192.168.0.114 deploy/rig/deploy-to-gtr.sh` → fabric PR; mechanical diff (source commit bump only).
4. Scale down: `kubectl -n chuggy scale deploy chuggy-api chuggy-finalizer chuggy-scheduler chuggy-selector chuggy-ticket-service chuggy-worker-plane chuggy-ui --replicas=0`; wait for the pods to go; suspend the importer cronjob for the window if it is due (`kubectl -n chuggy get cronjob`).
5. Wipe: `scp deploy/rig/wipe-tickets.sql geoff@192.168.0.114:/home/geoff/wipe-tickets.sql`; `kubectl -n chuggy exec -i postgres-0 -- psql -U postgres chuggy -v ON_ERROR_STOP=1 -q < /home/geoff/wipe-tickets.sql`; verify `select count(*) from journal_entry; select ticket_next from project;`.
6. Merge the fabric PR (`gh pr merge <n> --repo gdoteof/chuggy-fabric --admin --merge --delete-branch`); `flux reconcile source git fabric -n flux-system; flux reconcile kustomization apps -n flux-system`; wait `job/chuggy-migrate-<sha>-registry` (009 applied; ledger 9); rollout status ×7; replicas back from the manifests.
7. Sanity: console 200; `GET $P/tickets` empty; `GET $P/configurations` lists `repository:<main sha>:chuggy-development`; `GET $P/draft-initializations/<that revision>` → fence + defaults (no `workFanout` choice); `POST $P/drafts` (brief names `repository`, `finalization: {mode: "None"}`, a small real task), `GET $P/drafts/<n>` parses; drafts page 200; `ReleaseDraft`; selector is Paused → `ManualDispatch{ticket, expectedTicketVersion}` from `dispatch-view`; watch the ticket reach Work and a worker pod appear; `GET $P/tickets/<n>` shows `phase`, no `escalation`; `RevokeTicket` afterwards. Logs of the seven deployments free of error lines; importer's next run completes.
8. Record: `pr6/GOAL.md` progress, SPIKE.md Landed, memory `ticket-language-2026-09-20.md`, plan artifact step 6a; remove worktrees `fanout`, `fanout-schema` and branch `schema/work-fanout-goes`.
