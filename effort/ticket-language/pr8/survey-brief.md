# PR 8 survey — Commands, refusals, events, obligations, and how to cut it

Read-only. Detached worktree of your own at `origin/main` (aaaff1ec, PR 7b merged and released):

    git -C ~/claude/chuggy fetch -q origin && git -C ~/claude/chuggy worktree add --detach ~/claude/chuggy-wt/pr8-survey origin/main
    ln -s ~/claude/chuggy/node_modules ~/claude/chuggy-wt/pr8-survey/node_modules

Never `npm ci` under `ui/`. Read first: `~/claude/chuggy-effort/ticket-language/SPIKE.md` (the plan table's PR 8 and PR 9 rows, "Commands, events, decisions", "Decisions", "PR 8 split" — Geoff's candidate cuts are the minimum your Split section covers), `pr7/survey.md` and `pr7/GOAL.md` (the shape and depth wanted; the Split section is the model; the six divergences 7b left for PR 8 are listed in `model/AGENTS.md` on main), `pr7/tasks/7b/{A,B,E}-report.md` (what the tree holds now: `Ticket.evaluations`, `TaskDone{ticket, task, report, onFailure}` with the disposition stamped by the writer at serialization, the exhausted-retry outcome, a durable unreadable rework spawn parking as `EvaluationFailureEscalated`), the package's `~/claude/chuggy-effort/ticket-language/package/model/ticket-domain/ticket.qnt` whole (`TicketCommand`, `TicketRefusal`, `TicketEvent`, `Obligation`, `ReleasedTicket`, `decide`, `evolve`, `expectedRevision`) and `ticket-decision-context.md`, `IMPLEMENTATION_MAP.md`, `CLAUDE.md`, `.chug/tasks/review-change.md`. The fabric is at `~/claude/chuggy-fabric` (read-only); the console under `ui/chuggy-ui/`.

## Standing decisions

- The rig's tickets are disposable: a migration may refuse a non-empty journal and name `deploy/rig/wipe-tickets.sql`; no lift for old rows. Each piece you propose says whether it needs the wipe.
- Package pinned at 76c95a9; no package changes. A model file is imported by verbatim copy, gated like any model file; PR 9 deletes the copies and imports the package, so every PR 8 piece is measured by how much closer `model/ticket.qnt` and `model/domain.qnt` are to the package's `ticket.qnt` text afterwards.
- Geoff (2026-09-22): PR 8 is the one he expects to benefit from splitting. **The Split section is the deliverable**: name the pieces, what each moves in the model, schema, boundary, wire and console, which need each other, which need the wipe, which move the worker or the fabric, and the order. Recommend one cut, with the reason; say what a single PR would cost instead. Candidate cuts, at minimum: commands in; refusals; events; `Update` while Pending with `expectedRevision`; `ReleasedTicket` with opaque content; `evolve` applying an event only to the state that still owes it; and the six 7b divergences (`EvaluatorDefinition.task`, `EvaluationInput.acceptedSourceRef`, `taskDefinitionValid`, the accepted-ref invariant, `currentTaskObligations` yielding obligations, `applyProduced` taking a validated result).

## Questions to answer, each with file:line evidence

1. **The model today against the package's command surface.** chuggy's `DecisionEvent` (which arms remain after 7b) and `decideX` deciders, `Decision{rec: StepRecord, post}`, the effect strings and where they are embedded durably (the SPIKE says "durable keys and positions"); against `TicketCommand` (7 arms), `decide` → `TicketRefused | TicketDecided{event, obligations}`, `TicketEvent` (18), `Obligation`, `evolve`. Which chuggy events map one to one onto a command, which onto an event, and which have no counterpart (the reduce steps, `ResumeTicket`'s resume kinds).
2. **Refusals.** The interpreter's `operationRefusalCodes` and every "not enabled" site against the package's thirteen refusals; which are decided by the model today and which by the boundary (the door's validators, `accept_operation`, the mailbox) — and whether a refusal decided at the boundary can move into the model without the door losing a check.
3. **Events and the journal.** What a journal row is today (`journal_entry`, `decision_event_is_valid`, `ticket_command_is_valid`, decision semantics 6, the actor's replay), what it becomes when rows carry commands and events, and what the projection, the native-action rows, the continuation rows, the dispatch candidate and every stored-text reader (`stored-text-outside-the-journal` memory: `draft_revision.authoring` and its siblings) need. Which piece needs the wipe, and whether more than one does.
4. **Update while Pending.** The draft/release surface today (`drafts`, `ReleaseDraft`, `authoringVersion`, the fence) against `UpdateTicket` with `expectedRevision`; what the console's draft flow becomes; whether the drafts table survives.
5. **ReleasedTicket and the opaque refs.** `content`, `finalizationConfiguration`, and the six 7b divergences: what each opaque ref stands for in chuggy (brief text, configuration revision and digest, the accepted commit, the task definition per evaluator), where it would be minted and stored, and what the door checks about it.
6. **Obligations and the effects.** `SpawnWorkTasks`/`SpawnEvalTasks`/`RunFinalizer`/`CancelTicketWork`/`OpenHumanTask` against `ExecuteTask | FinalizeTicket | CancelTask` with the desk implied by Escalated; every durable key or position that embeds an effect string (`sourceRequest "6:0:SpawnEvaluation"` in the pod env, `inputBundle` positions, continuation kinds) and what replaces it.
7. **Wire and console.** What the ticket response, the operations surface and the console change per piece; what a reader sees differently.
8. **Surprises**: anything the SPIKE row got wrong, anything PR 3–7 left for PR 8 (grep the GOAL.md, survey and report files under `pr3/`–`pr7/` and `model/AGENTS.md` for "PR 8"), and any place the worker or the fabric would have to move.

## Output

`~/claude/chuggy-effort/ticket-language/pr8/survey.md`, in the style and depth of `pr7/survey.md`, under ~300 lines, numbered surprises at the top, a **Split** section with the recommended cut, and a closing **Release coupling** section per piece. Remove your worktree when done (`git -C ~/claude/chuggy worktree remove --force ~/claude/chuggy-wt/pr8-survey`). Write the file, reply with the surprises, the Split and the release-coupling sections only, and stop.
