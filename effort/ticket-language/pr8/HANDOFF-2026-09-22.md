# Handoff — ticket-language convergence, paused 2026-09-22 ~16:45Z before PR 8b

Written for the next session (mine or another's). Everything below is checkable in the tree, the effort dir or the rig; nothing here is the only copy of a fact.

## Where things stand

- **chuggy `main` is 02bd9572** (= #732 "The released ticket" + #733 test-race fix). Clean, full roster green on the branch tip before merge (`pr8/ci-full-8a-e3a353a6.log`).
- **The rig runs 02bd9572** with migration 013 applied (`chuggy-migrate-02bd9572-registry` "applied 13"), fabric main 78fcc8e7 (fabric PR #293). Journal was wiped (18 → 0). Sanity ticket 1 ran to **Done at seq 8**; its journal shows the 8a shapes (`CreateTicket` with the definition, `Dispatch{ticket, source}`, `TaskDone` reports carrying obligations, two `ticket_source` rows). Console pages 200. Importer resumed and imported chuggy at 02bd9572 (chuggy-development revision 21). The three `Error` importer pods are days old and the known vestigial-mirror failure, not new.
- **Backups**: `/home/geoff/backups/chuggy-pre-5c15a8b7.dump` and `chuggy-globals-pre-5c15a8b7.sql` on 192.168.0.114.
- **Rig helper files kept** (effort identity, remove at end of effort): `/home/geoff/convergence-admin.json` (Hydra client a01fa804-…, mode 600, never print), `/home/geoff/api-call.sh`, `/home/geoff/resume-84.json`; the Keto admins tuple for that client. `/home/geoff/wipe-tickets.sql` is main's current script (names `ticket_definition`/`ticket_source`). Sanity scratch files removed.
- **Artifact** "Ticket Convergence Plan" (https://claude.ai/artifact/FTzkDqocCgsCFAHZBUvCwk) updated to 8a landed / 8b paused.
- **Records updated**: `pr8/GOAL.md` (8a release line with the wipe trap), `pr8/reviews/8a-ledger.md`, `SPIKE.md` line 327, memory `ticket-language-2026-09-20.md`, `MEMORY.md`, `rig-release-runbook.md` (wipe trap), `chuggy-false-reds.md` (reason-rosters race).

## The one new trap from this release

The wipe script on main names tables the pending migration creates. Run before the fabric merge, it fails `relation "public.ticket_definition" does not exist`; run after, 013's guard refuses because journal rows exist. **Wipe with the previous release's script**: `git show <previous main>:deploy/rig/wipe-tickets.sql`. 8b's 014 adds no table the wipe names, so 8b's wipe can use main's script as is. Recorded in the runbook memory and `pr8/GOAL.md`.

## What was in flight: writing PR 8b's decisions

Nothing for 8b is written yet beyond the paragraph at `pr8/GOAL.md` line 11 and the survey. I had read the material and was about to draft "## PR 8b — decisions" in `pr8/GOAL.md` in 8a's format (numbered decisions, a sequence A/S/B, then progress lines), then briefs `pr8/tasks/8b/{A,S,B}.md` modelled on `pr8/tasks/8a/{A,S,B}.md` (A and S in parallel on opus in their own worktrees `~/claude/chuggy-wt/events` / `events-schema`, branches `model/ticket-events` / `schema/ticket-events`, own postgres containers on 55436/55437; B on the merged tip).

Read first, in this order: `pr8/survey.md` §1, §3, §6, §7 and surprises 1, 2, 5, 9, 10, 11, 12, 13; the package's `~/claude/chuggy-effort/ticket-language/package/model/ticket-domain/ticket.qnt` lines 150–245 (events, obligations, `TicketDecision`), 335–432 (`reportAdmissible`, `workTaskObligation`, `finalizationCurrent`, `executeEvaluationTasks`, `finalize`), 846–1172 (`decide`, `evolve` arms), 1285–1363 (`obligationValid`, `obligationAgrees`, `decisionValid`); chuggy's `model/ticket.qnt:411-423` (`Transition`, `StepRecord`, `Decision`), `model/refinement.qnt:280-288` (`DecisionEvent`, seven arms) and its uses of `.rec` (`:143,376,405-418,464,532`), `model/domain.qnt:551-663` (`decideTaskDone`, `decideEvalTaskDone` with `onFailure`, `decideWorkReduce`); `src/domain/effect.ts`; `src/actor/journal.ts:66-104` (replay re-decides and compares `rec`); `src/interpreter/decisionPlan.ts:52-64` (`identity`, `subject` positional), `:200-215` (native action position), `:385-410`; `src/interpreter/projectWriter.ts:396-416` (`onFailure` stamped at serialization), `:610-690,746-749,830-832` (transient deferral); `src/adapters/postgres/readiness.ts:455-475` (continuation), `relations.ts:718-732` (`project_continuation`); `src/adapters/kubernetes/workerPod.ts:246-280` (identity annotations); `pr7/reviews/7b-round1-machine.md` §1 (the deferred-forever wedge); memory `finalization-unavailable-2026-09-21.md` (PR 4's dwell rule).

### Decisions I had reached (draft, not yet written into GOAL.md; the next session may sharpen)

1. **`TicketEvent` in the package's spelling, eighteen arms**, replacing `DecisionEvent`'s seven. The journal row carries the event; `rec`/`StepRecord`/`Transition` leave the row, the model and the wire. Replay becomes `evolve` folded over the rows (package `evolve`), and `storedJournalLegalOn`'s "record reproduced" check becomes "the event is admissible at its prefix" (evolve of a non-owed event is identity, so legality is `evolve(g, e) != g` or an explicit admissibility predicate; decide which and say why).
2. **`decide` returns `TicketDecided{event, obligations}`** (refusal stays chuggy's `NotEnabled` bool until 8c; `TicketRefused` does not arrive). `Obligation` = `ExecuteTask | FinalizeTicket | CancelTask`; `OpenHumanTask` has no obligation (survey 10): `native_action` keys on `(authorizing_seq, ticket)`. `decisionValid` (package `:1349-1363`) is the model's own check and a conformance property.
3. **`WorkReduce` folds** into the work completion's decision (`TicketWorkResultAccepted` carries the `ValidatedTaskResult` and `acceptedSourceRef`); `project_continuation`, the `Continuation` input class, its aging term and `readiness.ts`'s continuation read are deleted; 014 drops the table.
4. **`onFailure` leaves the completion.** The writer's policy pick (`reworkDisposition(held, cyclesMax)`, `projectWriter.ts:396-416`) stays in the writer but feeds `decide` as the package's `EvaluationFailurePolicy`; the event records the edge (`TicketEvaluationReworkStarted` vs `TicketEvaluationFailureEscalated` with `evidence: List[EvaluationReworkEntry]`). Replay never re-draws it because the event says which edge.
5. **Resume splits three ways** by the escalation variant (`TicketWorkResumed`/`TicketEvaluationResumed`/`TicketFinalizationResumed`); the command stays one `ResumeTicket` (8c's business); the writer derives the event from the replayed ticket's escalation.
6. **The five durable identities** take the obligation's kind word: `"<seq>:<index>:ExecuteTask"` etc., where index is the obligation's position in `obligations` (positions stay per *spawn bundle* — survey 9 — so `execution_request.effect_position` becomes the obligation index of the first `ExecuteTask` of the bundle, or the request groups a run's `ExecuteTask`s; pick the grouping and keep `UNIQUE (tenant, project, authorizing_seq, effect_position, kind)`). Nothing parses the string; pods carry it opaquely. Zero worker pods before the wipe.
7. **`submit_task_completion`** keeps its signature; the door builds the report as 013 does; the journaled *event* is decided by the writer, not the door — the door still journals? **Open**: today the door journals `TaskDone` directly under the lock (013). With events, the door would have to run `decide` to know which of five events a completion is (`Progressed`/`Passed`/`ReworkStarted`/`FailureEscalated`/`Blocked`, and the work trio). Options: (a) the door journals a *command-shaped* row and the writer evolves it — breaks "the journal carries events"; (b) the door computes the event by reading rows (stage state) — a decider in SQL, the layer rule; (c) the completion becomes an inbox input the writer decides, the door recording only the `execution_result` row (7b-before shape). I was leaning (c): the writer already replays the ticket, holds the policy, and is the one process the layer rule names as a decider. Check what 7b/8a moved into the door and why (`pr7/tasks/7b/S-report.md`, `pr8/tasks/8a/S-report.md`, decision 8 of 8a) before choosing; the 8a reason for the door building the obligation was "never from the caller", which (c) keeps if the writer reads rows under the same lock.
8. **The transient-observation deferral** (carry-in from 8a): a `Dispatch` whose remote answers transient today is deferred and re-picked every quantum, the aging term drives it to the head, and the project's writer stops deciding anything else within the hour (7b round 1 §1's shape). Decide as PR 4 did: count passes on the operation row (or `ticket_source`-adjacent row), refuse `ExecutionSourceUnreadable` after `holdPassesMax`-style bound, evidence kept. Small; could be its own commit in B.
9. **014**: guard as 008–013; `decision_event_is_valid` rebuilt whole over the eighteen arms (spell from the codec once A lands; S in parallel drafts from the package's types, 8a's precedent); `project_continuation` dropped with its function/privilege rows; `native_action` unique re-keyed; `operation.command_tag` authority CHECK and `accept_operation`'s priority classification follow the event tags; render-diff 001–013 empty.
10. **Goldens**: twelve re-emitted; the corpus shows an event applied to a state that no longer owes it being identity; a completion folding the reduce; both failure edges; the three resumes.
11. **Out**: `TicketCommand`, `TicketRefused`, refusal names, `UpdateTicket`, `Ticket.revision` (8c); worker, fabric, console (events change nothing a reader sees — survey §7).

Sizing from the survey: 48 files name `DecisionEvent`, 77 name `StepRecord` or an effect string, 22 name `WorkReduce`/`ReduceWork`.

## Release shape for 8b

PR 7b's shape with 014: dump; phase 1 `CHUG_RIG_SSH=geoff@192.168.0.114 deploy/rig/deploy-to-gtr.sh` (log under `pr8/rig/`); mechanical diff of the fabric PR; suspend `chuggy-configuration-importer`; scale the seven deployments to 0; **confirm zero worker pods** (`kubectl -n chuggy get pods | grep -c task` or the pool's label) before the wipe; wipe with main's script (014 names no new table); merge the fabric PR; `flux reconcile source git fabric -n flux-system; flux reconcile kustomization apps -n flux-system`; wait `job/chuggy-migrate-<short>-registry`; resume the importer; rollout status ×7; API and console 200; sanity ticket via `api-call.sh` (draft body shape and operation bodies are in `pr8/GOAL.md`'s 8a release line and `pr7/GOAL.md`'s; `ManualDispatch` needs the dispatch-view's `ticketVersion`); record.

## Standing rules still in force

Fresh reviewer per round on running code, two halves (machine + boundary/surface); sweep second; a sweep round ships unless it finds a behaviour defect; test-only fixes need no new round. Merge with bare `gh pr merge <n> --admin --merge --delete-branch`. Attribution `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`; PR bodies end `🤖 Generated with [Claude Code](https://claude.com/claude-code)`. ~3 subagents at a time, each in its own worktree with `node_modules` symlinked, never `npm ci` under `ui/`. One gate run on the box at a time; kill orphaned `node --test` children before a full roster. Never print a secret. Geoff's `first-drive.json` Hydra client on the rig was exposed once and should be rotated.

## End of effort (not yet due)

Remove the temporary rig identity: the Keto admins tuple, Hydra client a01fa804-…, `/home/geoff/{convergence-admin.json,api-call.sh,resume-84.json}`.
