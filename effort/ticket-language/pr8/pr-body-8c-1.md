Step 8c-1 of the ticket-language convergence (`chug-ticket-domain` at 76c95a9): **commands and refusals.**

The decider takes the package's `TicketCommand` — `CreateTicket`, `DispatchTicket`, `RevokeTicket`, `ResumeTicket`, `ReportTaskTerminal`, `ReportFinalizationResult` (`UpdateTicket` arrives in 8c-2) — and `DecisionEvent` leaves the tree. A task report carries its ticket in every arm and a finalization result carries the cycle, the generation and a `FinalizationResult` with its evidence, both verbatim. `decide` returns `TicketDecided` or `TicketRefused` in the package's thirteen refusals, checked in the package's order and carrying their payloads (the missing or undone dependencies, the task that is not current, the cycle and generation); `decisionEventEnabled` is gone. A refusal changes nothing and is journalled nowhere. The three refusals only an update reaches are declared and unreachable until 8c-2. `FinalizationNotCurrent` fences the cycle and the generation, not the phase alone. A release may now name a revoked dependency, as the package allows; such a ticket stays pending. Decision semantics is 8.

The interpreter's envelope is `ProjectCommand`, its `Decide` carrying `ticketCommand`. The map from an envelope and the rows it names to a `TicketCommand` is one pure function (`src/interpreter/commandMap.ts`); the postgres adapter reads rows and calls it. The writer stores each refusal with its payload; `NotEnabled` and `CommandUnreadable` are gone, and the boundary keeps its own codes, two of them new: `TicketCapacityReached` (the deployment's release room, a bound the package does not have) and `FinalizationRequestClosed` (a result under a request a restore or an invalidation closed, where `decide` would have accepted it). The operation response's `Refused` arm carries `refusal`, strict per code; the console names what a refusal was over. Replay also refuses a row whose report is about another ticket.

**Migration 015.** Guard as 008–014, over the journal and the inbox: stored envelopes and refusals are in 8b's words, so it refuses while either holds a row and names `deploy/rig/wipe-tickets.sql` — the previous release's script. The journal validator requires a report's ticket to be its event's; the command grammar is rebuilt over the six commands, keeping the authorization-by-shape exclusions; the inbox's classification and authority constraint read the new tags; `submit_task_completion` is rewritten whole for the report's ticket. `decision_input` gains the refusal and CHECKs that enumerate the codes and hold each machine refusal to its payload; `finalization_request` gains the cycle and generation the finalizer's result is fenced on.

No fabric or worker change beyond the release's digests. The public mutations are unchanged.

Survey, decisions, task reports and reviews: the effort directory's `pr8/`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
