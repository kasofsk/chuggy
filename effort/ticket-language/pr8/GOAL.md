# PR 8 — Commands, refusals, events, obligations, in three PRs

Step 8 of the convergence (SPIKE.md; package pin 76c95a9). Base: main aaaff1ec (PR 7b #731 merged and released). Survey: `survey.md` (read it first; thirteen surprises, a Split section and a release-coupling verdict set the scope). Effort dir: this directory; tasks in `tasks/8a/`, `tasks/8b/`, `tasks/8c/`; reviews in `reviews/`, ledgers `reviews/8a-ledger.md` and so on.

## The split (orchestrator, 2026-09-22; Geoff: "it was 8 that i was thinking might benefit from it")

The survey's cut, taken as recommended, in its order. The three fail differently: (a) by a task running on the wrong material, (b) by a decision's durable consequences landing wrong or twice or not at all, (c) by a principal refused wrongly or told nothing useful. A sweep over one diff carrying all three cannot red-proof one term at a time.

**PR 8a — The released ticket.** `ReleasedTicket` in the package's spelling on `Ticket.definition`; `source` on the dispatch, `acceptedSourceRef` on the work result and carried through evaluation, rework and finalization; the definition materialized once at release and stored, never re-resolved per request; every ref minted by the release from digests the tree already computes, none authored, so the wire and the console hold; the copy's six divergences close and `evaluation.qnt` becomes verbatim. Migration 013 with the wipe. Branch `model/released-ticket`.

**PR 8b — Events and obligations.** `TicketEvent` and `evolve`; the journal row carries the event; `reportAdmissible`/`finalizationCurrent`; `StepRecord`, the effect strings and the `effects[i]`↔`transitions[i]` pairing go; `WorkReduce` folds into the completion and `project_continuation` is deleted; `onFailure` leaves the completion for the event; the five durable identities take the obligation's kind word. Migration 014 with the wipe; the release confirms zero running worker pods before the wipe (survey §Release coupling). Branch `model/ticket-events`, after 8a lands. Decisions written when 8a is in. Carried in from 8a's boundary review: the writer still **defers** a `Dispatch` whose source observation is transient (main's behaviour, narrowed by 8a to the dispatch alone), which is 7b round 1's wedge shape and leaves the operation unanswered while the remote stays transient — 8b decides whether a transient observation refuses after a dwell, as PR 4 did for finalization holds.

**PR 8c — Commands, refusals and Update.** `TicketCommand` at the boundary (the interpreter's envelope renamed); `decide` returning `TicketRefused`; the thirteen refusals on the wire and in the console; `readiness.ts`'s command→event map deleted; `UpdateTicket` with `expectedRevision`, `Ticket.revision`, and the draft/ticket authoring split survey §4 names. Migration, no wipe. Branch `model/ticket-commands`, after 8b lands.

**Release coupling.** 8a: none beyond the wipe — no ref is authored and the configuration document is untouched, so kept revisions parse and the drafts page reads as before; the release is PR 7b's shape with 013. 8b: zero worker pods before the wipe. 8c: PRs 1–7's shape minus the wipe.

## PR 8a — decisions (orchestrator, 2026-09-22)

1. **Types, in the package's spelling.** `model/ticket.qnt`: `type ReleasedContent = int` and `type ReleasedTicket = { id, content: ReleasedContent, dependencies: Set[int], workConfiguration: TaskDefinition, evaluationPlan: EvaluationPlan, finalizationConfiguration: int }` (package `ticket.qnt:5,56-63`); `Ticket.definition: ReleasedTicket` replaces `deps` and `program` (`ticket.definition.dependencies`, `ticket.definition.evaluationPlan.stages`; the instance's `begin` takes the plan itself). `Ticket.revision` does not arrive (8c). `releasedTicketValid` is the package's (`:265-271`) plus chuggy's bounds (`MAX_STAGES`, `N_TASKS`, positional stage keys — 7a decision 2 stands). `model/ticket-domain/evaluation/evaluation.qnt` becomes VERBATIM (`diff` empty); `model/AGENTS.md` says so and drops the six-line list. `EvaluatorDefinition.task` is a `TaskDefinition` per evaluator; that a stage's evaluators carry the same one is the release's rule (decision 6), not the model's.
2. **The source is a field of the ticket.** `Ticket.source: int`, `0` until dispatched. `Dispatch({ ticket, source })` sets it (package `DispatchTicket`, `:109`); a passed work report's `acceptedSourceRef` replaces it and is what the instance's `EvaluationInput.acceptedSourceRef` is begun with; an evaluation-failure rework, a finalization needs-work rework, a resume and the finalizer all run at the ticket's current `source` — **no spawn observes a source but the dispatch**. The package hangs the same value off `Work(WorkExecution).source`, `Escalated(WorkEscalation).source`, `FinalizationOperation.source` and `EvaluationInput.acceptedSourceRef`; chuggy carries one field on the record until PR 9 brings `TicketState`, and `ticket.qnt` says so at the field in two sentences. Invariant: `source > 0` exactly when the ticket has been dispatched; the copy's `acceptedSourceRef > 0` returns with it.
3. **The report carries the obligation.** `WorkResultReport { result: ValidatedTaskResult, acceptedSourceRef }` and `EvaluationResultReport { result: ValidatedTaskResult, verdict }` (package `:86-87`, minus the `ticket` the event carries — 7b's spelling); `TerminalFailureReport { evidence, kind }` untouched. `applyProduced` takes the `ValidatedTaskResult` (the copy's own). `reportMatchesTask` becomes the package's exact-obligation rule (README: "accepts a produced report only when its full task obligation equals the current obligation"): a work report's `result.obligation` equals `workTaskObligation(...)` and an evaluator's equals the one `currentTaskObligations` owes; a failure report still matches by identity. It is the same `NotEnabled` at the door until 8c names it. `contextRef` is the work cycle for a work task (package `:387-388`) and what the copy builds for an evaluator.
4. **Every ref is minted by the release, none is authored, and each is a fold of a digest the tree stores.** The wire does not change: the release body, `ReleaseDraft`, `authoringSchema` and the drafts read are as they are. `content` = the fold of the released revision's brief digest; `finalizationConfiguration` = the fold of the digest of the finalization binding resolved at release (`draft_brief.finalization_mode/_target` and the repository's landing as it stands at release — a landing default changed afterwards does not move a released ticket); `workConfiguration` = `{ workload, inputs, executionRequirements, resultContract }` resolved for kind `Work` under the release's configuration revision, and each evaluator's `task` the same four resolved for `Evaluation:<stage key>`: `executionRequirements` folds the materialized requirement's digest (`execution.requirement_digest` today), `workload` the configuration's image, `inputs` the block the briefing composes from, `resultContract` the manifest contract the worker attests. B names the exact material behind each of the four and says where a named one has no digest today (then the release computes and stores one). Folds are `result_digest_fold`'s shape in SQL and its TypeScript twin; a repository-less ticket (`BriefNamesNoRepository` is not raised; the sanity tickets are such) carries a reserved source reference distinct from every fold, named once in `src/adapters/postgres/schema/shared.ts`.
5. **The materialization is stored at release, and dispatch reads it.** 013 adds `ticket_definition (tenant, project, ticket, definition jsonb, digest text)` — one row per released ticket, written in the release transaction, holding the material the refs name (image, the requirement per kind and stage, the blocks, the finalization binding) in a shape `src/contract`/`src/interpreter` types — and `ticket_source (tenant, project, ticket, source bigint, repository text, commit text, ref text)` keyed `(tenant, project, ticket, source)`, one row per source the ticket has had (the dispatch's observation; each accepted work result's commit). `execution.requirement_*` is copied from `ticket_definition` at dispatch instead of re-materialized (`decision.ts:321-334`, `materializeExecutionRequirement` keeps one caller: the release). The refs themselves are read back from the journal's `CreateTicket` row (`journal_entry_release_ticket`), never from a second copy. `ticket_projection` unchanged.
6. **The release resolves.** `readiness.ts:192`'s `resolvedEvent` for `ReleaseDraft` carries the whole `ReleasedTicket`; the interpreter's `releaseTicketEvent(ticket, authoring, materials)` is pure and the adapter supplies the materials (configuration revision, brief, repository landing). Every evaluator of a stage gets the `Evaluation:<stage>` definition (7a decision 5). `draft_revision.authoring` stops being an encoded `CreateTicket` event (`encodeDraftAuthoring`, `authoring.ts:215`): it is the `ReleaseAuthoring` through `src/contract/authoring.ts`'s `authoringSchema`, and `parseDraftAuthoring` reads that — a definition depends on the revision chosen at release, so the draft cannot hold one; the wipe empties the table so no lift is owed (memory `stored-text-outside-the-journal`).
7. **The dispatch observes before it decides.** For the `Dispatch` command alone the writer observes the source at the ticket the command names, fills `source`, and refuses on `ExecutionSourceUnreadable`/`ExecutionSourceDenied` with no journal row — the same codes as today, decided before `execDecisionEvent`. `projectWriterExecutionSource`'s speculative re-decide (`projectWriter.ts:503-536`) is deleted; the `EscalateEvaluationFailure` park task E built for an unreadable rework source (`pr7/tasks/7b/E-report.md` §1b) goes with it, because a rework observes nothing. The pod's `TargetCommit` reference comes from `ticket_source` at the ticket's current `source`.
8. **The door builds the obligation from rows, never from the caller.** `submit_task_completion` (rewritten whole, 010/012's precedent; signature unchanged, `CREATE OR REPLACE`) journals the report of decision 3 with `result.obligation.task` from `execution_request_task`, `.definition` from the journal's `CreateTicket` row (work or the evaluator's stage), `.contextRef` per decision 3, `resultRef` as today's `result`, and `acceptedSourceRef` from `execution_result_source` for a passed work manifest — writing the `ticket_source` row under the same lock. A passed work manifest with no source row on a ticket that has a repository is refused (`BindingMismatch`'s shape; name it).
9. **013** = the guard as 008–012 with the wipe; `decision_event_is_valid` rebuilt whole: `CreateTicket` admits a `ReleasedTicket` (positive `id`, `content`, `finalizationConfiguration`; `dependencies` an array of positives; `workConfiguration` and every evaluator's `task` four positive ints; stages positional, evaluators unique within the bound — 011/012's rules kept) and drops 012's three dead conditions (`workFanout`, `finalizer`, `combinator`; survey 13); `Dispatch` admits `{ ticket, source }`; `TaskDone`'s two produced arms per decision 3. DDL: the two tables of decision 5, and `operation_completion_authority_is_its_boundary` re-rendered without `'ExecutionBlocked'` (survey 13). Render-diff 001–012 empty.
10. **Goldens** carry: a release whose definition is read back; a dispatch source carried into work; an accepted source replacing it and begun into the instance; a rework and a finalization at the accepted source; a report whose obligation differs refused. Twelve re-emitted.
11. **Out**: `Ticket.revision` and `UpdateTicket` (8c); `WorkInput`/`TicketState` (PR 9); events, obligations, refusal names; the worker, the fabric, the console, the configuration document.

## PR 8a sequence

A. model (`ticket.qnt`, `domain.qnt`, `refinement.qnt`, `api.qnt`, tests, mc), the copy made verbatim, goldens re-emitted, generated, `src/domain`, `src/actor`, conformance. opus. Worktree `~/claude/chuggy-wt/released`, branch `model/released-ticket`.
S. migration 013 + `migration.test.ts` + render-diff 001–012 empty. opus, parallel with A. Worktree `~/claude/chuggy-wt/released-schema`, branch `schema/released-ticket`, merged into `model/released-ticket` after A.
B. interpreter, adapters, contract: the release resolves and stores, dispatch reads, the door builds the report, the writer observes first. opus, on the merged tip.
Then: round 1 machine + surface reviews (fresh), fixes, sweep, full roster, merge, release with the wipe, sanity ticket (with a repository-less ticket and, if the rig has one bound, a ticket with a repository).

## PR 8a — progress

- 2026-09-22 ~05:20Z: A and S launched in parallel (opus). S landed 3c10ad8f (013: `ticket_definition`, `ticket_source`, the door reading the release row; the index over `id`; the boundary owner's column grant on `journal_entry`; thirty-three mutations red). A landed 59cea038 (both copies verbatim; `Ticket.definition`, `Ticket.source`; the report carries the obligation; `reportAdmissibilityRequiredTest` red-proofs decision 10's fifth shape, which cannot be a golden). Merged 36a9df7a.
- B landed 3dd30cbe (`ticketDefinition.ts`; the writer observes before it decides and the park E built in 7b is gone; `submit_worker_result` re-rendered so the source row precedes the completion; the wire did not move). B's commits carried the wrong attribution trailer; rewritten by filter-branch, tip cefa77f2.
- **Decision 3 sharpened by D**: the accepted work result is the reference the report carried. A had derived it from the identity (the cycle) and B's door followed; the package reads the report and PR 9 would have shifted `workResult` under a landed door. D landed 6f7a3788: `beginEvaluation(ticket, workResult)`, `producedResultRef` in a band of its own so a golden shows the report's number (401) beside cycle 1, the door folding the cycle's accepted work manifest for an evaluator's `contextRef` with `WorkResultUnrecorded` beside `SourceUnrecorded`.
- Affected roster clean at 378eb585 (one Prettier miss fixed; a first run's `check-random` was killed at its cap inside `ci.sh` and is sub-second alone — the false-reds roster's shape). Round 1 machine + boundary reviews launched on 378eb585.
- Round 1 (both fresh, opus, at 378eb585): machine CHANGES — `stageDefinitionEquals` and `instanceEquals` could not see an evaluator's `task` or an input's `acceptedSourceRef` (fixed a3ad1cf2 with per-shape rosters); boundary CHANGES — the door's stage conjunct was unheld because every fixture released one stage (fixed a7923309 with a two-stage case, red-proved). **Two corrections to decision 4 from the boundary review**: the reserved source reference lives in `src/interpreter/executionSource.ts`, not `shared.ts`; and a release refuses a brief that names no repository (`draftReleaseReadiness`), so the sanity tickets did name one and the reserved-reference path is close to unreachable. Both B and D overstate the scheduler's new arms: they record kind `ImpossibleState` with new evidence strings and are reached only by the migration suite's literals — a sweep item, not a defect.
- 2026-09-22 ~16:30Z: **8a released with the wipe (main 02bd9572 = #732 + the #733 test-race fix).** Dump `/home/geoff/backups/chuggy-pre-5c15a8b7.dump` (+ globals); phase 1 on 5c15a8b7 failed red on the reason-rosters race (fixed #733, `chuggy-false-reds` memory), phase 1 on 02bd9572 clean, fabric #293 mechanical (digests, source-commit, migrate job name). Importer suspended, seven deployments to 0, zero worker pods. **Trap:** the wipe script on main now names `ticket_definition` and `ticket_source`, which 013 creates only after the fabric merge, so the wipe before the migration must be the previous release's script (`git show aaaff1ec:deploy/rig/wipe-tickets.sql`) — a wipe script that names a table the migration it precedes creates cannot run before that migration; the next wipe (8b, 014) is unaffected because 014 adds no table the wipe names. Journal 18 → 0; fabric merged (78fcc8e7), `chuggy-migrate-02bd9572-registry` applied 13; seven rollouts clean; API 200; importer imported chuggy at 02bd9572 (chuggy-development revision 21). Sanity ticket 1 (stage 1 evaluators 1 and 2, stage 2 evaluator 1, repository chuggy): release stored `ticket_definition` (digest folds per task key `Work`, `Evaluation:1`, `Evaluation:2`; the requirement materialized once at release), `ManualDispatch` at version 1 wrote `ticket_source` (main at 02bd9572, `refs/heads/main`) and the journal's `Dispatch{ticket, source}`; Work → Evaluation with the `WorkResultReport` carrying its obligation and `acceptedSourceRef` (a second `ticket_source` row at the worker's result commit); three `EvaluationResultReport`s with obligations; **Done at seq 8** in about two and a half minutes. Console `/`, `/drafts`, `/drafts/1`, `/tickets`, `/tickets/1` all 200. Rig scratch files removed; the new wipe script stays at `/home/geoff/wipe-tickets.sql`.
- 2026-09-22 ~16:45Z: **paused before 8b** (token budget). Handoff in `pr8/HANDOFF-2026-09-22.md`: rig state, the wipe trap, the eleven draft 8b decisions (decision 7's door-vs-writer question open, leaning writer-decides), the 8b release shape.

## PR 8b — decisions (orchestrator, 2026-09-23)

Base: main 02bd9572 (8a #732 + #733, released). Read before these: survey §1, §3, §6 and surprises 1, 2, 9–12; the package's `ticket.qnt` 150–245 (events, obligations, `TicketDecision`), 335–432, 846–1172 (`decide`, `evolve`), 1285–1363 (`obligationValid`, `obligationAgrees`, `decisionValid`).

1. **The journal carries `TicketEvent`; the inbox keeps carrying today's commands.** `model/ticket.qnt` gains the package's `TicketEvent` minus `TicketUpdated` (seventeen arms; `TicketUpdated` arrives with `UpdateTicket` in 8c) and its payload records, spelled verbatim. `Entry = { seq, event: TicketEvent }`: `rec`, `StepRecord`, `Transition` and `Decision` leave the model, the mirror, the row and the digest. `DecisionEvent` stays **as the decider's input** and keeps its name until 8c renames it `TicketCommand` (the interpreter's envelope holds that name today, survey 4); it loses `WorkReduce` (decision 3) and `TaskDone.onFailure` (decision 4). So an operation row still carries `{command: "Decide", event: <DecisionEvent>}` and `operation.command_tag` still names a `DecisionEvent` tag — the inbox holds commands, the journal holds events, and neither is renamed here.
2. **`decide` returns `{ event, obligations }`; refusal stays `NotEnabled`.** chuggy's `decide(graph, DecisionEvent, failurePolicy): { event: TicketEvent, obligations: List[Obligation] }`, guarded by `decisionEventEnabled` as today (`TicketRefused` and the refusal names are 8c). `Obligation` = `ExecuteTask | FinalizeTicket | CancelTask`, the package's records verbatim; the desk has no obligation (survey 10, decision 6). `evolve(graph, TicketEvent): TicketGraph` is chuggy's, over chuggy's `Ticket` record (the `TicketState` sum is PR 9's), arm for arm the package's: each arm applies only to a state that still owes the event (`reportAdmissible`, `finalizationCurrent`, the phase match) and is identity otherwise. The machine's actions become `decide` then `evolve`; `lastStep` becomes the last `{ event, obligations }`. `decisionValid` (package `:1349-1363`, with `obligationValid`/`obligationAgrees`/`obligationsUnique`) is an invariant of every reachable decision and a conformance property in `test/conformance/`.
3. **Replay is `evolve` folded; legality is "the event moved the state".** `storedReplayGraph` = `rows.reduce(evolve, genesis)`. `storedJournalLegalOn` keeps semantics and dense seqs and replaces enablement-plus-record with: the event's ticket exists at its prefix (`TicketCreated`: does not) **and** `evolve(prefix, event) != prefix`. Why this and not a separate admissibility predicate: every arm the machine can decide changes the graph (a progressed evaluator settles a status; a pass leaves Evaluation), so "not identity" is exactly "owed", and it reuses `evolve` rather than restating its guards (the refinement's no-copied-expression rule). A proves that claim in the model as an invariant (`decided events are never identity`), which is what makes the check sound. `decisionSemanticsVersionCurrent` 6 → 7.
4. **`WorkReduce` folds into the completion.** A work `TaskDone` decides at once: a `WorkResultReport` is `TicketWorkResultAccepted` with `ExecuteTask` per evaluator of the first stage (the instance begun as today); a `ProcessFailure` is `TicketWorkProcessFailed` and parks at `WorkFailureEscalated`; `ExecutionUnavailableFailure` is `TicketWorkExecutionUnavailable` as today. `decideWorkReduce`, `reducibleWorkIn`, the `WorkReduce` arm, `project_continuation` (table, fence `projectWriter.ts:273-290`, the `Continuation` input class and its aging term, `readiness.ts:455-475`, `relations.ts:718-732`, `decisionPlan.ts:395-405`) are deleted. Whatever of the Work-phase `tasks`/`artifact` bookkeeping existed only to carry the pass to the reduce goes with it (A says which).
5. **`onFailure` leaves the completion; the event records the edge.** The writer's pick (`reworkDisposition(held, cyclesMax)`, `projectWriter.ts:396-416`) stays in the writer and is passed to `decide` as the package's `EvaluationFailurePolicy`; the model draws it nondeterministically as today. The event says which edge was taken — `TicketEvaluationReworkStarted` or `TicketEvaluationFailureEscalated`, each with `evidence: List[EvaluationReworkEntry]` — so replay never consults a policy.
6. **Resume splits by the escalation.** `ResumeTicket(int)` stays one command; `decide` yields `TicketWorkResumed` (work failure, work unavailable, evaluation failure), `TicketEvaluationResumed` (blocked) or `TicketFinalizationResumed` (finalization unavailable), per package `decideResume`.
7. **Identities take the obligation's kind word; request kinds stay the scheduler's.** `identity(seq, index, kind)` with `index` the obligation's position in `obligations` and `kind` its constructor: `"<seq>:<index>:ExecuteTask"`, `"…:CancelTask"`, `"…:FinalizeTicket"`. Nothing parses them (survey 11). **Grouping** (survey 9): a decision concerns one ticket, so its `ExecuteTask`s are one run and remain **one** `execution_request` + one `input_bundle` + one capacity account, numbered at the index of the run's first `ExecuteTask`; likewise its `CancelTask`s are one `CancelTicketWork` request. `UNIQUE (tenant, project, authorizing_seq, effect_position, kind)` holds unchanged. `execution_request.kind` keeps `SpawnWork | SpawnEvaluation | CancelTicketWork`: it is the scheduler's own vocabulary, derived from the task identity's variant, and the two filters that distinguish work from evaluation (`executionSourceHistory.ts:29`, `evaluationReports.ts:90`) stay as they are. The effect strings (`src/domain/effect.ts`) go, and `subject()`'s positional read with them — an obligation carries its ticket. **The desk** is materialized from the transition itself (post `Escalated`, pre not): `native_action.action` = `"<seq>:TicketEscalation"`, unique `(tenant, project, authorizing_seq) WHERE attempt IS NULL`; S drops `native_action.effect_position` if nothing else writes it, else writes 0 and says who does. `finalization_request.effect_position` is the `FinalizeTicket` index. The door's `in_source_effect` binding (`012:289`) compares the obligation index. Zero worker pods before the wipe.
8. **The completion door is unchanged in kind.** `submit_task_completion` already answers into the writer's inbox (an `operation` + `decision_input`, 013) and never writes the journal, so the writer — the one decider — turns a completion into its event, holding the policy and the replayed ticket. The door drops `onFailure` from the command it builds (it never set it; the writer stamped it) and nothing else moves; the handoff's open question 7 dissolves.
9. **A transient source stops deferring forever.** The dispatch observation that answers transient defers as today but counts: 014 adds `decision_input.deferred_passes int NOT NULL DEFAULT 0` and `deferred_since timestamptz`, bumped by the settle that defers; past `sourceDeferralPassesMax` (code default beside `holdPassesMax`, PR 4's dwell) the operation is refused `ExecutionSourceUnreadable` with the last evidence, no journal row. The aging term stops promoting an input whose passes are exhausted, because it is refused on its next pass. One commit in B, red-proved by a remote that stays transient.
10. **014** = the guard as 008–013 with the wipe; `decision_event_is_valid` becomes the **journal's** validator, rebuilt whole over the seventeen `TicketEvent` arms in the codec's spelling (S drafts from the package's types in parallel with A, 8a's precedent; B aligns); a second function (today's command shape minus `WorkReduce` and `onFailure`) validates `operation.command`, which `ticket_command_is_valid`/`public_ticket_command_is_valid` call; the two authorization-by-shape exclusions stay (survey §2). `project_continuation` dropped with its functions and privileges; `native_action`'s unique per decision 7; `accept_operation`'s priority classification loses `WorkReduce`; `operation_completion_authority_is_its_boundary` checked against the surviving command tags; `journal_entry_release_ticket` re-rendered over `TicketCreated`; `decision_input`'s two columns (decision 9). Render-diff 001–013 empty. S takes `project_continuation` out of `deploy/rig/wipe-tickets.sql`: 014 drops it, so a later wipe naming it would fail, while this release's pre-014 wipe loses nothing by skipping it (its rows die with the table). 014 creates no table, so main's script runs before the migration as is — 8a's trap does not recur.
11. **Goldens** carry: a work completion whose one decision is `TicketWorkResultAccepted` with its evaluators' `ExecuteTask`s; both failure edges from the same failing stage under the two policies; the three resumes; a revoke's `CancelTask`s; an event applied to a state that no longer owes it being identity (a unit test if no trace can carry it — 8a's `reportAdmissibilityRequiredTest` precedent). Twelve re-emitted, the trace step schema moving from `StepRecord` to `{ event, obligations }`.
12. **Out**: `TicketCommand`, `TicketRefused`, refusal names, `UpdateTicket`, `TicketUpdated`, `Ticket.revision` (8c); `TicketState` (PR 9); the worker, the fabric's manifests, the console (it reads projections and executions, never the journal — survey §7).

## PR 8b sequence

A. model (`ticket.qnt`, `domain.qnt`, `refinement.qnt`, `api.qnt`, tests, mc), goldens re-emitted, generated, `src/domain`, `src/actor`, conformance. opus. Worktree `~/chuggy-wt/events`, branch `model/ticket-events`.
S. migration 014 + `migration.test.ts` + render-diff 001–013 empty. opus, parallel with A. Worktree `~/chuggy-wt/events-schema`, branch `schema/ticket-events`, postgres on 55436, merged into `model/ticket-events` after A.
B. interpreter, adapters: the writer decides and journals events, materialization from obligations, continuation deleted, the deferral bound. opus, on the merged tip, postgres on 55437.
Then: round 1 machine + boundary reviews (fresh), fixes, full roster, merge, fabric PR, release with the wipe (zero worker pods first), sanity ticket through Work → Evaluation → Done and one evaluation failure under each policy if the rig's cap allows.

## PR 8b — progress

- 2026-09-23: resumed on David's Mac from the handoff branch (effort dir `~/chuggy-effort/ticket-language/`, package re-cloned at 76c95a9). Rig reached as `dev2@10.100.0.1` over the `dev2` WireGuard profile; state as the handoff says. Decisions above written; decision 7's open question (door vs writer) resolved by reading 013: the door already enqueues, the writer already decides. Commits in this effort carry this session's model trailer, `Co-Authored-By: Claude Opus 5.5 (1M context) <noreply@anthropic.com>`.
- A landed 2acfe7a7 (7 commits; all its gates 0, check-source red only in B's layers; `tasks/8b/A-report.md`). **Corrections it forced**: `TerminalFailureReport` becomes the package's `{failure: {task, evidence}, kind}` (8a decision 3's "untouched" no longer holds — an event carrying the report must name its task), so the door is rewritten whole after all; the `FinalizationResult` command gains `evidence`; `Ticket` gains `finalizationGeneration` and loses `tasks`/`artifact`; a 13th golden `revoke-cancels-tasks`. **Decision 3's legality is a known limit, accepted**: "the event moved the state" is sound for decided rows but does not re-check guards (a `TicketDispatched` over incomplete dependencies passes); the journal is written only by the writer after `decide`, so the check guards against rows that do nothing, not against a forged history — the package's `evolve` makes the same trade. S sent A's spellings and the two command changes mid-task.
- S landed 727b62d2 (`tasks/8b/S-report.md`); merged into `model/ticket-events` at 469425c8, clean. **Corrections**: the journal row had no validator at all before 014 — S added a BEFORE INSERT trigger calling `decision_event_is_valid`; `native_action.effect_position` stays (the finalization approval rows write it); **decision 10 was wrong about the wipe: the release wipes with main's pre-8b script (02bd9572's `deploy/rig/wipe-tickets.sql`), then migrates** — the branch script refuses on 013 because `project_continuation` holds an FK to `journal_entry`. So 8a's trap recurs in mirror image: the wipe that precedes a migration is always the previous release's script. The `FinalizationResult` evidence is the writer's choice (B).
- **Environment (David's Mac):** `check-source` on untouched `origin/main` 02bd9572 is red here, typecheck and lint only, all in `test/rig/*` (`@playwright/test` resolves nowhere; no `package.json` declares it). So `test/rig` playwright reds on this machine are the baseline, not the branch. B launched on 469425c8.
- B landed cf829352 (`tasks/8b/B-report.md`): all gates 0 but check-source's playwright baseline. Round 1 launched on cf829352; ledger `reviews/8b-ledger.md`.
- Round 1 machine: CHANGES, docs only (`reviews/8b-round1-machine.md`) — fixed in-line de28c424. Decision 2's "arm for arm" has one named exception: the work-failure arms require the failure to name the current cycle (the package checks the phase alone); PR 9 must keep it or reopen stale failure rows.
- Round 1 boundary: CHANGES (`reviews/8b-round1-boundary.md`) — fixed in-line e42da9bf: evidence rows red-proved, wipe-script header says the pre-migration wipe is the previous release's script, `deferred_since` dropped (decision 9's second column had no reader).
- 2026-09-23: **David approved the gtr database wipe and the fabric merge for 8b.** Release from this Mac: `npm install` in the release worktree's `test/rig` (its own package.json declares `@playwright/test`; that is the check-source baseline red); local `kubectl` is a shim forwarding over ssh to `dev2@10.100.0.1` (no local context; `~/dev2.kubeconfig`'s token is rejected and its SA cannot reconcile flux); `CHUG_RIG_SSH=dev2@10.100.0.1`; dump archive local. Sanity ticket via `sudo -u geoff /home/geoff/api-call.sh` (David's choice), credential used in place and never printed.
- 2026-09-23: **David stopped the 8b mutation sweep mid-run and disabled the step**: no sweep for 8b or after. Its brief is deleted; the sequence goes round 1 → fixes → full roster → merge → release.
- 2026-09-23: **8b merged: #734, main e78a93ce** (tree identical to 65cfcade, where the full roster ran clean). Release phase 1 runs from `~/chuggy-wt/events` detached at e78a93ce with its own full gate: kubectl shim over ssh to `dev2@10.100.0.1`, a `sed -i` shim for `deploy-to-gtr.test.sh:280` (GNU-only, pre-existing), `CHUG_CI_SUITES_BUDGET_SECS=900`, local postgres on 55439. Log `pr8/rig/release-e78a93ce-phase1.log`. Rig before: journal 8 rows (8a's sanity ticket), no pods in chuggy-work.
- 2026-09-23 ~20:57Z: **8b released with the wipe (main e78a93ce).** Phase 1's first run failed only at the render step (the kubectl shim sent `kustomize` to the node; it now runs locally); the rerun gated every gate clean and opened fabric #294 (mechanical: api digest ×8, source-commit ×9, migrate Job name). Dump `~/chuggy-effort/backups/chuggy-pre-e78a93ce.dump` (+ globals; 79 tables of data, pg_restore-listed). Importer suspended, seven deployments to 0, zero pods but postgres, none in chuggy-work; wipe with 02bd9572's script (journal 8 → 0). `deploy-to-gtr.sh --merge`: fabric merged e437ca3f, `chuggy-migrate-e78a93ce-registry` applied 14 (`project_continuation` gone, journal trigger present), seven rollouts 1/1; importer back unsuspended by flux; console `/`, `/drafts`, `/tickets`, `/tickets/1`, `/drafts/1` 200. Sanity ticket 1 (chuggy-development at e78a93ce, repository `https://github.com/kasofsk/chuggy.git` — the bound URL, not `chuggy`, or `POST /drafts` answers 404; two stages, one evaluator each; `finalization: None`): journal `TicketCreated`, `TicketDispatched`, `TicketWorkResultAccepted`, `TicketEvaluationProgressed`, `TicketEvaluationPassed`, `TicketFinalizationSucceeded` — **Done at seq 6** (8a's took 8: the reduce is gone); requests `2:0:ExecuteTask` SpawnWork, `3:0:ExecuteTask` and `4:0:ExecuteTask` SpawnEvaluation; no desk. API calls went through `sudo -u geoff /home/geoff/api-call.sh`; body files in the node's `/tmp`, removed. **8b is done; 8c is next.**

## PR 8c — split (orchestrator, 2026-09-23)

8c is cut in two. **8c-1: commands and refusals** — the decider's input is the package's `TicketCommand`, `decide` refuses in the package's thirteen words, the refusal reaches the wire and the console. **8c-2: Update** — `UpdateTicket`, `Ticket.revision`, `TicketUpdated`, the draft/ticket authoring split. 8c-1 fails by a principal refused wrongly or told nothing; 8c-2 by an edit landing on the wrong revision. **David chose (2026-09-23) to take the reports verbatim in 8c-1**: the package's `TaskTerminalReport` carries `ticket` in every arm and the reports ride inside journalled events, so 8c-1 moves the journal text and **releases with the wipe** (David approved the gtr wipe and the fabric merge for 8c-1); PR 9 stays wipe-free.

## PR 8c-1 — decisions (orchestrator, 2026-09-23)

Base: main e78a93ce (8b #734, released). Read before these: survey §1, §2, §7 and surprises 4–6; the package's `ticket.qnt` 70–160 (reports, `TicketCommand`, refusals), 230–300 (`TicketDecision`, `commandValid`), 470–845 (every decider and each `refused(...)`), 1340–1363 (`decisionValid`'s refused arm).

1. **`TicketCommand` is the package's, minus `UpdateTicket`.** `model/`: `CreateTicket(ReleasedTicket) | DispatchTicket({ ticket, source }) | RevokeTicket(int) | ResumeTicket(int) | ReportTaskTerminal(TaskTerminalReport) | ReportFinalizationResult(FinalizationResultReport)`, spelled verbatim; `DecisionEvent` is deleted and its name leaves the tree. `TaskTerminalReport` becomes verbatim (`ticket` in every arm); `TaskTerminalFact` stays `{ ticket, report }` as the package has it. `FinalizationResultReport = { ticket, workCycle, generation, result: FinalizationResult }` with the package's `FinalizationResult = FinalizationSucceeded(int) | FinalizationNeedsWork(int) | FinalizationResultUnavailable(int)` replacing chuggy's `FinalizationOutcome` and its separate `evidence`. `commandValid` is the package's (plus chuggy's bounds), replacing `reportValid` as the shape guard.
2. **`decide` returns `TicketDecision`.** `decide(graph, TicketCommand, policy): TicketDecided({ event, obligations }) | TicketRefused(TicketRefusal)`, the thirteen-arm `TicketRefusal` and its payload records verbatim, each decider checking in the package's order so the first failing check names the refusal, and computing the payload (`DependenciesNotFound`'s missing set, `DependenciesIncomplete`'s, the task, the cycle and generation). `decisionEventEnabled` is deleted: enabled means not refused. A refused decision changes nothing and is journalled nowhere (`decisionValid`'s refused arm: applying it is identity). The three refusals only `UpdateTicket` reaches — `TicketIdentityMismatch`, `TicketRevisionStale`, `TicketDependenciesChanged` — are declared now and unreachable until 8c-2. chuggy's model-bounded draw sets (`dispatchSources`, the evidence draws) stay the machine's draws, not refusals. 8b's named exception (a work failure must name the current cycle) stays; its refusal is `TaskNotCurrent`.
3. **`FinalizationNotCurrent` fences the cycle and the generation**, as the package does, not the phase alone (survey §2). The writer fills `workCycle`/`generation` from the `finalization_request` row the submission names; `submit_finalization_result`'s `BindingMismatch` stays the boundary's own fence.
4. **The envelope is renamed.** The interpreter's `TicketCommand` (`ticketCommand.ts`, survey 4) becomes `ProjectCommand` (`OperationCommand` is taken, `operationInbox.ts:79`); its `Decide` arm carries `command: TicketCommand` in place of `event`. The stored operation text changes; the wipe empties it.
5. **The command map leaves the adapter.** `readiness.ts`'s per-envelope `resolvedEvent` (release, the two dispatches, a native-action answer, the finalizer's submission, a completion) becomes one pure interpreter function from the envelope and the materials the adapter read to a `TicketCommand`; the adapter reads rows and calls it, and no file under `src/adapters/` constructs a `TicketCommand` literal.
6. **Refusals are stored with their payload and reach the wire.** The writer's `RefusalCode` is the thirteen domain names plus the boundary's own (`AuthoringChanged`, `ConfigurationInvalid`, `TicketChanged`, `SelectionChanged`, `ExecutionSourceUnreadable`, `ExecutionSourceDenied`, `BriefNamesNoRepository`); `NotEnabled` leaves, and `CommandUnreadable` with it (no producer, survey §2). `decision_input` stores the refusal payload beside `outcome_code`, in the codec's spelling, under a CHECK that enumerates the codes and requires the payload exactly for the thirteen. The operation response's `Refused` arm carries `refusal`, strict per code, absent for a boundary code; ticket numbers in the wire's ticket spelling.
7. **The console names the refusal.** `operationRefusalLabel`/`operationRefusalSentence` answer every code; a domain refusal's sentence names its numbers (which dependencies are not done, which task is not current, which cycle and generation). `threads.ts`/`operationFollow.ts` follow. No new page, no new offer.
8. **Versions.** `decisionSemanticsVersionCurrent` 7 → 8; `event_schema_version` moves if it names the journal text's shape (A says).
9. **015** = the guard as 008–014 with the wipe; the journal validator's report arms carry and check `ticket`; the command validator rebuilt over the six tags in the codec's spelling, `ticket_command_is_valid`/`public_ticket_command_is_valid` keeping the two authorization-by-shape exclusions in the new names (`CreateTicket`, `ReportFinalizationResult`); `operation.command_tag`'s authority constraint and `accept_operation`'s priority classification over the new tags; `submit_task_completion` builds `ReportTaskTerminal` (rewritten whole, signature unchanged); `decision_input`'s refusal column and code CHECK. Render-diff 001–014 empty. **The wipe before 015 is e78a93ce's script** (the previous release's, 8b's lesson); 015 adds no table the wipe names unless S says otherwise.
10. **Goldens and conformance carry refusals.** Every refusal reachable in the model's instance is compared model ↔ TypeScript, in a golden or a table the model emits (A picks and says which); the thirteen goldens re-emitted in the new command spelling.
11. **Out**: `UpdateTicket`, `TicketUpdated`, `Ticket.revision`, the update-only refusals' reachability, the authoring split (8c-2); `TicketState` (PR 9); the worker and the fabric's manifests.

## PR 8c-1 sequence

A. model (`ticket.qnt`, `domain.qnt`, `refinement.qnt`, `api.qnt`, tests), goldens, generated, `src/domain`, `src/actor`, conformance. opus. Branch `model/ticket-commands`.
S. migration 015 + `migration.test.ts` + render-diff 001–014 empty. opus, parallel with A. Branch `schema/ticket-commands`, postgres on 55440, merged after A.
B. interpreter, adapters, contract on the merged tip. opus, postgres on 55441.
C. console, on B's tip. opus.
Then: round 1 machine + boundary reviews (fresh), fixes, full roster, merge, fabric PR, release with the wipe (zero worker pods first, e78a93ce's wipe script), sanity ticket to Done and one refused command read back through the API.

## PR 8c-1 — progress

- 2026-09-23: 8c-1 decisions written; A and S launched in parallel (opus, own worktrees off e78a93ce). B and C briefs drafted (`tasks/8c-1/`), tips filled when A and S land.
- S landed 47af8924 (`tasks/8c-1/S-report.md`). **Correction to decision 4: the Decide arm carries `ticketCommand`, not `command`** (`command` is the envelope discriminator). The guard also refuses a non-empty `operation`; e78a93ce's wipe script empties it. `submit_finalization_result` builds no command; the writer does. Watch `sessionStoreRows` (a 10 s timeout under load, unconfirmed).
- A landed 60947d77 (`tasks/8c-1/A-report.md`); merged with S at cdc7ead7 (clean), pushed `model/ticket-commands`. Refusal = stutter. **Behaviour change accepted as the package's:** a release may name a Revoked dependency (only a missing one refuses, `DependenciesNotFound`); such a ticket stays Pending until revoked — reviewers to confirm nothing at the boundary relied on the old `dependableIn` refusal. **`event_schema_version` stays 1** (8b's precedent: the wipe empties the journal and semantics 8 gates replay). B launched on cdc7ead7.
- B landed 1e257fa7 (`tasks/8c-1/B-report.md`), pushed as `model/ticket-commands`. **Correction to decision 3**: `finalization_request` held no cycle or generation; 015 adds `work_cycle`/`finalization_generation`. Command map is `ticketCommandOf` (`src/interpreter/commandMap.ts`). All B gates 0 but check-source (C's `ui/` + playwright baseline). C launched on 1e257fa7.
- C landed 4afc1244 (`tasks/8c-1/C-report.md`), pushed. All C gates 0 but the playwright baseline. Round 1 machine + boundary reviews launched on 4afc1244; ledger `reviews/8c-1-ledger.md`.
- Round 1 machine: CHANGES (`reviews/8c-1-round1-machine.md`). (1) the writer lost the release-room guard (`canReleaseIn`: fleet < `nTickets`, id within `ticketIdUniverse`) — a chuggy bound the package lacks, configured per project in `domain_configuration`, so the writer can now journal a state `ticketIdsWellFormed` forbids. **Decision: keep it as a boundary refusal** (the deployed bound is real; retiring it is a product change nobody asked for): the writer refuses a release outside the room before `decide` with a new boundary code `TicketCapacityReached`, no payload, in the roster, 015's CHECK and the console; the two comments fixed. (2) `storedJournalLegalOn` does not check a journalled report's `ticket` equals the event's — add it beside `eventTicketStands` (015 already refuses it on insert).
- Round 1 boundary: CHANGES (`reviews/8c-1-round1-boundary.md`). **Decision: a finalizer result whose request is closed while the ticket still awaits that (cycle, generation) is refused with a new boundary code `FinalizationRequestClosed`, no payload**; `FinalizationNotCurrent` is stored only when `decide` returned it. Round 1 fixes to one builder (F) on 4afc1244: both reviews' findings, the two new boundary codes, the wire's `TicketRevisionStale` numbers positive.
- F landed 07abd55b (`tasks/8c-1/F-report.md`), pushed; every round 1 finding fixed and red-proved; orchestrator read the writer and replay diffs. Full roster running at 07abd55b (`ci-full-8c1-07abd55b.log`, postgres on 55444).
- Full roster at 07abd55b: every gate 0 but two shell suites — `check-conformance.test.sh` (its tamper fixture assumed a golden's state 1 is a decided release; now a refusal can be first; fixed c5d0ea83 by pointing it at `work-execution-unavailable`, 9/9) and `deploy-to-gtr.test.sh` (65 s over the 60 s cap, 125/125 alone; the false-reds shape). PR #735 opened at c5d0ea83; the admin merge was denied by the permission classifier — waiting on David.
- 2026-09-24: **David approved the admin merge and the deploy. 8c-1 merged (#735, main b6e5b4fb, tree = c5d0ea83) and released with the wipe.** Phase 1 gated every gate clean and opened fabric #295 (mechanical: digests, source-commit, migrate Job name) (`rig/release-b6e5b4fb-phase1.log`). Importer suspended, seven deployments 0, zero pods but postgres, none in chuggy-work; dump `~/chuggy-effort/backups/chuggy-pre-b6e5b4fb.dump` (+ globals; 78 tables of data); wipe with e78a93ce's script (journal 6 → 0; operation, decision_input, finalization_request 0). `--merge`: fabric ec1ca936, `chuggy-migrate-b6e5b4fb-registry` applied 15 (`decision_input.refusal`, `finalization_request.work_cycle` present), seven rollouts 1/1, importer unsuspended; console 200. Sanity: drafts 1 and 2 (2 depends on 1) released at seq 1, 2; `ManualDispatch` of 2 **Refused `DependenciesIncomplete {ticket: 2, dependencies: [1]}`** on the wire and in `decision_input.refusal`, journal unmoved; ticket 1 dispatched → `TicketDispatched`, `TicketWorkResultAccepted`, `TicketEvaluationProgressed`, `TicketEvaluationPassed`, `TicketFinalizationSucceeded` — **Done at seq 7**; requests `3:0`, `4:0`, `5:0:ExecuteTask`. Ticket 2 left Pending. **8c-1 is done; 8c-2 is next.**

## PR 8c-2 — decisions (orchestrator, 2026-09-24; the authoring split is David's choice)

Base: main b6e5b4fb (8c-1 #735, released). Read before these: survey §4, §5, §7 and surprise 7; the package's `ticket.qnt` 70–115 (`Ticket.revision`, `UpdateTicket`), 150–200 (`TicketUpdated`), 495–525 (`decideUpdate`), 880–915 (`evolve`'s `TicketCreated`/`TicketUpdated`), 1230–1245 (the revision invariant).

1. **The draft stays the one editor (David, 2026-09-24).** While its ticket is `Pending`, a released draft is revisable again, and releasing it again sends `UpdateTicket`: the definition is re-resolved from the revised draft at the configuration revision the release names, exactly as the first release resolves it (8a decisions 4–6), so an update may also re-pin the configuration. Once the ticket leaves `Pending` the draft is closed to revision as today (`NotDraft`). The ticket page gains an edit offer while the ticket is `Pending`; it opens the draft for revision and releases the update.
2. **Model: `UpdateTicket`, `TicketUpdated`, `Ticket.revision`, verbatim.** `TicketCommand` gains `UpdateTicket({ ticket, expectedRevision, definition: ReleasedTicket })`; `TicketEvent` gains `TicketUpdated({ ticket, revision, definition })` (the eighteenth arm); `Ticket.revision` is `1` at `TicketCreated` and bumped by `TicketUpdated`; `decideUpdate` and `evolve`'s arm are the package's, so `TicketNotPending`, `TicketIdentityMismatch`, `TicketRevisionStale`, `TicketDependenciesChanged` become reachable and are compared model ↔ TypeScript like the rest (8c-1 decision 10). An update owes no obligation.
3. **No wipe; semantics stays 8.** Adding a command and an event arm moves no stored row: every journalled row replays to the same state with `revision` 1 on each created ticket. A proves that (a stored 8c-1 journal replays legal and equal under the new `evolve`, as a test over a golden's rows); if it cannot, stop and report rather than bump. `event_schema_version` stays 1.
4. **The boundary.** A new public mutation `UpdateTicket { ticket, expectedRevision, authoringVersion, configurationRevision }` beside `ReleaseDraft` (which keeps its meaning: the first release); the envelope gains the matching `ProjectCommand` arm, fenced like `ReleaseDraft` on the draft's authoring version, configuration revision and digest (`release_draft_fenced`'s fence, reused); `ticketCommandOf` resolves it to `UpdateTicket` with the definition the release path mints. Its refusals are `decide`'s four plus the release path's boundary codes (`AuthoringChanged`, `ConfigurationInvalid`, `BriefNamesNoRepository`).
5. **The draft door.** `revise_draft` admits a `Released` draft whose ticket is `Pending`, and refuses there a revision that changes the dependencies with a new draft-door result `DependenciesLocked` (so a draft can never hold authoring its ticket's update must refuse); a `Released` draft whose ticket has left `Pending` answers `NotDraft` as today. The draft keeps the authoring version each ticket revision was released from, so the ticket page can say which draft revision is live and whether the draft holds unreleased changes.
6. **The projection and the wire.** `ticket_projection` gains `revision` (NOT NULL, 1 for a release); `ticketResponseSchema` carries `revision`; the draft read carries the released authoring version beside the current one. The console reads `revision` as `expectedRevision`; nothing else on the wire moves.
7. **The console.** The ticket page offers Edit while `Pending` (`core/ticketOffers.ts`, `core/ticketActions.ts`), opening the creation form in an edit mode prefilled from the draft with the dependencies shown and locked; submitting revises the draft and sends `UpdateTicket`. `TicketProvenance.tsx` names the live revision and the draft authoring version it came from, and says when the draft holds unreleased changes. A refused update says which of the four refusals, in 8c-1's sentences.
8. **016**, no wipe: the journal validator's `TicketUpdated` arm (its `ReleasedTicket` as `TicketCreated`'s, `revision` > 1); the command validator's `UpdateTicket` arm (`expectedRevision` > 0); the public admission of the new mutation (not an authorization-by-shape exclusion: a principal may update); `ticket_projection.revision`; `revise_draft` rewritten whole (decision 5), signature unchanged or a new overload with the old dropped — S says which; the draft's released authoring version column; `DependenciesLocked` wherever the draft door's results are enumerated. Render-diff 001–015 empty.
9. **Goldens** carry: an update then a dispatch that runs the updated definition; a stale `expectedRevision` refused; a dependency change refused; an update after dispatch refused `TicketNotPending`; identity mismatch if the instance can draw it.
10. **Out**: `TicketState` (PR 9); editing a ticket past `Pending`; changing dependencies after release.

## PR 8c-2 sequence

A. model, goldens, generated, `src/domain`, `src/actor`, conformance. opus. Branch `model/ticket-update`.
S. migration 016 + `migration.test.ts` + render-diff 001–015 empty. opus, parallel with A. Branch `schema/ticket-update`, postgres on 55445, merged after A.
B. interpreter, adapters, contract on the merged tip. opus, postgres on 55446.
C. console, on B's tip. opus.
Then: round 1 machine + boundary reviews (fresh), fixes, full roster, merge, release **without** the wipe (phase 1, scale down, dump, `--merge`), sanity: update ticket 2 (Pending on the rig) and dispatch it to Done on the updated definition, plus one stale update refused.

## PR 8c-2 — progress

- 2026-09-24: 8c-2 decisions written (David chose the draft-reopens split). A and S launched in parallel (opus, own worktrees off b6e5b4fb).
- S landed cef37d77 (`tasks/8c-2/S-report.md`). **Decision 11 (from S's gap 1): what a ticket runs is what was released, never the live draft.** `draft_brief` is unversioned, so revising a Pending ticket's draft would leak into dispatch. Each release (first or update) snapshots the brief material the briefing composes from (intent, links, checks, finalization binding, repository) into the released material (`ticket_definition`, upserted in the update transaction), and dispatch and the briefing read only that; nothing on the dispatch path reads `draft_brief`. The failing `authoring.test.ts` case is restated to that: an unreleased revision does not move what a retry or dispatch reads. S also fixed `submit_task_completion` to read the latest definition.
- A landed dea8951a (20 goldens; no-wipe proof `test/actor/storedBeforeUpdate.test.ts` over main's `evaluation-blocked-resume` rows; semantics 8). Merged with S at ae1da0d9, pushed. B launched.
- B landed 5a30137c, pushed. Snapshot is `ticket_definition.brief` (digest-checked on read); scheduler, finalizer and `submit_finalization_result` read it, not `draft_brief`; an update re-pins the configuration. Leftover reds from A/S: `check-paths` (storedBeforeUpdate.test.ts:7), `check-duplication` (016 restating 014/015), lint max-lines in migration.test. **Decision: ticket reads (title, brief) show the released snapshot**, the draft page shows the draft. F (fixes) and C (console) launched in parallel on 5a30137c.
- F landed ea337531 (2a3fe6b8 paths, 800a68b6 016 spells restated names as literals — render byte-identical, 138/138, 107571f4 lint helpers, ea337531 ticket reads from `ticket_definition.brief`; API role gains SELECT(tenant, project, ticket, brief) on it, not `definition`). Gates 0 but the playwright baseline. Waiting on C (on 5a30137c) to merge the two.
- C landed e8952dba (Edit offer, `/tickets/$ticket/edit`, provenance lines; console brief panel still reads the draft). Merged with F at a26bf034, pushed. Round 1 reviews launched on a26bf034.
- F2 landed a173815d, a fast-forward of a26bf034. It holds the TS refusal-order table, the postgres re-pin and restart case, and the ticket page drawn from ticket.brief and the released pin (016 grants the API SELECT on configuration_revision). All red-proven; gates 0. Open: provenance evaluation stages and ledger grouping still read the draft authoring. Round 2 review and ci-full launched.
- Round 2: CHANGES, one finding. The ledger grouping (ticketPageFacts.ts:84-90 → ticketLedger) and the provenance "evaluation stages" read the draft program, which a revision can change without an update. The result is phantom Queued/Missing stages, "of N" labels and a wrong cycleComplete. Round 1 is confirmed closed; gates 0.
- F3 landed b4869932 (fa1cdfed: the ticket read carries its released program via draft_revision at released_authoring_version, with no grant change; b4869932: the ledger and provenance stages come from ticket.program). Red-proven. Postgres limit: the harness cannot commit an update with a different program; that is proved at the UI tier. Round 3 review and ci-full launched.
- Round 3: APPROVE at b4869932. One nit (the ticketPageFacts header still mentioned the draft) fixed by the orchestrator at 95326551, a comment only; check-comments, figures, paths and prettier all 0. ci-full running at b4869932.
- ci-full at b4869932: every gate 0 except the known deploy-to-gtr.test.sh 60s cap false red (rc=124); it passes alone (125/125, 47s). 95326551 is a comment-only change on top. PR #736 opened.
- #736 merged as ccbf0c38 (tree = 95326551). Awaiting deploy approval (no wipe).
- Release ccbf0c38 is live (fabric #296 b34d6435, migrate applied 16, rollouts 1/1, console 200). Flux restored the scale-down before the first dump, so it ran live. The --merge archive dump was taken while scaled down. Sanity found a production bug: the first UpdateTicket wedged the partition (ActivationFailed: permission denied for table ticket_projection), because decisionRepin writes configuration_revision/digest but 016 grants the ticket service UPDATE(revision) only. The user approved a hotfix grant on the rig; the classifier blocked it, so it was handed to the user to run. 017 dispatched (tasks/8c-3/G.md).
- G landed on model/repin-grant (49f21d3c: migration 017 grant plus migration cases; a6e750c8: the harness decides as the ticket-service role and accepts as the api role, across all 78 suites). Red-proven: 017 emptied gives the rig error in 4 authoring cases; the old harness passes (the blind spot). Gates 0 except the rig baseline. Review launched.
- 017 review: APPROVE at a6e750c8. Nit (a) fixed at 47e1dc6f. Uncovered (not live): executionSources.observe binding read, journal legality, domain-config precondition, forge installations and harness.authoring still run as owner in suites; epoch catch-all in harness. ci-full launched.
- User-approved hotfix grant applied on the rig 2026-09-24; the partition is unwedged. Sanity: ticket 2 was updated to revision 2 (seq 8); the stale update was refused TicketRevisionStale {expected:1,current:2}; ManualDispatch then ran to Done at seq 13 on the revision-2 brief (ticket_definition confirms). 8c-2 released.
- ci-full at 47e1dc6f: all gates clean. PR opened.
- #737 merged as f8998a22.
- Released f8998a22: fabric #297 merged 1e987b1f, migrate applied 17, rollouts 1/1, importer unsuspended, no ActivationFailed, console 200, ticket 2 Done at seq 13. Backup at backups/merge-phase-f8998a22/.
