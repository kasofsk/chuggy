Step 8a of the ticket-language convergence (`chug-ticket-domain` at 76c95a9): **the released ticket.**

A ticket is what its release froze. `Ticket.definition` is the package's `ReleasedTicket` — an opaque content reference, the dependencies, the work task's definition, an evaluation plan whose every evaluator carries its own task definition, and a finalization configuration — in place of the dependency set and the program the record carried. Both package copies, `model/task-contract/task.qnt` and `model/ticket-domain/evaluation/evaluation.qnt`, are now verbatim; the six divergences 7b listed in `model/AGENTS.md` close here.

A ticket also carries the source it runs at. The dispatch observes it, before the decision and for the dispatch alone: an unreadable or denied remote is refused with no journal row where it used to be a park on a re-taken edge. A passed work report carries the source the result was accepted at and the reference of the result itself; the instance is begun from both, and every rework, resume, evaluator and finalizer runs at that accepted source without observing a second. A produced report carries its whole obligation — identity, definition and context — and is admitted only when it equals the one the ticket owes.

No reference is authored. The release resolves each as a fold of a digest of the material it names — the brief, the image, the block the briefing composes from, the materialized execution requirement, the manifest contract, the finalization binding as it stands at release — and stores that material in `ticket_definition`, which dispatch reads instead of materializing the requirement per request. The wire, the console and the configuration document are untouched.

**Migration 013.** Guard as 008–012: refuses while any journal row exists and names `deploy/rig/wipe-tickets.sql`. `decision_event_is_valid` admits `CreateTicket` as the definition, `Dispatch` with its source and the two produced reports with their obligations, and drops three conditions for shapes earlier steps removed. Two tables: `ticket_definition` and `ticket_source`, both in the wipe. `submit_task_completion` is rewritten whole, building the obligation from rows under the lock — the definition from the journal's release row, an evaluator's context from the fold of its cycle's accepted work manifest — and refusing by name a result with no source row or no passed work result; `submit_worker_result` writes the source row before the completion that reads it. The release index moves to the definition's `id`, and the completion authority check no longer names a tag 7b retired.

No fabric or worker release: the pod is handed a server-composed briefing and the manifest is untouched.

Survey, decisions, task reports and reviews: `~/claude/chuggy-effort/ticket-language/pr8/`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
