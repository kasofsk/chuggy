# C report (8c-1) — filed by the orchestrator from the builder's hand-back

Tip 4afc1244 "the console names what a refusal was over, from the refusal the operation carries" (one commit on 1e257fa7, local branch model/ticket-commands-8c-c; pushed by the orchestrator). Hook clean.

- Console `OperationRefusal` = the wire's `refusal` ({type, value}) for a machine code, {type} for a boundary code; `Settled` carries `refusal` in place of `refusalCode`; `operationRefusalLabel` keyed by code; `operationRefusalSentence` reads the payload (separate switch for machine refusals). NotEnabled/CommandUnreadable gone. `operationFailureSentence`/`Label` no longer read an actor refusal code (no HTTP route sends one as failure) — falls back to "console does not know it". threads.ts unchanged. Nothing under src/ changed.
- Samples (label | sentence): TicketAlreadyExists | Ticket already exists | #3 already exists, so there is nothing left to release. DependenciesNotFound | Dependencies not found | #3 depends on #7 and #5, which the project has no ticket for. SelfDependency | Depends on itself | #3 names itself as a dependency; revise it to depend only on other tickets. TicketNotFound | Ticket not found | the project has no ticket #3. TicketNotPending | No longer pending | #3 is no longer pending, and only a pending ticket accepts this. TicketIdentityMismatch | Different ticket | what was submitted describes a different ticket from #3. TicketRevisionStale | Revision out of date | this was written against revision 2 of #3, which is at revision 4 now. TicketDependenciesChanged | Dependencies changed | what #3 depends on cannot change once it is released. DependenciesIncomplete | Dependencies not done | #3 waits on #2, which is not done yet. TicketNotRevocable | Not revocable | #3 is past the point where it can be revoked. TicketNotResumable | Nothing to resume | #3 is not stopped at anything a resume can pick up. TaskNotCurrent | Task not current | the report was for the work of cycle 1 of #3, which is not the task the ticket is waiting on. FinalizationNotCurrent | Finalization not current | the finalization result was for work cycle 2, generation 1 of #3, which is not the finalization the ticket is waiting on. Seven boundary codes unchanged.
- Files (ui/chuggy-ui/): app/core/{codeSentences,codeLabels,operationFollow,ticketCreationRun}.ts, app/browser/{Inbox,TicketActions}.tsx, test/{codeSentences,codeLabels,operationFollow,inboxAnswers}.test.ts, test/repositoryPage.test.tsx (unknown-code sample NotEnabled → RepositoryLocked); new test/operationRefusalSentences.test.ts; new test/ui/operationRefusals.test.ts (end to end interpreter refusal → operationResponse → contract parse → follow → label/sentence; codes seen = operationRefusalCodes).
- Gates: check-source 1 (test/rig playwright only), check-console 0, console-sheets 0, figures 0, comments 0, paths 0, duplication 0; test/ui 9/9.
