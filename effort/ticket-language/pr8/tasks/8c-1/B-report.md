# B report (8c-1) — filed by the orchestrator from the builder's hand-back

Tip 1e257fa7 (local branch model/ticket-commands-8c-b off cdc7ead7; pushed by the orchestrator as model/ticket-commands): 93c91307 "the writer decides the package's commands and keeps each refusal it is given", 1e257fa7 "a refusal's vocabulary is a module of its own, so the codec that reads it closes no cycle". Hook clean both.

- interpreter: ticketCommand.ts → projectCommand.ts (`ProjectCommand`, `Decide{ticketCommand}`); new refusal.ts (`Refusal = TicketRefusal | {type: BoundaryRefusalCode}`, 13+7 codes, allRefusalCodes, allBoundaryRefusalCodes, boundaryRefusal, isTicketRefusal) — moved out of projectDecision.ts to break wire→projectDecision→projectStore→wire cycle. projectWriter answers with decide's result. wire.ts: encodeRefusalText, parseStoredRefusal, parseRefusalValue; stored completion refused for extra keys (re-encode compare; generated decoder drops unknown keys).
- adapters: readiness.ts reads rows, calls the map; decision.ts writes outcome_code = refusal.type and decision_input.refusal; finalization_request work_cycle/finalization_generation inserted; nativeReads/nativeWeb/http codecs+contract carry refusal.
- contract: operationRefusalCodes = 13 machine (model order) + 7 boundary; Refused arm discriminatedUnion("code"); contractDocument.json no diff (request schemas only); representations.ts gains ticketRefusals, refusedOperation. Public mutations unchanged.
- Wire refusal = strictObject{type, value}; boundary code: no key. Ticket-only: TicketAlreadyExists, SelfDependency, TicketNotFound, TicketNotPending, TicketIdentityMismatch, TicketDependenciesChanged, TicketNotRevocable, TicketNotResumable. Dependencies*: {ticket, dependencies: page(ticketNumberSchema).min(1)}. TicketRevisionStale {ticket, expected: count, current: count}. TaskNotCurrent {ticket, task: taskIdentitySchema}. FinalizationNotCurrent {ticket, workCycle: ticketNumberSchema, generation: ticketNumberSchema}.
- Command map: `ticketCommandOf` in src/interpreter/commandMap.ts (ReleaseDraft with 8a's releaseTicketEvent folded in, Decide, ResolveNativeAction, SubmitFinalizationResult with cycle/generation from the request row, both dispatches). Grep of command literals under src/adapters: none; name hits only http/contract.ts:515,523 (public mutation cases; command built by actor constructors) and schema/README.md prose.
- Former NotEnabled sites: preflight/dispatch plan → decide's refusal; closed native action → decide's refusal else TicketChanged; closed finalization request → decide's refusal else FinalizationNotCurrent from the command; release outside commandValid or unresolved → ConfigurationInvalid; any other command outside commandValid → IntegrityContradiction (the writer's existing error class); source → its boundary code.
- 015 gained finalization_request.work_cycle, finalization_generation bigint NOT NULL CHECK ≥1 (`finalization_request_attempt_is_named`), no default (guard empties the table). No grants changed.
- Revoked dependency: no boundary check refused it; the only refusal was the deleted decisionEventEnabled/dependableIn (NotEnabled). authoring.ts:387 (dependable=true) still filters revoked tickets out of a draft's offered candidates. No refusal added.
- Tests: every code round-trips (nativeReadRows, responses, rosters; allRefusalCodes = operationRefusalCodes); DependenciesIncomplete wire body exact; boundary refusal carries none; migration.test checks 015 CHECK literals = allRefusalCodes; dispatchWriter: DependenciesIncomplete{2,{1}}, stale completion TaskNotCurrent, old-generation FinalizationNotCurrent{1,1,1}; finalizerQueue (postgres) moved request settles FinalizationNotCurrent; i3/projection/ticketServiceRun Work → Evaluation → Done.
- No files outside B's layers.
- ui/ sites for C: type errors codeSentences.ts:69,71,81; stale spellings codeLabels.ts:455,465; tests test/codeSentences.test.ts:4,98,101, test/codeLabels.test.ts:271, test/operationFollow.test.ts:113,122,138, test/repositoryPage.test.tsx:314,322.
- Gates: check-source 1 (ui codeSentences + test/rig playwright only), boundaries 0, queries 0, postgres 0 (sessionStoreRows timeout did not recur), conformance 0, random 0, figures 0, comments 0, paths 0, duplication 0.
- GOAL/reports wrong: decision 3 — finalization_request held neither column (015 adds them); S's hook-clean missed a max-lines lint on its test, stale command_tag expectations, and privileges.test's forged completion in the old `event` spelling; A's report omitted that decisionEvent.ts held the only revoked-dependency check.
