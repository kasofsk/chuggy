# A report (8c-1) — filed by the orchestrator from the builder's hand-back

Tip 60947d77 on model/ticket-commands (off e78a93ce): 108f2170 model, 2a7e38a3 generated/domain/actor, 6278c92e tests, 2bb954c6 goldens, 60947d77 comment cap. All five `--no-verify` (hook's check-source cannot pass until B compiles).

- model: `take(command, onFailure)` guards on commandValid then matches `decide`; new `refuse` action draws refusedCommandsIn (commandProbesIn filtered to refused); refinement journals accepted events only; refusal rlabel "actor-refused". **A refusal is a stutter**: tickets unchanged, lastStep' = Refused(reason), nothing journalled or emitted. A command failing commandValid is not taken at all (no refusal).
- src/domain: deciders return TicketDecision; enablement.ts holds only draw sets (commandProbesIn, refusedCommandsIn, failureReportOf, finalizationReportOf; the UI imports the file so the name stays); finalizableIn, finalizationOutcome*, outstandingTaskIn removed; config.finalizationResults; decisionValid/applyDecision/invariants cover Refused (post == pre).
- src/actor: decisionEvent.ts removed, command.ts replaces it (createTicketCommand, revokeTicketCommand, dispatchTicketCommand(ticket, source), reportTaskTerminalCommand(report), reportFinalizationResultCommand(ticket, workCycle, generation, result), resumeTicketCommand, commandSubject); journalStep journals nothing on refusal; decideValid throws on !commandValid; effectCrash throws on a refused command; semantics 8.
- Goldens: 14 (343 steps), new `task-not-current` (seed 0x1, 60000 samples, maxSteps 32, invariant `lastRefusal != "TaskNotCurrent"`, 22 steps). Seed not re-pinned.
- Spellings (model-api.ts, {type,value}): TicketCommand CreateTicket: ReleasedTicket | DispatchTicket {ticket, source} | RevokeTicket n | ResumeTicket n | ReportTaskTerminal TaskTerminalReport | ReportFinalizationResult {ticket, workCycle, generation, result: FinalizationResult}. TaskTerminalReport WorkResultReport {ticket, result, acceptedSourceRef} | EvaluationResultReport {ticket, result, verdict} | TerminalFailureReport {ticket, failure, kind}. TicketRefusal: TicketAlreadyExists n, DependenciesNotFound {ticket, dependencies: Set}, SelfDependency n, TicketNotFound n, TicketNotPending n, TicketIdentityMismatch n, TicketRevisionStale {ticket, expected, current}, TicketDependenciesChanged n, DependenciesIncomplete {ticket, dependencies}, TicketNotRevocable n, TicketNotResumable n, TaskNotCurrent {ticket, task: TaskIdentity}, FinalizationNotCurrent {ticket, workCycle, generation}.
- Refusals compared model↔TS via goldens + conformance replay: walk (DependenciesIncomplete, FinalizationNotCurrent, SelfDependency, TicketAlreadyExists, TicketNotFound, TicketNotResumable, TicketNotRevocable), evaluation-blocked (TicketNotPending), DependenciesNotFound (rework-started-eval-failure, revoke-cancels-tasks, task-not-current), task-not-current (TaskNotCurrent); coverage.test.ts asserts every reachable refusal appears. The three update-only are unreachable (codec round-trip + unit tests only). The directed emitter has no `refuse`, so long goldens carry none.
- event_schema_version (journal.ts:121 check, :261 write, digest.ts:62/125, relations.ts:630 default) not moved; 8b left it at 1 too.
- B must change: interpreter/ticketCommand.ts:31,33,179; projectWriter.ts:61-66; projectDiscovery.ts:39; finalizer.ts:78,630,632,637; finalizerTelemetry.ts:31,80; wire.ts:32,242; operationInbox.ts; adapters/postgres/readiness.ts:100-103; adapters/postgres/finalizer.ts; adapters/http/contract.ts:12; tests interpreter/{nativeWeb, i3, wire, schedulerContext, reworkCap, dispatchView, dispatchWriter, projection}, postgres/{nativeActionAdmits, sourceDeferral, finalizerClosure, schedulerHarness, readiness, finalizerHarness, ticketProjection, harness}; roots/* and ui/mutationSentences transitive.
- Gates: check-model 0 (124 tests; log scratchpad/ac909/gate-check-model.log), conformance 0, random 0, model-api 0, figures 0, comments 0, paths 0, duplication 0; check-source 1 (B's layers + test/rig playwright only; 213 unit pass in A's layers). No files outside A's layers.
- GOAL wrong: (1) decideCreate admits a Revoked (or any existing) dependency — only a missing one refuses — while the release draw still uses dependableTickets; (2) id universe/release room are draw bounds, not refusals; (3) event_schema_version question; (4) directed emitter has no refuse; (5) three update-only refusals unreachable.
