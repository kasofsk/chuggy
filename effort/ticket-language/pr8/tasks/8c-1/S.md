# Task S (PR 8c-1) — migration 015: the inbox holds the package's commands, reports carry their ticket, refusals are stored with their payload

Branch `schema/ticket-commands` off `origin/main` e78a93ce. Setup: `_setup.md` beside this file (read it first; it is part of this brief). Use your own postgres: `docker run -d --name chuggy-check-postgres-8c-s -e POSTGRES_PASSWORD=chuggy-check -p 55440:5432 postgres:18-alpine`, `CHUG_PG_URL` on 55440; remove the container when done.

Read first: `/Users/david/chuggy-effort/ticket-language/pr8/GOAL.md` §"PR 8c — split" and §"PR 8c-1 — decisions" (6 and 9 are yours; 1–5 say what the rows carry), the 8b decisions and progress lines above them, `pr8/survey.md` §2 and surprises 5, 6, the package's `model/ticket-domain/ticket.qnt` lines 70–160, `pr8/tasks/8b/S.md` and `S-report.md` (014's shape and red-proofs), `src/adapters/postgres/schema/migrations/{012,013,014}-*.ts` whole, `baseline/{relations,constraints}.ts` for `decision_input` and `operation`, `deploy/rig/wipe-tickets.sql`, the notes `migrations-render-literals.md` and `migrations-edited-in-place.md` (see `_setup.md`), `CLAUDE.md`, `.chug/tasks/review-change.md`.

## Scope

`src/adapters/postgres/schema/migrations/015-*.ts` and `index.ts`, `test/postgres/migration.test.ts` and any postgres fixture that spells a journal row or an operation command, `deploy/rig/wipe-tickets.sql` only if 015 adds a table, the schema README if it describes what the rows carry.

- **Guard** as 008–014: refuse while any `journal_entry` row exists, naming `deploy/rig/wipe-tickets.sql`; the header says why (a journalled report now carries its ticket) and that the wipe before 015 is the previous release's script.
- **Spellings.** A runs in parallel and its codec is not written yet: spell the arms from the package's types in the codec's `{"type","value"}` idiom as 014 does, in consts at the top so a rename lands in one place, and expect B to align when A lands.
- **Journal validator.** `decision_event_is_valid`, rebuilt whole from 014's: every arm carrying a `TaskTerminalReport` requires the arm's positive `ticket`, equal to the fact's `ticket`; everything else as 014 admits it.
- **Command validator.** Rebuilt whole over the six `TicketCommand` tags (decision 1): `CreateTicket`, `DispatchTicket { ticket, source }`, `RevokeTicket`, `ResumeTicket`, `ReportTaskTerminal` (the report as the journal validator checks it), `ReportFinalizationResult { ticket, workCycle, generation, result }` with a positive evidence in each result arm; every 8b tag (`Revoke`, `Dispatch`, `TaskDone`, `FinalizationResult`) refused. `ticket_command_is_valid`/`public_ticket_command_is_valid` keep their two authorization-by-shape exclusions over `CreateTicket` and `ReportFinalizationResult`, and whatever else they exclude today, renamed.
- **The inbox.** `operation.command_tag`'s authority constraint, `accept_operation`'s priority classification (the revoke's safety priority keeps its meaning) and anything else naming an 8b command tag, over the new tags; the envelope's `Decide` arm carries `command` in place of `event` (decision 4) wherever SQL opens it. `submit_task_completion` builds `ReportTaskTerminal` with the report's `ticket` (rewritten whole as 013/014 did, signature unchanged). Say whether `submit_finalization_result` builds a command (if it does, it builds `ReportFinalizationResult`).
- **Refusals (decision 6).** `decision_input` gains the refusal payload column (jsonb or text as the table's other stored text is; say which and why), a CHECK enumerating the refusal codes — the thirteen domain names and the seven boundary codes, `NotEnabled` and `CommandUnreadable` gone — and a CHECK that the payload is present exactly for the thirteen, validated by a function over their payload shapes. Any other CHECK or function naming `NotEnabled` follows.
- Landed migrations are not edited. Render-diff of 001–014 main vs branch empty (8a's script is at `pr8/scratch/S/render.mjs`).
- `test/postgres/migration.test.ts`: fresh install records 015; the guard; journal rows (a report arm without `ticket` refused, with a mismatched `ticket` refused, a good one admitted); a command table (one admitted per tag, each 8b tag refused, the authorization exclusions still refused); `decision_input`'s codes (each admitted, `NotEnabled` refused, a domain code without payload refused, a boundary code with one refused, a malformed payload refused). Red-proof each new case against a fresh prepare; name each mutation in the report.

Single-suite recipe: `node --experimental-strip-types .chug/tasks/postgres-databases.ts prepare postgres://postgres:chuggy-check@127.0.0.1:55440/postgres <db>`, then `CHUG_PG_URL=postgres://postgres:chuggy-check@127.0.0.1:55440/<db> node --experimental-strip-types --test --test-name-pattern='…' test/postgres/migration.test.ts`; drop the database after. Full `check-postgres` on your tip will be red where TypeScript still writes 8b's spellings — B's; report which suites.

## Report

Tip; 015's shape; the spellings each validator admits; the refusal column and its CHECKs; what `submit_task_completion` and `submit_finalization_result` build; render-diff result; red-proofs; which postgres suites are red and why they are B's; anything GOAL.md got wrong. Under ~50 lines.
