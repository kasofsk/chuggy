# S report (8c-2) — filed by the orchestrator from the builder's hand-back

Tip cef37d77 on schema/ticket-update (one commit on b6e5b4fb): 016-ticket-update.ts, index.ts, schema README, migration.test.ts (137/137), fixtures finalizer.test.ts (×2), leadToolsDurable.test.ts (write released_authoring_version). No table added; wipe script untouched.

- No guard, no wipe. decision_event_is_valid, decision_command_is_valid (CREATE OR REPLACE); public_ticket_command_is_valid, accept_operation rewritten whole; draft.released_authoring_version (backfilled; CHECK draft_release_names_its_version: set iff Released, 1..authoring_version; UPDATE to boundary owner); release_draft_fenced rewritten (writes released_authoring_version); new update_draft_fenced (same signature/fence, requires Released, writes the column; boundary owner, EXECUTE ticket service); revise_draft rewritten, signature unchanged; ticket_projection.revision bigint NOT NULL DEFAULT 1 CHECK ≥1 (SELECT api, UPDATE ticket service); submit_task_completion rewritten (reads latest TicketCreated|TicketUpdated by entry seq); ticket_definition UPDATE(definition, digest) to ticket service.
- Journal: TicketUpdated {ticket, revision > 1, definition (released_ticket_is_valid), definition.id = ticket}. Command: UpdateTicket {ticket, expectedRevision ≥ 1, definition as CreateTicket's; admits id ≠ ticket (decide's TicketIdentityMismatch)}.
- Envelope `{version:1, command:"UpdateTicket", ticket, expectedRevision, authoringVersion, configurationRevision}` (const updateEnvelope): public_ticket_command_is_valid arm (numbers ≥1, revision string 1–256); Decide exclusion NOT IN ('CreateTicket','UpdateTicket'); accept_operation tag UpdateTicket, priority/admission Ordinary, draft-revision existence check; authority CHECK unchanged (ELSE true, as ReleaseDraft). B calls update_draft_fenced at decide; pending-ness left to decide (TicketNotPending).
- revise_draft order: NotFound; NotDraft unless Draft or (Released and projection phase Pending; no projection → NotDraft); Stale; Released only: DependenciesLocked if dependencies not an array or differ as a set from the released version's draft_revision; ConfigurationNotFound, RepositoryNotBound, LandingUnbranched. Success returns Revised with the draft's own state. No CHECK enumerates draft-door results; the TS DraftRevised type is B's.
- Populated upgrade test: 015 db with 8c-1 journal, operations, refusals, drafts, projection → 016; journal rows re-inserted through the trigger (rolled back), UPDATE x=x rechecks CHECKs, commands still valid, backfills right.
- Render-diff 001–015 byte-identical prefix. 37 named mutations all red (scratchpad/redproof.py).
- check-postgres rc=1: only authoring.test.ts "a released ticket's brief no longer moves, which is what lets a retry read it" (expects NotDraft on revising a Pending released draft) — B's to restate, and see gap 1.
- GOAL missed: (1) draft_brief is not versioned — an unreleased revision leaks into what dispatch reads; (2) submit_task_completion read only TicketCreated's definition (fixed in 016); (3) ticket_definition was insert-only (UPDATE granted; B upserts in the update transaction); (4) envelope and command share the name UpdateTicket (harmless).
