# F3 — round 2 fix for PR 8c-2 (branch `model/ticket-update`, tip a173815d)

Setup: `tasks/8c-2/_setup.md` (part of this brief; commit from a173815d, push `HEAD:refs/heads/model/ticket-update-f3`, never to the branch itself). Read `pr8/reviews/8c-2-ledger.md` (round 2 line) and GOAL.md decision 11: a ticket runs, and its reads show, what was released.

Finding: the ticket page's ledger grouping (`ui/chuggy-ui/app/core/ticketPageFacts.ts:84-90` → `core/ticketLedger.ts` `stageRowsOf`/`programStageRow`/`stageExpected`/`cycleComplete`) and the provenance "evaluation stages" (`browser/ticket/TicketProvenance.tsx:151-157`) read the draft's evaluation program. A Pending ticket's draft can be revised (program changed, 1→2 stages) without an update, so the page draws a stage that will never run, "of 2" labels, or a stage short of its roster.

Fix: draw the program from what was released. 016 already keeps it (the released authoring at `authoring_version = released_authoring_version`; see the join near 016:475, and `ticket_definition`). Carry the released program on the single-ticket read (column-scoped grants, as a173815d did for configuration_revision; 016 is unreleased so edit it in place), and have the ledger and "evaluation stages" use it. Add a ticketPageLedger case where the draft's program is ahead of what ran; red-prove it against the old source. Scan the rest of the ticket page for any other draft-sourced fact presented as what the ticket runs, and fix it the same way or name it in your report.

Run at the end, one at a time: check-source (test/rig playwright baseline), check-postgres, check-queries, check-console, check-console-sheets, check-boundaries, check-paths, check-comments, check-figures, check-duplication. Final message: commits, red-proofs, gate exits, under 30 lines.
