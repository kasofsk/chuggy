# F2 — round 1 fixes for PR 8c-2 (branch `model/ticket-update`, tip a26bf034)

Setup: `tasks/8c-2/_setup.md` (part of this brief; commit on `model/ticket-update` from a26bf034, push `HEAD:refs/heads/model/ticket-update-f2`, never to the branch itself). Read `pr8/reviews/8c-2-ledger.md` and `pr8/GOAL.md` §"PR 8c-2 — decisions" (decision 11: a ticket runs, and its reads show, what was released).

Fix three findings, each with a test that is red without the fix (red-prove it and say how):

1. **TS refusal order of `decideUpdate` is unguarded** (`src/domain/deciders.ts:254-270`). Moving the IdentityMismatch check below RevisionStale survives every suite. Add a TypeScript table test mirroring the model's `updateRefusalOrderTest` (multi-fault commands: wrong id + stale + deps changed → IdentityMismatch; stale + deps changed → RevisionStale; not pending + everything → NotPending), in the style of the existing domain tests.
2. **Ticket page shows the draft, not what the ticket runs.** `ui/chuggy-ui/app/browser/ticket/TicketMain.tsx:44-45` (`TicketBrief state={draftState}`), `TicketProvenance.tsx` (~95-117, draft.brief and draft.configurationRevision labelled "released under"), `TicketPage.tsx:177` (head intent from draft). Draw brief and intent from the ticket's released brief (`ticket.brief` on `ticketResponseSchema`) and "released under" from the ticket's own pin; if the wire lacks the released pin, add it from `ticket_projection`/`ticket_definition` (API grants stay column-scoped). Keep the "version N holds unreleased changes" line. Console tests: a ticket whose draft is ahead shows the released brief.
3. **Update re-pin unchecked at the durable tier.** Mutations surviving: removing `await decisionRepin(...)` (`src/adapters/postgres/decision.ts:733`); narrowing `src/adapters/postgres/journal.ts:208` to `TicketCreated`. Add one postgres case: release at config revision A, update at revision B, assert the projection's pin and the dispatch's `execution_request` pin/digest are B, and that after a writer restart the rebuilt dispatch contract is still B. Both mutations must turn it red.

Run at the end, one at a time: check-source (test/rig playwright is baseline), check-boundaries, check-queries, check-postgres, check-console, check-console-sheets, check-conformance, check-figures, check-comments, check-paths, check-duplication. Final message: the commits, the red-proofs, and the gate exits, under 40 lines.
