# C report (8c-2) — filed by the orchestrator
Tip e8952dba (afb5d9ea fixtures gain revision; 2226fa39 Edit offer, edit flow, provenance; e8952dba shared test/answeringApi.ts). Edit link only for Pending (`ticketRevisable` checked against `revisablesIn`), route /$tenant/$project/tickets/$ticket/edit; form prefilled from the draft's current revision; dependencies shown fixed; submit = PUT draft (expectedVersion) then UpdateTicket{ticket, expectedRevision: ticket.revision, authoringVersion, configurationRevision}, followed like a release. Refusals: DependenciesLocked, DraftNotEditable, DraftChanged (Stale, re-read), update refusals via operationRefusalSentence + "the draft holds this revision at version N, not released". Provenance: "revision 2, from draft version 2"; "nothing unreleased" / "version 3 holds unreleased changes" / "not recorded which version is live". Notes: brief panel still reads the draft; edit form re-pins to the latest ready configuration; TicketHead doesn't show revision; check-comments' awk misreads JSX `</` (offer moved to its own file). Gates: console 0, sheets 0, figures 0, comments 0; source/paths/duplication reds all F's (fixed at ea337531).
