# Task S (PR 8a) — migration 013: the released definition on the journal, its material and its sources in two tables

Worktree `~/claude/chuggy-wt/released-schema`, branch `schema/released-ticket` off `origin/main` (PR 7b merged; confirm `git log -1 origin/main` is aaaff1ec "Merge pull request #731"). Create it yourself:

    git -C ~/claude/chuggy fetch -q origin && git -C ~/claude/chuggy worktree add -b schema/released-ticket ~/claude/chuggy-wt/released-schema origin/main
    ln -s ~/claude/chuggy/node_modules ~/claude/chuggy-wt/released-schema/node_modules

Never `npm ci` under `ui/`. Standing prefixes: Node 24 on PATH, `TMPDIR=/tmp`, `FORCE_COLOR=0 NO_COLOR=1`. Use your own postgres: `docker run -d --name chuggy-check-postgres-8a -e POSTGRES_PASSWORD=chuggy-check -p 55436:5432 postgres:18-alpine` and `CHUG_PG_URL` on 55436 (memory `~/.claude/projects/-home-geoff-claude-chuggy/memory/chuggy-false-reds.md` §postgres says why); remove it when done. Read first: `~/claude/chuggy-effort/ticket-language/pr8/GOAL.md` §"PR 8a — decisions" (5, 8 and 9 are yours; 1–4 say what the rows carry), `pr8/survey.md` §3, §5, §6 and surprises 8, 11, 13, the package's `~/claude/chuggy-effort/ticket-language/package/model/ticket-domain/ticket.qnt` lines 1–110 and `model/task-contract/task.qnt` (`TaskDefinition`, `TaskObligation`, `ValidatedTaskResult`), `pr7/tasks/7b/S.md`, `S-report.md` and `B-report.md` §Addendum (012's shape, its red-proofs, and how the door builds a report from rows), `src/adapters/postgres/schema/migrations/{010-task-identity,011-evaluator-keys,012-task-report}.ts` whole, `src/adapters/postgres/schema/shared.ts` (`digestFoldFunction`), `deploy/rig/wipe-tickets.sql` (the two new tables join its list), the memories `migrations-render-literals.md` and `migrations-edited-in-place.md`, `CLAUDE.md`, `.chug/tasks/review-change.md`.

## Scope

`src/adapters/postgres/schema/migrations/013-*.ts` and `index.ts`, `test/postgres/migration.test.ts`, `test/postgres/nativeReads.test.ts` and any fixture that spells a `CreateTicket`, `deploy/rig/wipe-tickets.sql`, `baseline/privileges.ts` only if a grant is needed for the new tables (the roles that write and read `execution` write and read these), the schema README if it describes the journal's events or lists tables.

- Guard as 008–012: refuse while any `journal_entry` row exists, naming `deploy/rig/wipe-tickets.sql`; the header says why (the `CreateTicket` row carries the released definition; the dispatch carries its source; a produced report carries its obligation).
- **The event spelling is the generated codec's.** A runs in parallel and its report is not yet written; spell the arms from the package's types in the codec's `{"type","value"}` idiom as 012 did, in consts at the top so a rename lands in one place, and expect B to align one or two names when A's mirror lands: `CreateTicket.value` = `{id, content, dependencies: [..], workConfiguration: {workload, inputs, executionRequirements, resultContract}, evaluationPlan: {stages: [{key, evaluators: [{key, task: {…four…}}]}]}, finalizationConfiguration}`; `Dispatch.value` = `{ticket, source}`; `TaskDone.value.report` = `WorkResultReport{result: {obligation: {task: <TaskIdentity as 010 spells it>, definition: {…four…}, contextRef}, resultRef}, acceptedSourceRef}` or `EvaluationResultReport{result: <the same>, verdict}` or `TerminalFailureReport{evidence, kind}` (012's, unchanged).
- `decision_event_is_valid` replaced whole per decision 9: every ref a positive integer (`IS DISTINCT FROM` shape tests so an absent key is refused, 012's precedent), stages positional and evaluators unique within the bound as 011/012 state them, 012's three dead `CreateTicket` conditions dropped, `deps`/`prog` refused (a row in the old spelling is refused), `Dispatch` with a non-positive or absent `source` refused, a produced report without an obligation or with a `result` that is a bare integer refused.
- DDL (decision 5): `ticket_definition (tenant, project, ticket, definition jsonb, digest text)` primary key `(tenant, project, ticket)`, FK to `ticket_projection`'s key if the baseline's tables carry such FKs (follow their pattern), a bounded-size CHECK on the JSON and a non-empty digest; `ticket_source (tenant, project, ticket, source bigint, repository text, commit text, ref text)` primary key `(tenant, project, ticket, source)`, CHECK `source >= 1`, `(repository IS NULL) = (commit IS NULL)`. Grants as the tables beside them. `operation_completion_authority_is_its_boundary` re-rendered without `'ExecutionBlocked'` (survey 13).
- `submit_task_completion` rewritten whole (decision 8; `CREATE OR REPLACE`, signature unchanged): the journaled report carries `result.obligation` built under the lock — `task` from `execution_request_task` as 012 builds the identity, `definition` read from the ticket's `CreateTicket` journal row (`journal_entry_release_ticket`; the work definition for a work task, the evaluator's `task` for an evaluation task by stage key and evaluator key), `contextRef` = the cycle for a work task and, for an evaluator, what the copy's `currentTaskObligations` builds (read `model/ticket-domain/evaluation/evaluation.qnt` in the package; if it is not derivable from the row, say so and use the cycle), `resultRef` = today's `result`; `acceptedSourceRef` from `execution_result_source` for a passed work manifest, writing the `ticket_source` row in the same statement; a passed work manifest with no source row on a ticket whose `ticket_source` has a repository is refused (name the outcome; `BindingMismatch`'s shape). `TerminalFailureReport` path as 012.
- Landed migrations are not edited. Render-diff of 001–012 main vs branch empty (say which script).
- `test/postgres/migration.test.ts`: fresh install records 013; the guard case; a `CreateTicket` table (one admitted row, and one row each for: `deps`/`prog` spelling, a zero ref, an absent `workConfiguration`, an evaluator without `task`, a non-positional stage, a duplicate evaluator key); `Dispatch` admitted/refused; a produced-report table (work with obligation admitted, evaluation admitted, bare-integer `result` refused, absent `acceptedSourceRef` refused); the two tables' CHECKs; `submit_task_completion` journals a work pass with its obligation and accepted source from seeded rows (and the `ticket_source` row appears), an evaluator pass with the evaluator's definition, and refuses the no-source case. Red-proof each new case against a fresh prepare (memory `chuggy-false-reds` §Migration red-proofs); name the mutation in the report.

Single-suite recipe: `node --experimental-strip-types .chug/tasks/postgres-databases.ts prepare postgres://postgres:chuggy-check@127.0.0.1:55436/postgres <db>`, then `CHUG_PG_URL=postgres://postgres:chuggy-check@127.0.0.1:55436/<db> node --experimental-strip-types --test --test-name-pattern='…' test/postgres/migration.test.ts`; drop the database after. Full `check-postgres` on your tip will be red where TypeScript still journals `deps`/`prog` or a bare `result` — B's; report which suites.

## Commits

On `schema/released-ticket`, in the tree's voice, ending `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.

## Report

`~/claude/chuggy-effort/ticket-language/pr8/tasks/8a/S-report.md`: tip, 013's shape, the spellings the arms admit, the two tables' columns and grants, how the door builds the obligation and what it reads it from, the render-diff result, the red-proofs, which postgres suites are red and why they are B's, anything GOAL.md got wrong. Under ~50 lines. Write the report, reply with its contents, and stop.
