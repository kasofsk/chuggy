# Task B (PR 8a) — the release resolves and stores, dispatch reads, the writer observes first, the door's report crosses the boundary

Worktree `~/claude/chuggy-wt/released`, branch `model/released-ticket`, which carries Task A (copy verbatim, model, goldens, generated, domain, actor) merged with Task S (migration 013). `node_modules` is a symlink to the root's; never `npm ci` under `ui/`. Standing prefixes: Node 24 on PATH, `TMPDIR=/tmp`, `FORCE_COLOR=0 NO_COLOR=1`. Use your own postgres: `docker run -d --name chuggy-check-postgres-8b -e POSTGRES_PASSWORD=chuggy-check -p 55437:5432 postgres:18-alpine` and `CHUG_PG_URL` on 55437; remove it when done. Read, in order: `~/claude/chuggy-effort/ticket-language/pr8/GOAL.md` §"PR 8a — decisions" (4–8 are yours; 1–3 say what the event carries), `pr8/survey.md` §3, §5, §6 and surprises 3, 5, 8, 11, 12, `pr8/tasks/8a/A-report.md` and `S-report.md` (what landed, by file:line; the codec's spellings; the sites S names as yours; and the two things they do not agree on, settled below), `pr7/tasks/7b/B.md` and `B-report.md` (this task's shape in 7b, and how the door's report reaches the wire), `CLAUDE.md`, `.chug/tasks/review-change.md`.

## Scope

`src/interpreter/`, `src/adapters/` (postgres writes and reads, scheduler, kubernetes placement, http contract), `src/contract/`, `test/contract/{representations.ts, contractDocument.json}`, and the tests of all of them under `test/`; and 013 (unlanded, S's) for what the settled points below change in it.

- **The release resolves (decision 4, 6).** `releaseTicketEvent(ticket, authoring, materials)` pure in `src/interpreter/authoring.ts`, building the whole `ReleasedTicket`; `readiness.ts`'s `ReleaseDraft` arm supplies the materials from the draft, the configuration revision the command names and the repository's landing. Name the material behind each of the four `TaskDefinition` refs and `content`/`finalizationConfiguration` in the report; where a named one has no digest today, compute one canonically and store it in `ticket_definition.definition`. Every evaluator of a stage gets the `Evaluation:<stage key>` definition. `encodeDraftAuthoring`/`parseDraftAuthoring` move to `authoringSchema`'s JSON (decision 6); `draft_revision.authoring` is no longer a journal event.
- **The materialization is stored (decision 5).** The release transaction (`decision.ts:511-577`) writes `ticket_definition`; `decisionExecution` (`decision.ts:321-334`) copies `execution.requirement_*` from it instead of calling `materializeExecutionRequirement`, which keeps one caller (the release). The pod's `TargetCommit` reference (`decisionPlan.ts:295`, `inputBundleReferencesOf`) comes from `ticket_source` at the ticket's current `source`.
- **The writer observes before it decides (decision 7).** For a `Dispatch` command alone: observe at the ticket the command names, fill `source` = the fold of the observed commit (a repository-less ticket gets the reserved reference of decision 4, and its `ticket_source` row carries NULLs), write the `ticket_source` row in the settle, refuse `ExecutionSourceUnreadable`/`ExecutionSourceDenied` with no journal row. Delete `projectWriterExecutionSource`'s speculative re-decide and the `EscalateEvaluationFailure` park for an unreadable rework source (task E's, `pr7/tasks/7b/E-report.md` §1b, and its `test/postgres/finalizerRework.test.ts` case becomes: a rework runs at the accepted source with no observation). `executionSourceHistory.ts` and every other reader of a per-spawn observation follow.
- **The door (decision 8, S's 013)** is S's; you own two edits to it: the evaluator's `contextRef` (settled below) and any spelling A's codec moved. The scheduler's completion path gains an arm for S's `SourceUnrecorded` outcome (S-report §"What GOAL.md got wrong"; today it would land at `schedulerCompletion.ts:379` as `Conflicting` + `ImpossibleState`) — it is a terminal refusal the incident should name, not an impossible state.
- **Readers.** `nativeReads.ts:447,492,540` read `->'value'->'ticket'` and `->'value'->'deps'` off the release row: now `id` and `dependencies`. Fixtures `nativeReads.test.ts:105,326,363`, `ticketInstants.test.ts:229` and every fixture that journals a `CreateTicket` or a bare-integer `result` follow (S lists fifteen postgres cases in five suites; `test/actor/harness.ts`'s history is A's if A moved it, else yours).
- Contract: no wire change is expected (decision 4). `ticketResponseSchema`, `draftResponseSchema`, `authoringSchema` stay; if a schema must move, say why. The contract document follows only if one does.
- Tests: the release stores a definition and its digest, and a second release under another revision stores a different one; dispatch copies the requirement from the stored definition (red-proof: re-materialize under a changed platform default and watch the copy differ); the writer refuses an unreadable source before any journal row exists; a repository-less ticket dispatches with the reserved source; a rework and a resume spawn at the accepted source with no observation; `i3`/`ticketServiceRun`/`projection` for a work pass whose accepted source reaches the instance, its evaluators' `contextRef` equal to the work result's `resultRef`, and a finalization at the accepted source.
- Comments: nothing says a source observed at a spawn, a requirement materialized at dispatch, or a draft holding an event.

NOT yours: `model/`, `src/domain/`, `src/actor/`, `ui/`. If a compile forces an edit there, make the smallest one and list it.

## Addendum (orchestrator, after A and S)

The branch tip is 36a9df7a: A's 59cea038 merged with S's 3c10ad8f, cleanly. A's report §"The generated spellings" is the codec (`id`, `dependencies` as a Set on the domain side and a distinct array on the wire twin, `WorkResultReport.value = {result: {obligation: {task, definition, contextRef}, resultRef}, acceptedSourceRef}`); S's 013 already spells the same shape, so align only what `npx tsc --noEmit` and the migration suite disagree on. A's §"What B must change" is your site list (every typecheck red is yours; `ReleaseAuthoring`/`releaseAuthoringOf` are gone for `releaseTicketEvent(definition)`/`releasedTicketOf(event)`, `plainDefinitionOf`/`flatPlan` in the actor harness, `dispatchEvent`/`decideDispatch` take a source). S's §"What GOAL.md and the brief got wrong" is the rest of it (the `id` index, the door's `journal_entry` grant, `SourceUnrecorded`'s missing arm, `nativeReads.ts` reads). A's middle commits bypassed the hook because the tree does not compile until you land; yours must not.

## Settled between A and S (orchestrator)

1. **An evaluator's `contextRef` is the accepted work result's `resultRef`** (package `evaluation.qnt:336`; A begins the instance with `workResult` = the accepted `result.resultRef`). S used the cycle. Edit 013's door: for an evaluation task, `contextRef` = `result_digest_fold` of the cycle's accepted work manifest digest — the work task `(ticket, cycle)`'s `execution_result` row, read under the same lock; refuse (S's `BindingMismatch` shape, named) when no such row exists. Add the migration-suite rows and red-proof them (S's single-suite recipe, fresh prepare).
2. **The release payload's ticket key is `id`** (S indexed `journal_entry_release_ticket` over it; A's codec spells it so).

## Gates on the tip

`check-source`, `check-boundaries`, `check-queries`, `check-postgres`, `check-conformance`, `check-random`, `check-model-api` (only if you touched `src/generated`, which you should not need to), `check-figures`, `check-comments`, `check-paths`, `check-duplication`. Report each exit.

## Commits

On `model/released-ticket`, small, in the tree's voice, ending `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.

## Report

`~/claude/chuggy-effort/ticket-language/pr8/tasks/8a/B-report.md`: tip, what changed per layer, the material behind each ref (a table), what 013 gained, whether the wire moved, files outside your layers touched, gates on the tip, anything GOAL.md or the A/S reports got wrong. Under ~60 lines. Write the report, reply with its contents, and stop.
