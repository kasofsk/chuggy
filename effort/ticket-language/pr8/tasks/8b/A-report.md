# Task A (PR 8b) — report (filed by the orchestrator from the builder's hand-back)

Tip 2acfe7a7 on `model/ticket-events` (7 commits on 02bd9572: 56bbda47, b04dae3c, fe132eb1, 1b09f45d, 072d79c5, 11289e33, 2acfe7a7). Not pushed; no --no-verify.

Gates: check-model 0 (122 tests, 0 failures), check-conformance 0, check-random 0 (shrink seed re-pinned 10975 → 1, mutant moved), check-model-api 0, check-figures 0, check-comments 0, check-paths 0, check-duplication 0, check-source 1 (reds only in src/interpreter, src/adapters and tests importing them: test/{interpreter,adapters,roots,postgres,rig}/*, test/ui/ticketActions.test.ts). Shell suites check-random 16/16, check-conformance 9/9, emit-goldens 14/14.

Per layer: model — TicketEvent (17), Obligation, Entry {seq, event}, LastDecision (NoDecision | Decided); StepRecord/Transition/Decision, Ticket.tasks and .artifact gone (artifact derived: artifactOf); reduce and reducibleWorkIn gone; invariants decisionsValid, eventsNeverIdentity, finalizationGenerationHeld; replay = evolve folded. Goldens 12 re-emitted aimed at lastEvent + a 13th `revoke-cancels-tasks`. Domain — evolve.ts, decisionValid.ts; effect.ts, taskSet.ts deleted; equality.ts moved src/actor → src/domain (eventsNeverIdentity needs graph equality). Actor — decide(graph, event, policy) replaces execDecisionEvent; semantics 7; src/actor/obligations.ts type renamed RefinementObligation.

Spelling ({type, value}): TicketCreated(ReleasedTicket); TicketDispatched{ticket, source}; TicketRevoked(int); TicketWorkResumed/EvaluationResumed/FinalizationResumed(int); TicketWorkResultAccepted{ticket, result, acceptedSourceRef}; TicketWorkProcessFailed/WorkExecutionUnavailable{ticket, task, evidence}; TicketEvaluationProgressed/Passed/Blocked{ticket, report}; TicketEvaluationReworkStarted/FailureEscalated{ticket, report, evidence: [{evaluator, resultRef}]}; TicketFinalizationSucceeded/NeedsWork/Unavailable{ticket, workCycle, generation, evidence}. Obligation: ExecuteTask{ticket, task: {task, definition, contextRef}}; FinalizeTicket{ticket, finalization: {workCycle, generation, input, source}, configuration}; CancelTask{ticket, task}.

Never-identity: `eventsNeverIdentity` in model/domain.qnt (in allInvariants), checked by check-model's random run, witness + refinement suites, the TS bundle on every golden state, check-random; sampled 5000×40 mc_chuggy, 1000×30 refinement clean. Unit: eventsNotOwedAreIdentityTest, mirrored in test/domain/deciders.test.ts.

B's sites: projectWriter.ts :59 (graphEquals from src/domain/equality.ts), :63/:246/:338/:589 (decide + evolve), :65 (workReduceEvent gone), :75 (effect.ts gone), :224 (storedJournalLegalOn takes no config), :341 (row holds decision.event), :412 (taskDoneEvent no onFailure; reworkDisposition is the policy), :590 (old label); decisionPlan.ts :23/:36, :58/:158/:241/:375/:417 (entry.event, decision.obligations), :127/:411/:430 (TicketEvent tags); ticketCommand.ts:123 (WorkReduce gone); postgres/decision.ts :273 (eventTicket from src/domain/evolve.ts), :478 (obligations), :657 (TicketCreated); postgres/journal.ts :210 (TicketCreated), :429 (drop config); readiness.ts:381 (finalizationResultEvent takes evidence). Runtime-only: TerminalFailureReport on the wire is {failure: {task, evidence}, kind}; Ticket drops artifact/tasks, gains finalizationGeneration.

Outside layers: .chug/tasks/check-random.test.sh (new mutant: a revoke that decides a completion; red only on eventsNeverIdentity), check-conformance.test.sh (tamper alters a decision), emit-goldens.test.sh (aim uses lastEvent), check-conformance.sh (header wording).

GOAL.md corrections: (1) TerminalFailureReport takes the package's `failure: TaskFailure` — an event carrying the report must name its task; (2) the FinalizationResult command gains `evidence`; (3) Ticket gains finalizationGeneration; (4) a 13th golden (no walk revoked a ticket owing CancelTask); (5) decision 3's legality is weaker than enablement — e.g. a TicketDispatched row for a ticket whose dependencies are not Done passes, because evolve moves it; sound for decided rows, does not re-check guards; (6) lastStep is a sum since the initial state records no decision.
