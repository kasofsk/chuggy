# Task S (PR 8b) — report (filed by the orchestrator from the builder's hand-back)

Tip 727b62d2 on `schema/ticket-events` (one commit on 02bd9572), hook clean. Merged into `model/ticket-events` as 469425c8 (clean).

014: guard as 008. `journal_entry.entry` had NO validator before; now BEFORE INSERT trigger `journal_entry_carries_an_event` → `journal_entry_is_an_event()` (SECURITY DEFINER, boundary owner): entry exactly `{seq, event}`, seq equals the row's, `decision_event_is_valid(entry->'event')`. Trigger not CHECK so tamper tests (UPDATE entry) still run. `decision_event_is_valid` rebuilt over A's 17 arms (2acfe7a7 codec); helpers `released_ticket_is_valid` (013's walk), `task_report_is_valid`, `task_identity_is_valid`, `validated_task_result_is_valid`; boundary-owned, PUBLIC revoked. Command validator `decision_command_is_valid` = 013 grammar + failure.task equals the command's task + FinalizationResult needs positive evidence; `public_ticket_command_is_valid` calls it (release exclusion kept); `ticket_command_is_valid` unchanged. Dropped: `project_continuation` (+ FK to journal_entry, grants), `publish_continuation`, `decision_input_continuation_head`, `Continuation` from decision_input CHECKs, `Stale` state (only a continuation reached it). Added `decision_input.deferred_passes` (CHECK >= 0), `deferred_since`, ticket-service UPDATE on both. Re-rendered `journal_entry_release_ticket` over `TicketCreated`/`id`; `submit_task_completion` whole (reads TicketCreated; failure `{task, evidence}`). Unchanged by check: `operation_completion_authority_is_its_boundary`, execution_request unique + kind CHECK.

native_action: unique `native_action_decision_opens_one_desk (tenant, project, authorizing_seq) WHERE attempt IS NULL` replaces the old; `effect_position` kept — `request_finalization_approval` copies finalization_request.effect_position into approval rows (attempt NOT NULL) and `decision.ts:439` writes it for the desk (B's).

Wipe: `project_continuation` removed from the script. **The branch script fails against a database at 013** (FK from project_continuation to journal_entry, TRUNCATE without CASCADE); main's 02bd9572 script wipes cleanly. Release must wipe with the pre-8b script, then migrate.

Render-diff (8a's render.mjs, git archive 02bd9572 vs branch): one appended 014 hunk, 517 lines, nothing else. Red-proofs: 39 single mutations, all red (list in hand-back; `ticket_unfloored` survived round one until a finalization-fact-about-no-ticket row was added). migration.test 118 cases, 117 pass, 1 red is B's (release reads).

B's reds: everything reaching the writer (`readiness.ts:620`, `decision.ts:475` read project_continuation) — i3, finalizer (14), ticketProjection (5), nativeActionAdmits, harness suites; full check-postgres hit the 1800s cap. check-queries 3 findings (decision.ts:474/483, readiness.ts:620). nativeReads.ts matches ReleaseTicket/CreateTicket; nativeReads.test 4 fixtures with rec, then hang to 300s cap. Any TS writer journalling rec is refused by the trigger. privileges (38), finalizerPrivileges (14) clean. check-source red only in test/rig (`@playwright/test` absent from node_modules). duplication, comments, figures, paths, boundaries 0.

GOAL/brief wrong: accept_operation never named WorkReduce (its Dispatch branch dead since 013); decision 10's "pre-014 wipe loses nothing" is wrong (FK); the journal validator had no call site (created the trigger); the door moves more (release tag, failure shape); FinalizationResult evidence is not the SQL door's to give — `submit_finalization_result` writes a `SubmitFinalizationResult` envelope (request, requestGeneration, attempt, outcome, kind) and readiness.ts (~:468) builds `FinalizationResult`, so B chooses the evidence.
