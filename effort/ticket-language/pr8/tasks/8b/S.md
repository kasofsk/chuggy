# Task S (PR 8b) — migration 014: the journal validates events, the inbox validates commands, the continuation table goes

Branch `schema/ticket-events` off `origin/main` 02bd9572. Setup: `_setup.md` beside this file (read it first; it is part of this brief). Use your own postgres: `docker run -d --name chuggy-check-postgres-8b-s -e POSTGRES_PASSWORD=chuggy-check -p 55436:5432 postgres:18-alpine`, `CHUG_PG_URL` on 55436; remove the container when done.

Read first: `/Users/david/chuggy-effort/ticket-language/pr8/GOAL.md` §"PR 8b — decisions" (7, 9 and 10 are yours; 1–6 say what the rows carry), `pr8/survey.md` §2, §3, §6 and surprises 2, 9, 10, 11, 13, the package's `model/ticket-domain/ticket.qnt` lines 150–245 and `model/task-contract/task.qnt`, `pr8/tasks/8a/S.md` and `S-report.md` (013's shape and red-proofs), `src/adapters/postgres/schema/migrations/{010,011,012,013}-*.ts` whole, `baseline/{relations,constraints,privileges}.ts` for `project_continuation`, `native_action`, `decision_input` and `operation`, `deploy/rig/wipe-tickets.sql`, the notes `migrations-render-literals.md` and `migrations-edited-in-place.md` (see `_setup.md`), `CLAUDE.md`, `.chug/tasks/review-change.md`.

## Scope

`src/adapters/postgres/schema/migrations/014-*.ts` and `index.ts`, `test/postgres/migration.test.ts` and any postgres fixture that spells a journal row, `deploy/rig/wipe-tickets.sql`, `baseline/privileges.ts` only where a dropped object's grants must go, the schema README if it describes the journal's contents or lists tables.

- **Guard** as 008–013: refuse while any `journal_entry` row exists, naming `deploy/rig/wipe-tickets.sql`; the header says why (a row now carries an event, not a command and a record).
- **Two validators (decision 10).** A runs in parallel and its codec is not written yet: spell the arms from the package's types in the codec's `{"type","value"}` idiom, in consts at the top so a rename lands in one place, and expect B to align names when A lands. (a) `decision_event_is_valid` becomes the journal row's validator, rebuilt whole over the seventeen `TicketEvent` arms: every ref positive (`IS DISTINCT FROM` shape tests so an absent key is refused), `TicketCreated`'s `ReleasedTicket` checked as 013 checks `CreateTicket`, the two rework-fact arms with a non-empty `evidence` array of `{evaluator, resultRef}` positives, the three finalization facts with positive `workCycle`/`generation`/`evidence`; any `rec`, `StepRecord` or command tag refused. (b) A command validator for `operation.command`: 013's command arms minus `WorkReduce`, and `TaskDone` without `onFailure` (a row carrying either refused); `ticket_command_is_valid` and `public_ticket_command_is_valid` call it and keep their two authorization-by-shape exclusions (survey §2). Say which function checks the `journal_entry.entry` column and where it is called.
- **DDL.** `project_continuation` dropped with its functions, constraints and privileges (list them); the `Continuation` input kind leaves any CHECK naming it; `accept_operation`'s priority classification loses `WorkReduce`; `operation_completion_authority_is_its_boundary` checked against the surviving tags; `journal_entry_release_ticket` re-rendered over `TicketCreated`'s `id`. `native_action`: unique `(tenant, project, authorizing_seq) WHERE attempt IS NULL`; drop `effect_position` if no writer but the decision plan sets it, else keep it and say who writes it (grep `src/`). `execution_request`'s unique and `kind` CHECK are **unchanged** (decision 7). `decision_input.deferred_passes integer NOT NULL DEFAULT 0` (CHECK `>= 0`) and `deferred_since timestamptz` (decision 9).
- **The completion door.** `submit_task_completion` builds the command without `onFailure` (confirm it never set one; if it did, rewrite it whole as 013 did, signature unchanged) and still validates it with `ticket_command_is_valid`. Nothing else in it moves.
- **Wipe script.** `project_continuation` leaves `deploy/rig/wipe-tickets.sql` (decision 10); check it is not referenced by an FK the wipe order relies on, and say so.
- Landed migrations are not edited. Render-diff of 001–013 main vs branch empty (say which script; 8a's is at `pr8/scratch/S/render.mjs`).
- `test/postgres/migration.test.ts`: fresh install records 014; the guard; a journal-row table (one admitted row per event arm, and refused rows for: a `rec` field, a command-shaped `TaskDone`, a zero ref, an absent `evidence`, an empty `evidence`); a command table (`TaskDone` without `onFailure` admitted, with it refused, `WorkReduce` refused, the two authorization exclusions still refused); `project_continuation` absent; the `native_action` unique; `decision_input`'s two columns and their CHECK. Red-proof each new case against a fresh prepare; name each mutation in the report.

Single-suite recipe: `node --experimental-strip-types .chug/tasks/postgres-databases.ts prepare postgres://postgres:chuggy-check@127.0.0.1:55436/postgres <db>`, then `CHUG_PG_URL=postgres://postgres:chuggy-check@127.0.0.1:55436/<db> node --experimental-strip-types --test --test-name-pattern='…' test/postgres/migration.test.ts`; drop the database after. Full `check-postgres` on your tip will be red where TypeScript still journals `rec` or a `WorkReduce` — B's; report which suites.

## Report

Tip; 014's shape; the spellings each validator admits; objects dropped and grants removed; `native_action`'s outcome; the wipe script change; render-diff result; red-proofs; which postgres suites are red and why they are B's; anything GOAL.md got wrong. Under ~50 lines.
