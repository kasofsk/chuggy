# Mutation sweep — PR 8a "The released ticket" (branch `model/released-ticket`)

You did not write this change. Fresh detached worktree of your own at the tip named in `reviews/8a-ledger.md` under "Tip for sweep":

    git -C ~/claude/chuggy worktree add --detach ~/claude/chuggy-wt/released-sweep <tip>
    ln -s ~/claude/chuggy/node_modules ~/claude/chuggy-wt/released-sweep/node_modules
    ln -s ~/claude/chuggy/ui/chuggy-ui/node_modules ~/claude/chuggy-wt/released-sweep/ui/chuggy-ui/node_modules

Never `npm ci` under `ui/`. Standing prefixes: Node 24 on PATH, `TMPDIR=/tmp`, `FORCE_COLOR=0 NO_COLOR=1`. Your own postgres: `docker run -d --name chuggy-check-postgres-8s -e POSTGRES_PASSWORD=chuggy-check -p 55440:5432 postgres:18-alpine`, `CHUG_PG_URL` on 55440; remove it when done. Read: `~/claude/chuggy-effort/ticket-language/pr8/GOAL.md`, `pr8/tasks/8a/{A,S,B,D}-report.md`, `pr8/reviews/8a-round1-{machine,boundary}.md` and the fix reports the ledger names, `pr7/reviews/7b-sweep.md` (the shape and depth wanted), `~/.claude/projects/-home-geoff-claude-chuggy/memory/{scoped-iteration-gates,chuggy-false-reds,guards-fail-open}.md`, `.chug/tasks/review-change.md`.

Whole-branch mutation sweep over `git diff aaaff1ec..<tip>`: for every added or changed behaviour in the model, the domain, the actor, the interpreter, the adapters, 013, the contract and the fixtures, apply one plausible mutation (the work-pass arm keeping the dispatch source; the instance begun at the cycle instead of the report's reference; `reportMatchesTask` comparing identities only; a definition ref accepted at zero; two evaluators of a stage given different definitions at release; the release's fold reading the wrong material; the stored requirement ignored at dispatch; the writer observing after the decision or on a rework; the reserved source given to a repository ticket; `ticket_source` written without the fold; the door's evaluator `contextRef` taken from the cycle, or from another ticket's cycle; the `WorkResultUnrecorded`/`SourceUnrecorded` refusals deleted; `journal_entry_release_ticket` indexed at the old key; a CHECK or a grant dropped; a wipe-list table missing) and run the narrowest gate or suite that should catch it; record RED or SURVIVED with the mutation, the file:line and the case. Goldens' step bodies are the model's output: mutate the manifest and the draws, not the traces. Survivors are findings only when they name a behaviour defect or a proof gap a cheap case closes. Docs pass: every comment the diff touches reads true against the code beside it; both copies byte-identical to the package's; `model/AGENTS.md` names no divergence. Fabric alignment: nothing under `.chug/`, `images/`, `deploy/` or in `~/claude/chuggy-fabric` names `deps`, `prog`, `program`, a per-request requirement materialization, or a source observed at a spawn; the worker manifest and `submit_worker_result`'s sourceless overload are what the fabric's worker calls.

Verdict to `~/claude/chuggy-effort/ticket-language/pr8/reviews/8a-sweep.md`: APPROVE or CHANGES; the mutation table; findings with file:line; under ~120 lines. Remove your worktree and drop your container. Write the verdict, reply with the verdict, the counts and the findings only, and stop.
