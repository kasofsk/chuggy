# 8c-1 round 1, boundary half (4afc1244) — CHANGES (filed by the orchestrator from the reviewer's hand-back)

1. **Finalization request's cycle/generation unpinned.** src/interpreter/decisionPlan.ts:258-259 copies obligation.value.finalization.{workCycle,generation}; src/adapters/postgres/decision.ts:~393 writes 015's columns. Mutations `generation: 1,` and `workCycle: 1,` survive test/interpreter, test/roots, test/conformance (1329/1329) and dispatchWriter, i3, finalizerQueue, finalizer, finalizerClosure, finalizerUnavailable (88/88). dispatchWriter.test.ts:1228-1235 asserts only the name; i3.test.ts:667 name/position/requestGeneration; postgres fixtures insert the columns by hand. Fix: assert on the materialization at first mint (1,1) and after a finalization resume (1,2); one postgres case reading the minted row back (house rule 13).
2. **Writer stores a machine refusal the machine never returned.** src/interpreter/projectWriter.ts:439-455, :501: a finalizer result whose request is closed (recovery epoch superseded by restore, or Invalidated — readiness.ts:447-452, adapters/postgres/finalizer.ts:618) while the ticket still waits on the same (cycle, generation): decide accepts, writer settles FinalizationNotCurrent with exactly the awaited numbers; console says "…not the finalization the ticket is waiting on" — false. Contradicts 015 header :16. Native-action branch answers TicketChanged. Fix: a boundary code; keep FinalizationNotCurrent only when decide returned it.
3. Minor: src/interpreter/commandMap.ts:5-10 "nothing else spells a TicketCommand" — http/contract.ts:520,528 builds revoke/resume via constructors for public Decide. Narrow the sentence.

Holding: gates boundaries, queries, postgres, conformance, console, console-sheets, figures, comments, paths, duplication 0; check-source 1 (test/rig baseline). Former NotEnabled sites right. IntegrityContradiction (projectWriter.ts:359) would wedge the inbox (projectTicketWriterRun returns without settling) but is unreachable (reportValid ≡ task_report_is_valid; submit_task_completion raises first; Revoke/Resume always valid; evidence ≥1); nothing tests the equivalence — noted. 015 render-diff clean; command validator spot-checks; public exclusions (no tag main refused now admitted); refusal CHECKs; five mutations red, reverted. Wipe: 015 refuses at 014 with rows, applies after e78a93ce's script (which truncates decision_input and finalization_request). Wire/console right; ticketNumberSchema for cycle/generation matches commandValid >0. Revoked dependency: readiesIn keeps it out of candidates; revoke works.
Notes: TicketRevisionStale expected/current countSchema (allows 0) vs DB positive — unreachable until 8c-2.
