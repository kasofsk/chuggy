# 8c-1 round 1, machine half (4afc1244) — CHANGES (filed by the orchestrator from the reviewer's hand-back)

1. **Release-room refusal lost, unrecorded.** At e78a93ce `decisionEventEnabled`'s CreateTicket arm conjoined `canReleaseIn(config, graph, id)` (fleet < nTickets, id ≤ 2·nTickets) — chuggy's, not the package's; the writer answered NotEnabled past the deployed `domain_configuration.nTickets`. Now `commandValid` (src/domain/deciders.ts:160) and `decideCreate` (:205) carry no bound; `projectWriterDecision` (src/interpreter/projectWriter.ts:357) journals it. `ticketIdsWellFormed` (src/domain/invariants.ts:295-302; model/domain.qnt:1663) still claims the bound — the writer can journal a state violating it (model holds it only via `releasableIds` draws). Comments src/domain/enablement.ts:6-8 and model/domain.qnt:451 false for the writer. test/actor/journal.test.ts:253 pins acceptance. Red-proof: journalStep createTicketCommand j=1,2,3,9 with nTickets=2 all Decided; allInvariants false from j=3; at e78a93ce the third throws.
2. **`storedJournalLegalOn` (src/actor/journal.ts:82-99) does not check report.ticket = event.ticket.** `TicketEvaluationPassed{ticket:1, report:{…ticket:7}}` moves ticket 1 and passes. Red-proof by tampering walkToFirstJudgement + firstJudgement("EvaluatorPass"): journalLegalOn stayed true. 015 refuses on insert; the actor doesn't. Add `reportTicket(report) === eventTicket(event)` for the five report-bearing arms beside `eventTicketStands`. Lower severity (8b's accepted forged-history limit).

Holding: decider order and payloads line by line (swapping Create's first two checks reddens conformance); vanished conjuncts otherwise clean (dependableIn = package rule, recorded; dispatchSources/finalizationEvidences draw bounds; maxStages/nTasks real, answered Invalid); refusal as stutter (model + TS; mutating journalStep's refused arm reddens state.test/recovery.test); reports verbatim; goldens byte-for-byte, coverage red-proof; check-model 0 (124), conformance 0, random 0, model-api 0, figures 0, comments 0, paths 0, duplication 0; names grep two negative-test hits (model-api.test.ts:174, migration.test.ts:7710); update-only refusals declared unreachable in one sentence, coverage asserts none fires.

Notes: the corpus's only TaskNotCurrent is a stale work report on a Revoked ticket (catch-all arm); in-phase arms compared only by name in journal.test.ts and model refinement tests. decideWorkTerminal's EvaluationResultReport arm unreachable, comment says so.
