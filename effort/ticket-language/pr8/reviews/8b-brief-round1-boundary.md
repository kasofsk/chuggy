# Review round 1, boundary half — PR 8b "Events and obligations" (tip cf829352, branch `model/ticket-events`)

Setup: `_review-setup.md` beside this file (part of this brief). Use your own postgres: `docker run -d --name chuggy-check-postgres-8r -e POSTGRES_PASSWORD=chuggy-check -p 55438:5432 postgres:18-alpine`, `CHUG_PG_URL` on 55438; remove it when done. Your half: `src/interpreter`, `src/adapters`, `src/contract`, `src/roots`, `src/adapters/postgres/schema/migrations/014-ticket-events.ts`, `deploy/rig/wipe-tickets.sql`, `test/{contract,interpreter,postgres,adapters,roots,ui}`. The machine half is another reviewer's. Diff: `git diff 02bd9572..cf829352 -- <your paths>`.

Read first: `pr8/GOAL.md` §"PR 8b — decisions" and every 8b progress line, `pr8/survey.md` §2, §3, §6 and surprises 2, 9–12 and §Release coupling, `pr8/tasks/8b/{A,S,B}-report.md`, `pr7/reviews/7b-round1-machine.md` §1, the notes `stored-text-outside-the-journal`, `migrations-render-literals`, `guards-fail-open` (see setup), `.chug/tasks/review-change.md`, `CLAUDE.md`.

## What to check, each with a failure that actually happens

1. **One decision, one row, its consequences once.** The writer decides once, journals `{seq, event}` and materializes from obligations in the same transaction; a crash between decision and settle re-decides to the same event on retry (the inbox input is still pending) and cannot materialize twice (the uniques). Find a path that journals a row whose materialization is missing or doubled.
2. **Identities and grouping (decision 7).** A two-stage, two-evaluator ticket (the rig's shape): the work pass writes ONE `SpawnEvaluation` request with two `execution_request_task` rows, identity `"<seq>:<i>:ExecuteTask"` at the first `ExecuteTask`'s index, one bundle; the stage advance another; the door's `in_source_effect` binding still matches a completion to its request. Revoke with and without live tasks (B's departure 2: confirm nothing waited on the empty `CancelTicketWork`). The desk: `"<seq>:TicketEscalation"`, `effect_position` 0, the new unique; approval rows (`request_finalization_approval`) unaffected. Nothing parses an identity (grep `split(':')` and friends across `src/`, `images/`, `deploy/`).
3. **The continuation is gone and nothing waited on it.** `git grep -n -i continuation src test deploy` — each remaining hit justified; a work completion reaches Evaluation with no second inbox input; B's removal of the `Stale` settle: was there any other input `Stale` answered? The aging base kept at 3 — does the priority order among surviving classes match main's?
4. **The deferral bound (decision 9).** Reproduce B's red-proof; then: does a spent input still count against the project's mailbox depth or admission; is `deferred_passes` reset if the same operation later observes readable (it should not need to be, but say); is the refusal's evidence the last observation's; can a *durable* evidence on pass 5 still refuse at once. 7b round 1 §1's wedge: with a transient remote, does every other input of the project still get decided within the bound?
5. **014 whole.** Guard byte-identical to 008's; the trigger fires on INSERT only and the ticket service cannot bypass it (another insert path, `COPY`, a function running as owner?); `decision_event_is_valid` admits exactly the codec's spellings (compare `src/generated/model-api.ts`, not S's prose) — spot-check three arms by inserting what the TypeScript codec encodes; the command validator; dropped objects and grants (`\dp` on a fresh migrate); `Stale` dropped from `decision_input`'s states — no row or function still writes it. Render-diff 001–013 main vs branch empty (8a's script `pr8/scratch/S/render.mjs`). Re-prove four of S's red-proofs by mutation.
6. **The wipe (S's correction).** On a database at 013 with a journal and a continuation row, main's 02bd9572 `wipe-tickets.sql` then 014 succeeds; the branch script then 014 fails as S says; after 014 the branch script wipes cleanly. The release instruction in GOAL.md is the right one.
7. **Finalization evidence.** `digestFold(attempt_digest)` of the settled attempt, else `requestGeneration`: is the fallback ever a ref another fact of the same ticket could collide with in a way the model reads? Is `finalizationSettledOn` reading the attempt the submission settled, under the fence that authorizes it?
8. **The wire and the console.** `git diff 02bd9572..cf829352 -- src/contract test/contract` empty; `cd ui/chuggy-ui && npx tsc --noEmit && npx vitest run` green with no ui edit; the pod annotation / `CHUG_WORKER_TASK` carry the new identity and the worker image or fabric (`/Users/david/chuggy-fabric` if present, else say it was not checked) parses neither.
9. Comments and docs: true, in the tree's voice, no quantities, no stale path claims. Run `check-source`, `check-boundaries`, `check-queries`, `check-postgres`, `check-conformance`, `check-figures`, `check-comments`, `check-paths`, `check-duplication` at the tip, one at a time.

Verdict: APPROVE or CHANGES; each finding names file:line, the input and what goes wrong; under ~80 lines.
