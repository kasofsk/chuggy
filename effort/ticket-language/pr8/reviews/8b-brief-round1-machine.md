# Review round 1, machine half — PR 8b "Events and obligations" (tip cf829352, branch `model/ticket-events`)

Setup: `_review-setup.md` beside this file (part of this brief). Your half: `model/`, `test/golden`, `src/generated`, `src/domain`, `src/actor`, their tests, and `.chug/tasks/{check-random,check-conformance,emit-goldens}.test.sh`. The boundary half is another reviewer's. Diff: `git diff 02bd9572..cf829352 -- <your paths>`.

Read first: `pr8/GOAL.md` §"PR 8b — decisions" and every 8b progress line, `pr8/survey.md` §1, §3, §6 and surprises 1, 2, 9, 10, 12, `pr8/tasks/8b/{A,S,B}-report.md`, the package's `model/ticket-domain/ticket.qnt` 150–245, 335–432, 846–1172, 1285–1363, `.chug/tasks/review-change.md`, `CLAUDE.md`.

## What to check, each with a failure that actually happens

1. **`evolve` arm for arm against the package.** For each of the seventeen arms, the guard under which the package applies it and the state it produces, against chuggy's over its `Ticket` record. An arm that applies to a state the package's would leave alone (or the reverse) is a finding; name the input. Particular: `TicketWorkResultAccepted` compares the whole obligation; the four evaluation arms check `reportAdmissible` and the resulting instance state; the finalization arms check `finalizationCurrent` over cycle *and* generation (`finalizationGeneration` is new — confirm resume bumps it and nothing else does).
2. **`decide` against the package.** Each arm's event and obligations: a work pass is one `TicketWorkResultAccepted` with exactly the first stage's `ExecuteTask`s; a stage advance's `TicketEvaluationProgressed` owes the next stage's tasks and a same-stage one owes none; both failure edges by policy with `evidence` from `reworkEntries`; the three resumes by escalation variant; revoke's `CancelTask`s are exactly the live tasks. Mutate one arm's obligations and confirm `decisionsValid` or a golden reddens.
3. **The never-identity claim that replay rests on (decision 3).** Read `eventsNeverIdentity`'s statement and where it is checked; try to construct an enabled command whose event is identity (a completion that settles nothing, a resume of a ticket whose escalation carries no continuation, a revoke of an idle ticket). Then weigh the accepted limit (a `TicketDispatched` over incomplete dependencies is "legal" on replay): is there a row the writer could actually journal that the old check refused and the new admits? If yes, a finding.
4. **The reduce fold.** `reducibleWorkIn`, `decideWorkReduce`, `Ticket.tasks`, `.artifact` gone with no reader left; `artifactOf` derives what the reduce used to read, from the latest instance, and is right for a ticket that has passed work in cycle 2 after an evaluation-failure rework in cycle 1.
5. **Replay and legality in `src/actor/journal.ts`**: dense seqs, semantics 7, ticket-exists, not-identity; a tampered row (event of another ticket, a duplicated completion, a finalization fact at the wrong generation) is refused. Red-proof one by mutation.
6. **Goldens and walks**: re-emitted, not hand-edited (`emit-goldens` byte for byte, manifest current); the 13th `revoke-cancels-tasks` present; decision 11's shapes present; `check-random`'s new mutant is meaningful (a revoke deciding a completion) and the seed re-pin argued. `check-model`, `check-conformance`, `check-random`, `check-model-api` at the tip.
7. **`RefinementObligation`, `equality.ts`'s move to `src/domain`**: no domain → actor import; `graphEquals` covers the fields the model's `==` sees (`finalizationGeneration` included) — 8a round 1 found exactly this blindness in `*Equals`.
8. Comments and docs: true, in the tree's voice, no quantities, no stale path claims; `git grep -n 'StepRecord\|Transition\b\|rec\.\|effects\b\|WorkReduce\|reduce\b\|onFailure\|continuation' model src/domain src/actor src/generated` empty or each hit justified. `check-figures`, `check-comments`, `check-paths`, `check-duplication` at the tip.

Verdict: APPROVE or CHANGES; each finding names file:line, the input and what goes wrong; under ~80 lines.
