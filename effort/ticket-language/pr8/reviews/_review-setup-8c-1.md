## Setup (every 8c-1 review)

You did not write this change. You are launched in your own git worktree; Bash is pinned there. Detach it at the tip once: `git checkout --detach <tip>` and confirm `git log -1 --oneline`. `node_modules`: `ln -s /Users/david/chuggy/node_modules node_modules` (lockfile equals the tip's); never `npm ci` under `ui/` (if you need the console's, `ln -s /Users/david/chuggy/ui/chuggy-ui/node_modules ui/chuggy-ui/node_modules`). Every gate and test runs with `PATH="/opt/homebrew/opt/node@24/bin:/opt/homebrew/opt/coreutils/libexec/gnubin:$PATH" TMPDIR=/tmp FORCE_COLOR=0 NO_COLOR=1`; capture output to a file and echo the real exit; never pipe a gate through `tail`. **Environment baseline:** on this machine `check-source` is red on untouched `origin/main` too, typecheck and lint only, all in `test/rig/*` (`@playwright/test` absent) — not a finding. Effort dir `/Users/david/chuggy-effort/ticket-language/` (Read tool); package at `.../package/` (76c95a9). The previous orchestrator's notes: `git show origin/handoff/ticket-language-2026-09-22:memory/<name>.md`. Base is e78a93ce. Make no commits. Mutations you apply to red-proof must be reverted before you finish. Reply with your verdict as your final message; do not write into the effort dir.
