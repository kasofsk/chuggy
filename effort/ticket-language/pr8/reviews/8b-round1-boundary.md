# PR 8b round 1, boundary half — verdict (filed by the orchestrator from the reviewer's hand-back)

**CHANGES** at cf829352; runtime sound everywhere pushed. Gates: check-source 1 (playwright baseline only), boundaries/queries/figures/comments/paths/duplication/conformance/postgres 0; ui tsc 0, vitest 117 files / 1311 tests, no ui edit; contract diff empty.

Findings: (1, must fix) 014's evidence predicates unpinned — deleting the finalization-arms evidence predicate (:328-330) or the work-failure one (:296-298) leaves migration.test 118/118; add refused rows "finalization fact at no evidence", "work failure at no evidence". (2) 014 header :22-24 says the guard names the script that empties the journal — false at this revision (branch script fails on 013 via project_continuation FK; main's 02bd9572 script then 014 works; after 014 the branch script wipes); fix header, and say in wipe-tickets.sql's header that the pre-migration wipe is the previous release's script. (3) readiness.ts:39-41 "refuses on its next pass whatever its class" untrue if the remote recovered (then it journals). (4) stale continuation comments: test/postgres/nativeActionAdmits.test.ts:53, ticketInstants.test.ts:82. (5) deferred_since written (decision.ts:695), never read.

Sound: one decision one row (decide once, decisionValid, evolve, journal + materialize + settle in one txn; retry re-derives or AlreadyTerminal; obligationsOf/executeRequest throw on mixed tickets/phases); identities/grouping (i3.test.ts:354 one SpawnEvaluation "<seq>:0:ExecuteTask", bundle "<seq>:0:InputBundle"; in_source_effect matches; desk "<seq>:TicketEscalation" pos 0; approvals keep their unique; empty-revoke change safe — nothing live outside Work/Evaluation); no identity parsing in src/images/deploy (fabric not checked, absent locally); continuation removal clean, Stale only answered continuations, priority order Safety 0 / Completion 1 / Ordinary 3 as main; deferral red-proofs reproduced, durable evidence refuses at any count, wedge bounded; 014 guard byte-identical, trigger INSERT-only with no bypass, codec spot-check (8 real encoded arms admitted, bad shapes refused), validators' call sites, render-diff empty, four re-run mutations red; wipe as S said; finalization evidence opaque to the model, attempt read under the door's binding, always positive.
