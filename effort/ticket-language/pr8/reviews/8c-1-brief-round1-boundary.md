# Review round 1, boundary half — PR 8c-1 "Commands and refusals" (tip 4afc1244, branch `model/ticket-commands`)

Setup: `_review-setup-8c-1.md` beside this file (part of this brief). Use your own postgres: `docker run -d --name chuggy-check-postgres-8c-r -e POSTGRES_PASSWORD=chuggy-check -p 55442:5432 postgres:18-alpine`, `CHUG_PG_URL` on 55442; remove it when done. Your half: `src/interpreter`, `src/adapters`, `src/contract`, `src/roots`, `src/adapters/postgres/schema/migrations/015-ticket-commands.ts`, `ui/chuggy-ui`, `test/{contract,interpreter,postgres,adapters,roots,ui}`. The machine half is another reviewer's. Diff: `git diff e78a93ce..4afc1244 -- <your paths>`.

Read first: `pr8/GOAL.md` §"PR 8c — split", §"PR 8c-1 — decisions" and every 8c-1 progress line, `pr8/survey.md` §2, §7 and surprises 4–6, `pr8/tasks/8c-1/{A,S,B,C}-report.md`, the notes `stored-text-outside-the-journal`, `migrations-render-literals`, `guards-fail-open` (see setup), `.chug/tasks/review-change.md`, `CLAUDE.md`.

## What to check, each with a failure that actually happens

1. **A principal refused wrongly.** Every former `NotEnabled` site (B's list): does it now answer the refusal `decide` returned, and is each fallback right — a closed native action answering `TicketChanged`, a closed finalization request answering `FinalizationNotCurrent` built from the command (is that ever a lie: the request moved for another reason?), a release outside `commandValid` answering `ConfigurationInvalid`, any other command outside `commandValid` throwing `IntegrityContradiction` (what does the writer do then — does one malformed input wedge the project's inbox, 7b round 1 §1's shape?).
2. **Told nothing useful.** The stored refusal round-trips: writer → `decision_input.refusal` → API read → wire `refusal` → console sentence, with the numbers intact, for `DependenciesIncomplete` (two dependencies), `TaskNotCurrent`, `FinalizationNotCurrent`. `FinalizationNotCurrent`'s `workCycle`/`generation` use `ticketNumberSchema` on the wire — right, or a count? A boundary code carries no payload at every layer.
3. **The command map (decision 5).** `ticketCommandOf` is pure and total over the envelope; `grep` under `src/adapters` for a constructed command; the finalizer's `workCycle`/`generation` come from the request row B's 015 columns hold, written where the request is minted and consistent with the model's `finalizationGeneration` (a resume bumps it: does the new request row carry the bumped generation?).
4. **015 whole.** Guard (journal and operation) and header; `decision_command_is_valid` admits exactly what `src/generated/model-api.ts` encodes — insert three commands the TypeScript codec encodes and three 8b spellings; the public exclusions (a public `Decide` of `CreateTicket`, `ReportFinalizationResult`, `DispatchTicket`, `ReportTaskTerminal`: which are refused where, and is any now admitted that main refused?); `accept_operation`'s revoke priority unchanged in meaning; the authority CHECK; `submit_task_completion` rewritten whole with the report's `ticket`; `decision_input`'s code CHECK equals `allRefusalCodes` and the payload CHECK is exact; B's `finalization_request` columns. Render-diff 001–014 main vs branch empty (`pr8/scratch/S/render.mjs`). Re-prove four of S's red-proofs by mutation.
5. **The wipe.** On a database at 014 with a journal and operations, e78a93ce's `wipe-tickets.sql` then 015 succeeds; 015 over a non-empty `operation` refuses. GOAL.md's release instruction is right.
6. **The revoked dependency (accepted change).** A release naming a Revoked ticket is now journalled; the ticket stays Pending. Does anything at the boundary or in the console misbehave for such a ticket (dispatch candidates, the tickets page, a revoke of it)?
7. **The console.** Every code has a label and a sentence; `NotEnabled`/`CommandUnreadable` gone from `ui/`; sentences name the payload's numbers; `check-console`, `check-console-sheets` at the tip.
8. Comments and docs: true, in the tree's voice, no quantities, no stale path claims. Run `check-source`, `check-boundaries`, `check-queries`, `check-postgres`, `check-conformance`, `check-console`, `check-figures`, `check-comments`, `check-paths`, `check-duplication` at the tip, one at a time.

Verdict: APPROVE or CHANGES; each finding names file:line, the input and what goes wrong; under ~80 lines.
