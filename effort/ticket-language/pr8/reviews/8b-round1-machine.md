# PR 8b round 1, machine half — verdict (filed by the orchestrator from the reviewer's hand-back)

**CHANGES** at cf829352. Gates 0: check-model (122), check-conformance (13 goldens, 204 steps), check-random (2000), check-model-api, figures, comments, paths, duplication; the three shell suites. emit-goldens re-emits byte for byte. Red-proofs: M1 `decideRevoke` owing [] → conformance red at revoke-cancels-tasks state 7; M2 evolve's finalization guard ignoring generation → deciders.test.ts:775 red.

Clean: decide arm for arm vs the package; finalizationGeneration bumped only by resume (spawnWork 0, pass 1, `finalizationGenerationHeld`); artifactOf right for a cycle-2 pass after cycle-1 rework; reduce/tasks/artifact gone; graphEquals covers finalizationGeneration; no domain→actor import; grep hits justified; check-random mutant meaningful, seed re-pin reasonable; no enabled command with an identity event found (completion, resume, idle revoke tried).

Finding 1 (docs overclaim legality): `model/refinement.qnt:131-141` (theorem 1, "'moved' is exactly 'owed'") and the journalLegalOn doc above :354; `model/domain.qnt:1393-1395`; `src/domain/invariants.ts:238-241`; `src/actor/journal.ts:13-16`, :75 ("exact … proved"); `model/tests/chuggy_refinement_test.qnt:90-96`. eventsNeverIdentity gives decided ⇒ moved only; moved-but-undecidable rows: TicketDispatched over an incomplete dependency, TicketCreated with self/absent dependency (replay breaks depsAcyclic), ReworkStarted with evidence not reworkEntries. None writable (projectWriter.ts:459 checks enablement first) — no real row newly admitted. "Proved" overstates: checked by random runs and suites. Fix: say sound for decided rows, no guard re-checked, name the dispatch example.

Finding 2 (work-failure arms stricter than package, described as matching): `model/domain.qnt:813-822`, `src/domain/evolve.ts` onCurrentWork. A Work cycle-2 ticket given TicketWorkProcessFailed{task: WorkTask(cycle 1)}: package parks it, chuggy identity. Deliberate (pinned by eventsNotOwedAreIdentityTest and deciders.test "a work failure for a cycle the ticket has moved past"), but domain.qnt:774-779, evolve.ts header and decision 2 say "arm for arm the package's". Fix: keep the guard, name it at the arm as chuggy's one strengthening, so PR 9's adoption does not silently reopen stale failure rows.

Note (not blocking): decisionValid checks soundness not completeness (as the package); a revoke owing fewer CancelTasks passes decisionsValid; TS side goldens hold completeness, model side no gate would catch such a decide mutation.
