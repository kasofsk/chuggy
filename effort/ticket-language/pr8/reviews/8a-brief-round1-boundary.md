# Review round 1, boundary half — PR 8a "The released ticket" (branch `model/released-ticket`)

You did not write this change. Review it fresh in a detached worktree of your own at the tip named in `reviews/8a-ledger.md`:

    git -C ~/claude/chuggy worktree add --detach ~/claude/chuggy-wt/released-review-boundary <tip>
    ln -s ~/claude/chuggy/node_modules ~/claude/chuggy-wt/released-review-boundary/node_modules
    ln -s ~/claude/chuggy/ui/chuggy-ui/node_modules ~/claude/chuggy-wt/released-review-boundary/ui/chuggy-ui/node_modules

Never `npm ci` under `ui/`. Standing prefixes: Node 24 on PATH, `TMPDIR=/tmp`, `FORCE_COLOR=0 NO_COLOR=1`. Use your own postgres: `docker run -d --name chuggy-check-postgres-8r -e POSTGRES_PASSWORD=chuggy-check -p 55438:5432 postgres:18-alpine`, `CHUG_PG_URL` on 55438; remove it when done. Your half: `src/interpreter`, `src/adapters`, `src/contract`, `src/adapters/postgres/schema/migrations/013-released-ticket.ts`, `deploy/rig/wipe-tickets.sql`, `test/contract`, `test/interpreter`, `test/postgres`, `test/adapters`, and whether `ui/` still builds against the contract. The machine half (`model/`, `src/domain`, `src/actor`, goldens) is another reviewer's. Diff: `git diff aaaff1ec..<tip> -- <your paths>`.

Read first: `~/claude/chuggy-effort/ticket-language/pr8/GOAL.md` §"PR 8a — decisions" (4–9), `pr8/survey.md` §3, §5, §6, §7 and surprises 3, 5, 8, 11, 13 and §Release coupling, `pr8/tasks/8a/{A,S,B}-report.md`, `pr7/tasks/7b/E-report.md` §1b (the park this branch deletes), the memories `~/.claude/projects/-home-geoff-claude-chuggy/memory/{stored-text-outside-the-journal,migrations-render-literals,guards-fail-open}.md`, `.chug/tasks/review-change.md`, `CLAUDE.md`.

## What to check, each with a failure that actually happens

1. The release resolves and stores (decisions 4–6): release a draft under revision R and read back the journal's `CreateTicket` (every ref positive, `id` the ticket, every evaluator of a stage carrying the `Evaluation:<stage>` definition) and the `ticket_definition` row (its digest, and the material each ref names — B's table); release another draft under revision R' and confirm the refs and the material differ where the configuration differs and only there. A ref that is the same fold for two different materials is a finding.
2. Dispatch reads the store (decision 5): `execution.requirement_*` on a spawned row equals the stored definition's, and `materializeExecutionRequirement` has exactly one caller (the release). Mutate the store's requirement after release and confirm the next spawn carries the mutation, not a re-materialization.
3. The writer observes first (decision 7): a `Dispatch` on a ticket whose remote answers unreadable/denied is refused `ExecutionSourceUnreadable`/`ExecutionSourceDenied` with **no journal row and no projection change**; a readable one journals `Dispatch{ticket, source}` with the fold of the observed commit and a `ticket_source` row; a repository-less ticket dispatches with the reserved reference and a NULL-repository row; `projectWriterExecutionSource`'s speculative `execDecisionEvent` is gone; a rework, a resume and a finalization spawn with **no** `observe` call (spy the port) and the pod's `TargetCommit` reference is the accepted source's commit from `ticket_source`. Reproduce E's finalizer-rework case (`test/postgres/finalizerRework.test.ts`) on this branch: it must run at the accepted source, never park, never defer.
4. The door (decision 8, 013): `submit_task_completion` builds `result.obligation` from rows — identity from `execution_request_task`, definition from the journal's release row (work, or the evaluator's own stage and key), `contextRef` = the cycle for work and the fold of the cycle's accepted work manifest for an evaluator, `resultRef` as before, `acceptedSourceRef` from `execution_result_source` with the `ticket_source` row written under the lock; a passed work manifest with no source row on a repository ticket is refused with the named outcome and the scheduler has an arm for it (not `ImpossibleState`). Re-prove four of S's red-proofs and two of B's by mutation. `journal_entry_release_ticket` is indexed over `value->'id'` and the door's read uses it (`EXPLAIN`). Render-diff 001–012 main vs branch empty (say which script). `check-postgres` reaches the new arms through a scheduler suite, not only the migration suite's literals.
5. Stored text (memory `stored-text-outside-the-journal`): `draft_revision.authoring` is `authoringSchema`'s JSON and no longer an event; every reader of a release row's `value->'ticket'`/`'deps'` (`nativeReads.ts`) reads `id`/`dependencies`; `git grep -n "'ticket'\|'deps'\|'prog'\|releaseAuthoringOf\|ReleaseAuthoring\|defaultProgram\|materializeExecutionRequirement" src` shows nothing stale.
6. The wire (decision 4, survey §Release coupling): `git diff aaaff1ec..<tip> -- src/contract test/contract/contractDocument.json` — if anything moved, say what the console must do and whether a kept configuration revision or the drafts page could stop reading; `cd ui/chuggy-ui && npx tsc --noEmit && npx vitest run` green at the tip with no ui edit.
7. 013 whole: guard byte-identical to 008's; `decision_event_is_valid` admits exactly the codec's spellings (compare `src/generated/model-api.ts`, not S's prose); the two tables' CHECKs and grants; the boundary owner's `journal_entry` grant is column-scoped; `operation_completion_authority_is_its_boundary` without `'ExecutionBlocked'`; `wipe-tickets.sql` names both tables and the statement still runs without CASCADE.
8. B's own departures (`tasks/8a/B-report.md`), each with a failure named or ruled out: the writer **defers** a `Dispatch` whose observation evidence is transient instead of refusing it — say whether a remote that stays transient wedges the project's writer through the aging term the way 7b round 1's finding did (`pr7/reviews/7b-round1-machine.md`), and whether the operation ever answers its principal; `submit_worker_result` re-rendered so the source row lands before the completion reads it — confirm the sourceless overload still serves the fabric's worker (nothing under `images/`, `deploy/` or `~/claude/chuggy-fabric` calls the other) and that a result submitted twice cannot write two source rows; `ticket_command_is_valid` now refuses a public `Decide` carrying `Dispatch` — confirm `ManualDispatch`/`ProposeDispatch` still reach the writer; and the fixtures B narrowed to the refinement instance's one-stage one-evaluator plan — confirm a two-stage, two-evaluator plan (the rig's sanity-ticket shape) completes through the door on this branch in a scheduler suite, with its evaluators' `contextRef` equal to the work result's reference.
9. Comments and docs: true, in the tree's voice, no quantities, no stale path claims. Run `check-source`, `check-boundaries`, `check-queries`, `check-postgres`, `check-conformance`, `check-figures`, `check-comments`, `check-paths`, `check-duplication` at the tip.

Verdict to `~/claude/chuggy-effort/ticket-language/pr8/reviews/8a-round1-boundary.md`: APPROVE or CHANGES; each finding names file:line, the input and what goes wrong; under ~80 lines. Remove your worktree and drop your container when done. Write the verdict, reply with it, and stop.
