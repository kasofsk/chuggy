Step 8b of the ticket-language convergence (`chug-ticket-domain` at 76c95a9): **events and obligations.**

The journal carries what happened, not what was asked. A decision is the package's `{ event, obligations }`: seventeen `TicketEvent` arms (`TicketUpdated` arrives with 8c's `UpdateTicket`) and three obligations — `ExecuteTask`, `FinalizeTicket`, `CancelTask`. `StepRecord`, `Transition`, the effect strings and the positional pairing between them are gone from the model, the mirror, the row and the digest. `evolve` is the only thing that moves a ticket; each arm applies its event only to a state that still owes it and is the identity otherwise. Replay folds `evolve` instead of re-deciding, and a row is legal when its ticket stands and its event moves the prefix — sound because a decided event is never the identity (`eventsNeverIdentity`, an invariant the model checks); it does not re-check a decider's guard, since every row is decided under its guard before it is written. The inbox still carries commands (`DecisionEvent`, renamed in 8c) minus `WorkReduce` and `onFailure`. Decision semantics is 7.

The reduce folds into the completion: a work report decides the pass or the failure in one step, so `project_continuation`, its input class and its aging term are deleted. The evaluation-failure pick stays the writer's and is passed to `decide` as the policy; the event records which edge was taken (`TicketEvaluationReworkStarted` or `TicketEvaluationFailureEscalated`, with the rework evidence), so replay never consults it. Resume yields one of three events by the escalation. A task failure names its task (the package's `TaskFailure`); a finalization result carries evidence, the fold of the settled attempt's digest.

Durable identities take the obligation's kind word — `"<seq>:<index>:ExecuteTask"`, `…:CancelTask`, `…:FinalizeTicket`, and the desk `"<seq>:TicketEscalation"` — while a decision's tasks remain one execution request, one bundle and one capacity account, and the scheduler's request kinds are unchanged. Nothing parses an identity. The work-failure `evolve` arms are the model's one strengthening over the package: the failure must name the cycle the ticket is on, so a failure row for a replaced cycle is inert.

A dispatch whose source answers transient no longer defers forever: its passes are counted on `decision_input.deferred_passes` and it is refused `ExecutionSourceUnreadable` after `sourceDeferralPassesMax`, while a spent input stops being promoted by age.

**Migration 014.** Guard as 008–013: refuses while any journal row exists and names `deploy/rig/wipe-tickets.sql` — the script the previous release shipped, since this one no longer names `project_continuation`, which holds a key into the journal until 014 drops it. The journal row gains a validator it never had: a BEFORE INSERT trigger holding every entry to `{seq, event}` with `decision_event_is_valid`, rebuilt over the seventeen arms; the mailbox's command grammar is its own function. `project_continuation`, `publish_continuation` and the `Continuation` input kind are dropped; the desk's unique is one per decision; the release index reads `TicketCreated`; `submit_task_completion` is rewritten whole for the failure it reports.

No fabric or worker change beyond the release's digests: the pod carries the new identity strings opaquely. The console reads projections and executions, never the journal; the wire is untouched.

Survey, decisions, task reports and reviews: the effort directory's `pr8/`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
