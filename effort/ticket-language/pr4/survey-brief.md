# Survey brief — PR 4, Finalization Unavailable escalates

Read-only. Worktree: `~/claude/chuggy` at main e5f7b3d3 (do not edit; scratch under `/tmp/claude-1000/-home-geoff-claude-chuggy/6988229d-0563-475a-9255-7ce0b2467e85/scratchpad/survey4/`). Read first: `CLAUDE.md`, `~/claude/chuggy-effort/ticket-language/SPIKE.md` (the plan table row 4 and "Tier 2" bullet), `pr3/survey.md` (the shape and depth wanted), then the package at `~/claude/chuggy-effort/ticket-language/package/model/ticket-domain/ticket.qnt` (`FinalizationOperation`, `FinalizationResult`, `FinalizationEscalation`, `TicketFinalizationUnavailable`, `TicketFinalizationResumed`, `resumedFinalization`, `finalizationCurrent`) and its `ticket-decision-context.md` and `README.md` where they speak of finalization.

The step, in chuggy's terms (settled by the plan; the survey maps the surface, it does not reopen the step): `FinalizationOutcome` gains a third constructor `FinalizationResultUnavailable`; `Reason` gains `FinalizationUnavailableEscalated`; `decideFinalizationResult` on it escalates with `resumeAt: ResumeFinalization` and the label `ticket-escalated finalization_unavailable_escalated`; the existing `ResumeFinalization` continuation (`RunFinalizer` again) is the package's "generation + 1 on the same pinned input", realized as chuggy already realizes a finalization request. The interpreter's finalization **holds** (`FinalizationHoldKind` in `src/interpreter/finalizer.ts`, eighteen kinds) are what becomes the Unavailable result: today a held finalization leaves the ticket silently in Finalization.

Map, with file:line and counts, exactly as pr3/survey.md did:

1. **Model.** Every site that matches on `FinalizationOutcome` or draws it (`domain.qnt finalizationOutcomes`, refinement, api, tests, mc); every `Reason` roster and the step-label roster; what `check-model.sh`/`.test.sh` count that a new label or reason changes; the goldens whose manifest invariants name reasons; what `coverage.test.ts` regenerates.
2. **Generated and domain TS.** `modelTypes.ts`, `model-api.ts`, `src/domain/*` rosters (`finalizationOutcomeTags`, `reasonTags`/`escalationReasons`, step labels), and every switch over them that TypeScript will make exhaustive.
3. **Actor.** Does adding an outcome and a reason change how any stored row is read? Read `src/actor/decisionSemantics.ts`'s header rule for when the semantics version moves and say whether this PR moves it to 6 or leaves 5, with the argument.
4. **The holds today.** For each of the eighteen `FinalizationHoldKind`s: where it is produced, what row records it (reconciliation? attempt? `finalization_request.state`?), what the ticket's phase is while it stands, how an operator ends it today (native action? evidence supply? the console?), and what the finalizer does on the next claim. Then: which holds are honestly "the result is unavailable, resume re-runs the same operation" and which are something else (a human decision such as `ApprovalDeclined`; an exhausted budget; a conflict that is really `NeedsWork`). The survey's judgment here is the input to the goal's biggest decision; be concrete per kind.
5. **The writer path.** `submit_finalization_result` (baseline `functions.ts:3665`, replaced in 005 and 006): `in_outcome` roster, `in_failure_kind`, how a result reaches the actor (`decision_input` command → `decision_event_is_valid`), and `checkedFinalizationSubmission` in `wire.ts`. What a third outcome touches in each.
6. **Schema.** Every CHECK and function naming a `Reason` or a finalization outcome after 006 (`ticket_projection_reason_is_known`, `native_action`, `decision_event_is_valid`, `ticket_command_is_valid`, `public_ticket_command_is_valid`, `accept_operation`, `submit_finalization_result`); whether `native_action.reason` and the resume CHECK need the new value; whether `finalization_request` needs anything (it already has `request_generation`; say what it is set to and by whom). Migration is **007**; name the render-diff tool (`scratch/B-fix0/render.mjs`).
7. **Wire and contract.** `escalationReasons`, the finalization outcome roster on the wire, `ticketResponseSchema`, the events stream, the contract document and fixtures; `executionBlockedBy`'s analogue: does the reader need the hold kind as evidence (`finalizationBlockedBy?`), and where would it come from.
8. **Console.** Reason labels/sentences/tones/sections/resume points/actions for the new reason; where a held finalization is shown today (Finalizer panel? attempts?) and what changes when it becomes an escalation.
9. **Fabric and worker.** Whether anything outside this repo names a finalization outcome or a hold kind (`images/`, `.chug/configurations`, the fabric's ticket-service config — grep the fabric worktree `~/claude/chuggy-fabric-wt/no-accounts` too).
10. **Surprises.** Anything the plan row got wrong or did not know; anything that makes this larger than "the handoff removal" sized PR the plan promised.

Write `~/claude/chuggy-effort/ticket-language/pr4/survey.md` (under ~200 lines, dense, file:line everywhere) and hand back a summary of the surprises and the per-hold judgment in under 40 lines.
