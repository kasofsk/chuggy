# Task L — land PR 4 and release it to the rig

Worktree `~/claude/chuggy-wt/finunavail`, branch `model/finalization-unavailable`, tip f953d9e2 (origin/main d126c09a merged in). Do not edit code. Read first: `~/claude/chuggy-effort/ticket-language/pr4/GOAL.md`, `pr4/pr-body.md`, `pr3/GOAL.md` (its release lines are the recipe), `~/.claude/projects/-home-geoff-claude-chuggy/memory/rig-release-runbook.md`, `memory/chuggy-rig.md`, `memory/chuggy-false-reds.md`.

## Steps

1. **Wait for the full roster.** `~/claude/chuggy-effort/ticket-language/pr4/ci-full-f953d9e2.log` is being written by a run already started; poll (`until grep -q '^ci exit'`) until it ends. It must say `ci exit 0` with no gate at 1 or 2. If it does not, stop and report the failing gate's lines; attribute an environmental red per `chuggy-false-reds.md` before calling it a real one, and do not fix code.
2. **Open and merge.** `git push -u origin model/finalization-unavailable`; `gh pr create --title "a finalization nothing can carry out escalates the ticket" --body-file ~/claude/chuggy-effort/ticket-language/pr4/pr-body.md`; then `gh pr merge <n> --admin --merge --delete-branch`. Then `git -C ~/claude/chuggy pull --ff-only` and note the new main sha.
3. **Dump the rig first.** `ssh geoff@192.168.0.114 'kubectl -n chuggy exec postgres-0 -- pg_dump -U postgres -Fc chuggy > /home/geoff/backups/chuggy-pre-<sha>.dump'`; confirm its size is near the earlier dumps in that directory.
4. **Release phase 1.** From `~/claude/chuggy` at the new main: `CHUG_RIG_SSH=geoff@192.168.0.114 deploy/rig/deploy-to-gtr.sh > ~/claude/chuggy-effort/ticket-language/pr4/release-<sha>-phase1.log 2>&1`. It builds images and opens a chuggy-fabric PR "release: chuggy <sha>" and exits saying "not merged". Check `gh pr diff <n> --repo gdoteof/chuggy-fabric` mechanically: only `source-commit` stamps, image digests and the migrate job name may change.
5. **Release phase 2.** `gh pr merge <n> --repo gdoteof/chuggy-fabric --admin --merge --delete-branch`; then on the rig: `flux reconcile source git fabric -n flux-system; flux reconcile kustomization apps -n flux-system; kubectl -n chuggy wait --for=condition=complete job/chuggy-migrate-<sha>-registry --timeout=300s; kubectl -n chuggy logs job/chuggy-migrate-<sha>-registry` (expect migration 007 applied: it adds `finalization_request.hold_kind/hold_passes/held_since`, the reason/outcome CHECKs and `record_finalization_hold`); then `kubectl -n chuggy rollout status deploy/chuggy-<api|finalizer|scheduler|selector|ticket-service|worker-plane|ui> --timeout=180s` for each.
6. **Sanity checks**, all through `bash /home/geoff/api-call.sh <METHOD> <path>` on the rig (it appends `HTTP <code>` as the last line), under `P=/api/v1/tenants/vteng/projects/chuggy`: `GET $P/tickets/84` 200; `GET $P/drafts/84` 200 with a `brief`; `GET $P/drafts` 200; `GET $P/configurations/repository:a56cf7faf00a03a037411c8be4a43556f51c426a:fabric-rollout` 200; `curl -s -o /dev/null -w '%{http_code}' https://chuggy.vteng.io/` 200; no pod in CrashLoopBackOff (`kubectl -n chuggy get pods`; old Error pods of the importer cronjob from hours ago are known); `kubectl -n chuggy logs deploy/chuggy-<each> --since=5m` free of error lines; the schema on the rig has the three new columns (`psql -U postgres chuggy -c '\d finalization_request'` via `kubectl exec postgres-0`) and `select proname from pg_proc where proname='record_finalization_hold'` answers one row; ledger shows 7 applied (`select * from schema_migration order by 1` or whatever the ledger table is called — find it in `src/adapters/postgres/schema/`).
7. **Record.** Append a dated progress line to `pr4/GOAL.md` (PR number, main sha, fabric PR, migrate log summary, each check's result, dump path). Add a `PR 4` line to the `## Landed` list in `~/claude/chuggy-effort/ticket-language/SPIKE.md` in the style of the lines there. Remove the worktree: `git -C ~/claude/chuggy worktree remove --force ~/claude/chuggy-wt/finunavail` and `git -C ~/claude/chuggy branch -D model/finalization-unavailable` if it remains.

Never print a secret. Never `npm ci` under `ui/`. If anything in steps 2–6 fails, stop there, leave the rig as it is (do not roll back on your own), and report exactly what you saw with the command output.

## Report

`~/claude/chuggy-effort/ticket-language/pr4/tasks/L-report.md`, under ~40 lines: the PR number, main sha, fabric PR, migrate outcome, the table of checks, and anything that deviated. Reply with its contents.
