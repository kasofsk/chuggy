# Task F1 — round 1 findings

Worktree `~/claude/chuggy-wt/finunavail`, branch `model/finalization-unavailable`, tip b866a535 (the machine finding is already fixed there). Root `npm ci` only if stale. Read `CLAUDE.md`, `.chug/tasks/review-change.md`, then `~/claude/chuggy-effort/ticket-language/pr4/reviews/round1-surface.md` whole. Fix exactly its three findings, nothing else:

1. `test/interpreter/finalizerRun.test.ts`: a case (beside the dwell cases ending near :2892) that drives two requests in one pass — the first ending at `RepositoryUnbound`, the second preparable — and asserts the recorder saw `["RepositoryUnbound", null]`, i.e. the second request records no kind. Red-proof by deleting the `heldReason` reset at `src/interpreter/finalizerRun.ts:1766`: the case must fail; restore it.
2. `test/postgres/finalizerPrivileges.test.ts` at `:412` and `:432`: extend the two door cases to `record_finalization_hold` (SECURITY DEFINER, owned by the boundary owner, `search_path` pinned, executable by the finalizer role and not the api role), and rename them if their names now understate the surface. Red-proof once by editing the migration's function to drop the `search_path` pin in a scratch copy (revert).
3. `ui/chuggy-ui/app/core/codeLabels.ts:94`: `"Proposal base is already head"` becomes a noun phrase (`"Proposal base at head"`); update any test that asserts the string.

Gates: `check-source` (unit), `check-postgres`, `check-console`, `check-comments`. Report exits. One to three commits in the tree's voice ending `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Never push. Report to `~/claude/chuggy-effort/ticket-language/pr4/tasks/F1-report.md` under 20 lines and hand back tip and gate exits.
