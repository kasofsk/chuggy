# Review brief, round 1 (machine half) — model/finalization-unavailable, tip 8a4cd92d (Task C, console only, lands after this tip; not your half), off main e5f7b3d3

Worktree `~/claude/chuggy-wt/finunavail-review-machine` (detached at the tip; root `npm ci` only, never inside `ui/chuggy-ui/`). You did not write this change. Read `CLAUDE.md` and `.chug/tasks/review-change.md` first, then `GOAL.md` in `~/claude/chuggy-effort/ticket-language/pr4/` (the decisions; do not reopen them, judge whether the code keeps them), `survey.md` §1–§6, §10, then `git diff main...HEAD --stat` and the diff of your half: `model/`, `test/golden/`, `src/domain/`, `src/actor/`, `src/adapters/postgres/schema/`, `test/actor/`, `test/postgres/migration.test.ts`, `test/conformance/`, `test/random/`. Another reviewer holds the interpreter, adapters, contract and console.

Judge, in this order:
1. **The model change is exactly the step.** A third finalization outcome, a new reason, one decider arm escalating at `ResumeFinalization` with the new label; nothing else in `model/` (diff against main modulo those). The rebuilt resume test reaches `ResumeFinalization` through the wall, not by hand. The witness went into an existing gate test: is the roster count in `check-model.sh`/`.test.sh` still true?
2. **Goldens.** Re-emit all ten into scratch with `.chug/tasks/emit-goldens.sh` and diff byte for byte; the new one carries the wall and the resume in one trace; the six that moved moved for the stated reason (the third branch re-walks a seed) and not by hand; manifest invariants true.
3. **Actor.** Semantics stays 5: is the header's argument sound (no stored row can name either new value; every correction at ≤4 would be empty)? Trace a row carrying the new reason at 5 through `storedJournalLegalOn`. Would any row a pre-PR main wrote replay differently? Run `~/claude/chuggy-effort/ticket-language/pr3/rig/replay-rig.ts` against this branch's `src` over `pr3/rig/vteng.jsonl` and report.
4. **Migration 007** (`007-finalization-unavailable.ts`): the two reason CHECKs and three outcome lists exact; the three columns and their wholeness; `record_finalization_hold`'s semantics as GOAL.md states them (same kind +1 with `held_since` untouched; different kind resets to 1 and now(); null clears; claim fence; off-roster kind refused by the column CHECK); the third binding arm admits only a submission whose kind equals the request's recorded `hold_kind`, with no attempt, and nothing else got looser (mutate the arm to fence on nothing and find the test that reddens); the grants are exactly what the two roles need and no more (the finalizer role can call the function; the api role can read; neither can write the columns directly); no-guard argument true. Render-diff of 001–006 main vs branch empty (`~/claude/chuggy-effort/ticket-language/scratch/B-fix0/render.mjs`). Is there a `finalization_request` row the rig could hold today (survey §6) that 007 refuses?
5. **Docs and comments** at the tree's bar: no figure a reader must trust, no claim naming a path that is not there, the `schema/README.md` enumeration true.

A finding must name a failure that actually happens with concrete input. Verdict APPROVE or CHANGES with findings ranked; write it to `~/claude/chuggy-effort/ticket-language/pr4/reviews/round1-machine.md` and hand back the verdict and findings in under 30 lines.
