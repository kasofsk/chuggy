# Review brief, round 1 (surface half) — model/finalization-unavailable, tip 344d443b, off main e5f7b3d3

Worktree `~/claude/chuggy-wt/finunavail-review-surface` (detached at the tip; root `npm ci` only, never inside `ui/chuggy-ui/`). You did not write this change. Read `CLAUDE.md` and `.chug/tasks/review-change.md` first, then `GOAL.md` in `~/claude/chuggy-effort/ticket-language/pr4/` (the decisions; do not reopen them, judge whether the code keeps them), `survey.md` §4, §5, §7, §8, §10, the memory rule that console copy is nouns and one-word statuses (`~/.claude/projects/-home-geoff-claude-chuggy/memory/chuggy-ui-copy-standard.md`), `tasks/B-report.md`, `tasks/C-report.md`, then `git diff main...HEAD --stat` and the diff of your half: `src/interpreter/`, `src/adapters/` (not `schema/`), `src/contract/`, `ui/chuggy-ui/`, `test/interpreter/`, `test/adapters/`, `test/contract/`, `test/postgres/` (except `migration.test.ts`), `test/ui/`, `ui/chuggy-ui/test/`. Another reviewer holds the model, actor, migration and goldens.

Judge, in this order:
1. **The thirteen and the five.** `finalizationUnavailableKinds` is exactly GOAL.md's thirteen; the five excluded are named with their reasons and are provably the complement of `allFinalizationHoldKinds` (a test says so). Trace one hold from each group through the pass: a reachability hold records and counts; an exhausted budget records and counts; `ProposalMergeBlocked` after `TargetUnreadable` neither counts nor clears; a pass that concludes clears. Find the pass outcome that should clear and does not, or that clears and should not.
2. **The dwell.** `holdPassesMax` is read where `preparationRestartsMax` is, with a code default; the report fires when the returned count reaches it and not before; a hold that changes kind restarts the count; a hold outside the thirteen never submits. The supplied-adapter test proves it (mutate the comparison to `>` and find the red).
3. **The submission.** `FinalizationResultUnavailable` reaches `submit_finalization_result`'s third arm with the hold kind; the postgres suite drives it as the finalizer role (not superuser — the twelfth signature in `~/.claude/projects/-home-geoff-claude-chuggy/memory/guards-fail-open.md`); the actor's decision lands the projection at `FinalizationUnavailableEscalated` / `ResumeFinalization`; a Resume from there produces a new `finalization_request` at a later seq with hold columns empty (a fresh count). `readiness.ts` handles the outcome (what evidence, if any, and why); `checkedFinalizationSubmission` admits the third tag.
4. **The evidence reaches the reader.** `finalizationBlockedBy`: most recent request by `authorizing_seq`, only while the reason matches; the query runs under the api role in a suite; the response schema carries it optional; absent when not parked; the console draws its label beside Parked and never while the reason is another.
5. **The console** (sonnet-authored): the fourth reason has label/sentence/tone/section/resume point/action; the fallback at `ticketActions.ts` got an arm; the thirteen labels are short nouns; the copy-budget test covers them; `check-console` clean. Read C's judgment calls and say whether any is wrong.
6. **Docs and comments** at the tree's bar in your half.

A finding must name a failure that actually happens with concrete input. Verdict APPROVE or CHANGES with findings ranked; write it to `~/claude/chuggy-effort/ticket-language/pr4/reviews/round1-surface.md` and hand back the verdict and findings in under 30 lines.
