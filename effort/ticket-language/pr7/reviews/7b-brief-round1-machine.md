# Review round 1, machine half — PR 7b "The evaluation instance" (branch `model/evaluation-instance`)

You did not write this change. Review it fresh in a detached worktree of your own at the tip named in `reviews/7b-ledger.md`:

    git -C ~/claude/chuggy worktree add --detach ~/claude/chuggy-wt/evinst-review-machine <tip>
    ln -s ~/claude/chuggy/node_modules ~/claude/chuggy-wt/evinst-review-machine/node_modules

Never `npm ci` under `ui/`. Your half: `model/`, `test/golden`, `src/generated`, `src/domain`, `src/actor`, `src/interpreter`, `src/adapters`, `src/contract`, `src/adapters/postgres/schema/migrations/012-task-report.ts`, and their tests. The surface half (`ui/`, `test/ui`) is another reviewer's. Diff: `git diff f8d6c8fd..<tip> -- <your paths>`.

Read first: `~/claude/chuggy-effort/ticket-language/pr7/GOAL.md` §"PR 7b — decisions" and the progress lines that correct them (decision 3 was corrected after S), `pr7/survey.md` §1, §3, §4, §5 and surprises 1, 5–9, 11, `pr7/tasks/7b/{A,S,B}-report.md`, the package's `~/claude/chuggy-effort/ticket-language/package/model/ticket-domain/evaluation/evaluation.qnt`, `.chug/tasks/review-change.md`, `CLAUDE.md`.

## What to check, each with a failure that actually happens

1. The copy: `diff` the package's `evaluation.qnt` against `model/ticket-domain/evaluation/evaluation.qnt` and confirm the delta is exactly the six hunks `model/AGENTS.md` lists (four decided, two forced by them), each attributed to PR 8. Any other edit is a finding, however small.
2. The instance on the ticket: a program of two stages, the second sparse, through dispatch, both evaluators of stage 1 resolving, stage 2, the pass, a rework after `EvaluationFailed`, and a second instance for the second cycle. `idsAccounted` holds after every step; every identity minted is new across instances; a stale `TaskDone` naming a retired identity is not enabled; the rework cap counts a work cycle only when its predecessor's instance ended `EvaluationFailed`.
3. Blocked and resume: kill one evaluator of a two-evaluator stage (`TerminalFailureReport{kind: ProcessFailure}` and `{kind: ExecutionUnavailableFailure}`, one case each); the stage concludes `EvaluationBlocked` only after the other's report; the ticket parks at `EvaluationBlockedEscalated`; `ResumeEvaluation` spawns only the blocked evaluator at generation 2 and the kept pass is not re-asked; a resume before the park, and a resume re-asking a passed evaluator, are not enabled. A work task's `ExecutionUnavailableFailure` still walls the ticket.
4. Goldens re-emitted, not hand-edited (`emit-goldens.sh` byte for byte); the walk in `test/random/draws.ts` draws every report kind for an evaluator and reaches a park and a resume; `check-conformance`, `check-random` at the tip.
5. The mint and the scheduler: `requestTasks` stays injective and monotone per ticket across a resume at generation 2 and across two instances; `executionScheduler.ts` no longer drains siblings and its blocked path submits a per-task report; the exhausted-retry path in `schedulerCompletion.ts` reaches `submit_task_completion` with the outcome B added, and no other producer of that outcome exists. The adapter's `Pass → EvaluatorPass`, `Fail → EvaluatorFail` map is in one place.
6. 012: guard byte-identical to 008's; `decision_event_is_valid`'s `TaskDone` arm admits exactly the codec's three report spellings (compare against `src/generated/model-api.ts`, not against S's prose: no `ticket` in a value, `evidence` on a failure, `onFailure` on the event) and refuses `verdict`; no `EvalReduce`/`ExecutionBlocked` arm remains; the exhausted-retry outcome's arm journals `TerminalFailureReport{kind: ProcessFailure}`; re-prove three of S's red-proofs and B's by mutation; render-diff 001–011 main vs branch empty; `check-postgres` reaches the new arms through at least one scheduler suite (a wall, a kill, a resume), not only the migration suite's literals.
7. Every stored-text reader (`wire.ts storedSchedulerCompletion`, the projection, the dispatch candidate) parses the new `TaskDone`; `git grep -n 'EvalReduce\|ExecutionBlocked\|stageGeneration\|Verdict\b'` on the branch is empty outside the migration ledger's earlier files, or every hit is justified.
8. Comments and docs: true, in the tree's voice, no quantities, no stale path claims; `model/AGENTS.md` names the divergences and nothing that landed. Run `check-figures`, `check-comments`, `check-paths`, `check-boundaries`, `check-queries`, `check-postgres`, `check-source` at the tip.

Verdict to `~/claude/chuggy-effort/ticket-language/pr7/reviews/7b-round1-machine.md`: APPROVE or CHANGES; each finding names file:line, the input and what goes wrong; under ~80 lines. Remove your worktree and drop any database you made. Write the verdict, reply with it, and stop.

## Addendum (orchestrator, after B)

Two of B's decisions (`tasks/7b/B-report.md`) departed from the briefs and are yours to weigh, each with a failure named or ruled out:

- **The disposition is stamped by the writer, not carried through the door.** The boundary journals `{ticket, task, report}` into the mailbox; 012 refuses a named `onFailure`; `projectWriter.ts` stamps `reworkDisposition(held, cap)` when it serializes the decision event, so the decision journal carries it and a replay reproduces it. Check that a replayed journal reproduces the same disposition the writer took (the replay reads the event, not the cap), that a cap changed between two runs cannot make a replay disagree with its journal, and that no principal can offer a `TaskDone` through the client surface.
- **A spawn no dispatch can source is deferred, not parked.** `ExecutionBlocked` parked such a ticket; now nothing does. B names a live case: a finalizer-rework work spawn's bundle carries `TargetCommit` and no `Repository`, so the next evaluation spawn's `observe` answers `RefUnreadable` and the ticket sits in Evaluation forever with no execution and no escalation. Reproduce it or refute it (main parks; this branch defers), and if it is real say what the smallest fix is: the bundle carrying `Repository`, or a spawn that cannot be sourced reporting `TerminalFailureReport{kind: ExecutionUnavailableFailure}` for the task it could not start. That is a finding, not a nit.
