# Mutation sweep — PR 7b "The evaluation instance" (branch `model/evaluation-instance`)

You did not write this change. Fresh detached worktree of your own at the tip named in `reviews/7b-ledger.md` under "Tip for sweep":

    git -C ~/claude/chuggy worktree add --detach ~/claude/chuggy-wt/evinst-sweep <tip>
    ln -s ~/claude/chuggy/node_modules ~/claude/chuggy-wt/evinst-sweep/node_modules
    ln -s ~/claude/chuggy/ui/chuggy-ui/node_modules ~/claude/chuggy-wt/evinst-sweep/ui/chuggy-ui/node_modules

Never `npm ci` under `ui/`. Read: `~/claude/chuggy-effort/ticket-language/pr7/GOAL.md`, `pr7/tasks/7b/{A,S,B,C}-report.md`, `pr7/reviews/7b-round1-{machine,surface}.md` and any fix reports the ledger names, `pr7/reviews/7a-sweep.md` (the shape and depth wanted), `~/.claude/projects/-home-geoff-claude-chuggy/memory/scoped-iteration-gates.md`, `.chug/tasks/review-change.md`.

Whole-branch mutation sweep over `git diff f8d6c8fd..<tip>`: for every added or changed behaviour in the model, the domain, the actor, the interpreter, the adapters, 012, the contract and the console, apply one plausible mutation (`begin` appending to the wrong ticket, `applyProduced` on a retired identity admitted, `concludeStage` blocking on a fail or failing on a block, `resumeBlocked` re-asking every evaluator or keeping the generation, `idsAccounted` counting the current run twice or not at all, the rework cap counting every cycle, the mint repeating a number across instances, the adapter mapping `Fail` to a block, the exhausted-retry outcome routed to `EvaluatorFail`, the arm admitting `verdict` or a fourth report kind or a report without a kind, the blocked path journalling the wrong task, the console merging generations or keying totals without the cycle) and run the narrowest gate or suite that should catch it; record RED or SURVIVED with the mutation, the file:line and the case. Goldens' step bodies are the model's output: mutate the manifest and the draws, not the traces. Survivors are findings only when they name a behaviour defect or a proof gap a cheap case closes. Docs pass: every comment the diff touches reads true against the code beside it; the copy at `model/ticket-domain/evaluation/evaluation.qnt` differs from the package's only by the six hunks `model/AGENTS.md` names. Fabric alignment: nothing under `.chug/`, `images/`, `deploy/` or in `~/claude/chuggy-fabric` names `EvalReduce`, `ExecutionBlocked`, a journal verdict, or `submit_task_completion`'s old outcome roster.

Verdict to `~/claude/chuggy-effort/ticket-language/pr7/reviews/7b-sweep.md`: APPROVE or CHANGES; the mutation table; findings with file:line; under ~120 lines. Remove your worktree and drop any database you made. Write the verdict, reply with the verdict, the counts and the findings only, and stop.

## Addendum (orchestrator)

Round 1 (`7b-round1-{machine,surface}.md`) was fixed by D (`tasks/7b/D-report.md`, console), E (`tasks/7b/E-report.md`, interpreter, adapters, 012's first DDL) and one orchestrator commit (2acc04d1, the console drawing `ProcessFailed` as a stop for an evaluator and a failure for a work task). You are the fresh eyes on those fixes; include each in the table. Two deserve your judgement, with a failure named or ruled out:

- **E's park.** The reviewer found a durable unreadable source at a rework spawn deferred forever and wedged the project's writer. E's fix re-decides that rework completion with `EscalateEvaluationFailure`, so the desk shows `EvaluationFailureEscalated` carrying the git evidence and a resume re-enters work and re-observes (`projectWriter.ts projectWriterUnreadableLanding`, `journaledPlan`). Say whether any durable unreadable input can still defer forever (E argues a continuation's source is the project's own history, so a durable unreadable there is a missing row, never an outage), whether the escalation name misleads a reader in a way the evidence does not cure, and mutate the transient guard and the override.
- **Every generation a row (D).** A superseded generation stays on the ledger, dimmed, and the rows sum to the cycle's rollup; the resumed-stage notice names the highest-numbered stage past generation one that is running. Mutate the fold to keep only the highest generation and confirm the sum case reddens.
