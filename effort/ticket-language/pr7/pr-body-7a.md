Step 7a of the ticket-language convergence (`chug-ticket-domain` at 76c95a9): **the program is a plan.** The first half of step 7; the evaluation instance follows as 7b.

A stage stops being a width. `StageDefinition` is `{key, evaluators: [{key}]}` in the package's spelling (minus the evaluator's task definition, which arrives with the released ticket), so the evaluation protocol's own file can replace the types without a rename. An evaluation task's identity names the evaluator's authored key, and keys need not be dense: `{1, 3}` is a well-formed stage, the goldens carry one, and the random walk draws them. Three places that assumed evaluator keys ran one to a count are fixed: the model retires a live set by key, the mirror holds the live set to the stage's roster, and the interpreter mints the wire integer by a task's position in its set rather than by its key, which is the one arithmetic that stays injective under a sparse stage. A stage's key is its position, chuggy's rule until the instance stores a stage index; the invariant states it and the migration's arm holds it.

The wire authors `{key, evaluators}` per stage; the draft initialization offers the evaluator-key bound and the stage bound in place of a page of widths; the console picks a count and mints dense keys. The configuration is untouched: every evaluator of a stage runs the stage's block, as N identical evaluators did.

**Migration 011.** Guard as 008–010: refuses while any journal row exists and names `deploy/rig/wipe-tickets.sql`. `decision_event_is_valid`'s `CreateTicket` arm admits positional stage keys with unique positive evaluator keys and refuses a fan-out; the stored-stage bound widened, so the mailbox bound is re-rendered and the token budget re-seeded as 009 did. No column moves; the evaluator column now carries the key.

No fabric or worker release: the worker reads a task kind and never a stage, an evaluator or a width.

Survey, decisions, task reports and reviews: `~/claude/chuggy-effort/ticket-language/pr7/`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
