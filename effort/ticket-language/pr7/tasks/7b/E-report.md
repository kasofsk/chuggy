# Task E (PR 7b) — round 1 machine fixes

Tip `10cb02ac` on `fix/evaluation-instance-machine` (six commits off 64a95abe). All ten gates green on the tip: source, boundaries, queries, postgres, conformance, random, duplication, figures, comments, paths.

## 1a — the specific case does not reproduce (`7703b102`)
A `FinalizationNeedsWork` bundle does carry `Repository`: `inputBundleReferencesOf`'s evidence branch spreads `...evidence.preparation`, and `finalizerBundleOf` puts `{kind:"Repository"}` first in the attempt's own bundle. Pinned end to end by a new `test/postgres/finalizerRework.test.ts` case that walks a finalizer rework into its evaluation and runs the real `executionSourceObservation` over `postgresExecutionSourceHistory`, asserting the observed source. Red-proved by deleting that spread: the mutant yields exactly the reviewer's predicted `{observed:"Unreadable", evidence:"RefUnreadable"}`.

## 1b — the general wall, as a park rather than per-task reports (`10cb02ac`)
The briefed shape is not expressible: a `TaskDone` needs a live task, tasks exist only once the spawn is journalled, one decision journals one event, and `submit_task_completion` requires an execution row. So the writer uses the authority it already has — which edge a failing judgement is taken on is its own pick. A durable unreadable under a completion (the only completion that spawns is a rework) re-decides that same completion with `EscalateEvaluationFailure`: one entry, `EvaluationFailureEscalated` on the desk carrying the git evidence (`projectionEvidence` already admits `gitEvidences`), and `resumeOf` re-enters work and re-observes. `projectWriterUnreadableLanding` returns a third landing, `journaledPlan` takes the wall's evidence as an override, and the doc is split across the type and the function (two sentences each, the cap); the sentence saying nothing parks such a ticket is gone. A continuation still defers: its source is the work's own bundle, never a remote, so a durable unreadable there is a missing row in this project's history rather than an outage — which is also why the briefed two-evaluator pin has no reachable input. Client operations still refuse.
`test/interpreter/dispatchWriter.test.ts`: "a rework no source can be read for parks the ticket in the one quantum" (every durable evidence, asserting the row, its evidence, and that the run did not defer) and "a rework meeting a source that may read later is deferred still". Red-proved one term at a time: the disposition, the evidence override and the transient guard each redden a case; the `TaskDone` narrowing is type-forced.
**Did the quantum loop need anything else?** No. `projectTicketWriterRun` returns from the whole quantum on `Deferred`, and `postgresReadinessConsumable` takes `DISTINCT ON (base_priority)` heads aged by `floor(age/agingInterval)`, so a durably-deferred head outranks every other class and starves the project. Committing the park is the whole fix; the deferrals that remain are transient, as on main.

## 2 — the privileges guard (`edc20ba5`)
Both fixtures at `privileges.test.ts:347-350` were shapes 012 deleted, so the tag arm was never reached. Replaced with one 012-spelled completion and a flattened loop. Re-proved by patching the tag list: the case goes red.

## 3 — escalation evidence gating (`895cb063`)
The wall's evidence is attached only when the post state's escalation is the wall's own (`WorkExecutionUnavailableEscalated`, `EvaluationBlockedEscalated`). Case: "a stage that failed beside a walled evaluator parks carrying no wall" — a two-evaluator stage, one failed and one walled, parks `EvaluationFailureEscalated` with no evidence.

## 4 — `model/AGENTS.md` (`5b050583`)
The delta sentence now names what the divergences drop (the package's task definition, and the source ref a work result is accepted at), which is true of all six hunks; the old "producer of a task definition" claim was false of the `acceptedSourceRef` pair.

## 5 — `ProcessFailed` on the execution row (`26ceaf74`)
012's first DDL re-renders `execution_outcome_is_known` with `ProcessFailed` (header says so), `submit_task_completion` folds `outcome = in_outcome` with the `CASE … THEN 'Failed'` gone, and `allExecutionOutcomes`, `executionOutcomes`, the rosters case and `contractDocument.json` follow. `schedulerSettle` passes one outcome to both the submit and the terminalization. Suite expectations updated in `schedulerStore` and the 012 door case; the door mutation was red-proved by mutating before `prepare`.
**`ui/` sites (not touched, for the orchestrator after D):** `ui/chuggy-ui/app/core/tones.ts:81-95` `executionTone` — exhaustive switch, stops compiling without a `ProcessFailed` arm; `ui/chuggy-ui/app/browser/ticket/TicketLedger.tsx:408-420` `summaryVerdict` — has `summary.identity.type`, so it can draw a work task's as Failed and an evaluator's as Blocked; `ui/chuggy-ui/app/core/projectTableRows.ts:137-144` `projectTableExecutionPhrase` — renders the raw word into "Terminal · ProcessFailed", against the one-word copy standard; `ui/chuggy-ui/test/tones.test.ts:85-86` iterates `executionOutcomes` and will exercise the new value.
